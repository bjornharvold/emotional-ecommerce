this error crawler is for lela internal dev only as it hammers the server with 5ms delays. use it only on local dev for maximum efficiency.

run:
install node (http://nodejs.org), run 'node crawler.js'

juho.hietala@lela.com
