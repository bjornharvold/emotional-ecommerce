// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.SeriesType;
import com.lela.data.domain.entity.SeriesTypeDataOnDemand;
import com.lela.data.domain.entity.SeriesTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SeriesTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SeriesTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SeriesTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SeriesTypeIntegrationTest: @Transactional;
    
    @Autowired
    SeriesTypeDataOnDemand SeriesTypeIntegrationTest.dod;
    
    @Test
    public void SeriesTypeIntegrationTest.testCountSeriesTypes() {
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to initialize correctly", dod.getRandomSeriesType());
        long count = SeriesType.countSeriesTypes();
        Assert.assertTrue("Counter for 'SeriesType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SeriesTypeIntegrationTest.testFindSeriesType() {
        SeriesType obj = dod.getRandomSeriesType();
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to provide an identifier", id);
        obj = SeriesType.findSeriesType(id);
        Assert.assertNotNull("Find method for 'SeriesType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SeriesType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SeriesTypeIntegrationTest.testFindAllSeriesTypes() {
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to initialize correctly", dod.getRandomSeriesType());
        long count = SeriesType.countSeriesTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'SeriesType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SeriesType> result = SeriesType.findAllSeriesTypes();
        Assert.assertNotNull("Find all method for 'SeriesType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SeriesType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SeriesTypeIntegrationTest.testFindSeriesTypeEntries() {
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to initialize correctly", dod.getRandomSeriesType());
        long count = SeriesType.countSeriesTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SeriesType> result = SeriesType.findSeriesTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SeriesType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SeriesType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SeriesTypeIntegrationTest.testFlush() {
        SeriesType obj = dod.getRandomSeriesType();
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to provide an identifier", id);
        obj = SeriesType.findSeriesType(id);
        Assert.assertNotNull("Find method for 'SeriesType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifySeriesType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'SeriesType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SeriesTypeIntegrationTest.testMergeUpdate() {
        SeriesType obj = dod.getRandomSeriesType();
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to provide an identifier", id);
        obj = SeriesType.findSeriesType(id);
        boolean modified =  dod.modifySeriesType(obj);
        Integer currentVersion = obj.getVersion();
        SeriesType merged = (SeriesType)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'SeriesType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SeriesTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to initialize correctly", dod.getRandomSeriesType());
        SeriesType obj = dod.getNewTransientSeriesType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SeriesType' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'SeriesType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SeriesTypeIntegrationTest.testRemove() {
        SeriesType obj = dod.getRandomSeriesType();
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SeriesType' failed to provide an identifier", id);
        obj = SeriesType.findSeriesType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SeriesType' with identifier '" + id + "'", SeriesType.findSeriesType(id));
    }
    
}
