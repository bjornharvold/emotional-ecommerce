// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemRecall;
import com.lela.data.domain.entity.ItemRecallDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ItemRecallDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ItemRecallDataOnDemand: @Component;
    
    private Random ItemRecallDataOnDemand.rnd = new SecureRandom();
    
    private List<ItemRecall> ItemRecallDataOnDemand.data;
    
    public ItemRecall ItemRecallDataOnDemand.getNewTransientItemRecall(int index) {
        ItemRecall obj = new ItemRecall();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setItem(obj, index);
        setRecallDate(obj, index);
        setRecallEvent(obj, index);
        setRecallHazard(obj, index);
        setRecallIncidentsInjuries(obj, index);
        setRecallSeverity(obj, index);
        setRecallUnits(obj, index);
        setRecallUrl(obj, index);
        return obj;
    }
    
    public void ItemRecallDataOnDemand.setDateCreated(ItemRecall obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ItemRecallDataOnDemand.setDateModified(ItemRecall obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ItemRecallDataOnDemand.setRecallEvent(ItemRecall obj, int index) {
        String recallEvent = "recallEvent_" + index;
        obj.setRecallEvent(recallEvent);
    }
    
    public void ItemRecallDataOnDemand.setRecallHazard(ItemRecall obj, int index) {
        String recallHazard = "recallHazard_" + index;
        obj.setRecallHazard(recallHazard);
    }
    
    public void ItemRecallDataOnDemand.setRecallIncidentsInjuries(ItemRecall obj, int index) {
        String recallIncidentsInjuries = "recallIncidentsInjuries_" + index;
        obj.setRecallIncidentsInjuries(recallIncidentsInjuries);
    }
    
    public void ItemRecallDataOnDemand.setRecallSeverity(ItemRecall obj, int index) {
        String recallSeverity = "recallSeverity_" + index;
        obj.setRecallSeverity(recallSeverity);
    }
    
    public void ItemRecallDataOnDemand.setRecallUnits(ItemRecall obj, int index) {
        String recallUnits = "recallUnits_" + index;
        obj.setRecallUnits(recallUnits);
    }
    
    public void ItemRecallDataOnDemand.setRecallUrl(ItemRecall obj, int index) {
        String recallUrl = "recallUrl_" + index;
        obj.setRecallUrl(recallUrl);
    }
    
    public ItemRecall ItemRecallDataOnDemand.getSpecificItemRecall(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ItemRecall obj = data.get(index);
        Long id = obj.getId();
        return ItemRecall.findItemRecall(id);
    }
    
    public ItemRecall ItemRecallDataOnDemand.getRandomItemRecall() {
        init();
        ItemRecall obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ItemRecall.findItemRecall(id);
    }
    
    public boolean ItemRecallDataOnDemand.modifyItemRecall(ItemRecall obj) {
        return false;
    }
    
    public void ItemRecallDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ItemRecall.findItemRecallEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ItemRecall' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ItemRecall>();
        for (int i = 0; i < 10; i++) {
            ItemRecall obj = getNewTransientItemRecall(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
