// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryDataSourceSeriesType;
import com.lela.data.domain.entity.CategoryDataSourceSeriesTypeDataOnDemand;
import com.lela.data.domain.entity.CategoryDataSourceSeriesTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoryDataSourceSeriesTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoryDataSourceSeriesTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoryDataSourceSeriesTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoryDataSourceSeriesTypeIntegrationTest: @Transactional;
    
    @Autowired
    CategoryDataSourceSeriesTypeDataOnDemand CategoryDataSourceSeriesTypeIntegrationTest.dod;
    
    @Test
    public void CategoryDataSourceSeriesTypeIntegrationTest.testCountCategoryDataSourceSeriesTypes() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to initialize correctly", dod.getRandomCategoryDataSourceSeriesType());
        long count = CategoryDataSourceSeriesType.countCategoryDataSourceSeriesTypes();
        Assert.assertTrue("Counter for 'CategoryDataSourceSeriesType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoryDataSourceSeriesTypeIntegrationTest.testFindCategoryDataSourceSeriesType() {
        CategoryDataSourceSeriesType obj = dod.getRandomCategoryDataSourceSeriesType();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to provide an identifier", id);
        obj = CategoryDataSourceSeriesType.findCategoryDataSourceSeriesType(id);
        Assert.assertNotNull("Find method for 'CategoryDataSourceSeriesType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategoryDataSourceSeriesType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoryDataSourceSeriesTypeIntegrationTest.testFindAllCategoryDataSourceSeriesTypes() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to initialize correctly", dod.getRandomCategoryDataSourceSeriesType());
        long count = CategoryDataSourceSeriesType.countCategoryDataSourceSeriesTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategoryDataSourceSeriesType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategoryDataSourceSeriesType> result = CategoryDataSourceSeriesType.findAllCategoryDataSourceSeriesTypes();
        Assert.assertNotNull("Find all method for 'CategoryDataSourceSeriesType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategoryDataSourceSeriesType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoryDataSourceSeriesTypeIntegrationTest.testFindCategoryDataSourceSeriesTypeEntries() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to initialize correctly", dod.getRandomCategoryDataSourceSeriesType());
        long count = CategoryDataSourceSeriesType.countCategoryDataSourceSeriesTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategoryDataSourceSeriesType> result = CategoryDataSourceSeriesType.findCategoryDataSourceSeriesTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategoryDataSourceSeriesType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategoryDataSourceSeriesType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoryDataSourceSeriesTypeIntegrationTest.testFlush() {
        CategoryDataSourceSeriesType obj = dod.getRandomCategoryDataSourceSeriesType();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to provide an identifier", id);
        obj = CategoryDataSourceSeriesType.findCategoryDataSourceSeriesType(id);
        Assert.assertNotNull("Find method for 'CategoryDataSourceSeriesType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategoryDataSourceSeriesType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategoryDataSourceSeriesType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryDataSourceSeriesTypeIntegrationTest.testMergeUpdate() {
        CategoryDataSourceSeriesType obj = dod.getRandomCategoryDataSourceSeriesType();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to provide an identifier", id);
        obj = CategoryDataSourceSeriesType.findCategoryDataSourceSeriesType(id);
        boolean modified =  dod.modifyCategoryDataSourceSeriesType(obj);
        Integer currentVersion = obj.getVersion();
        CategoryDataSourceSeriesType merged = (CategoryDataSourceSeriesType)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategoryDataSourceSeriesType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryDataSourceSeriesTypeIntegrationTest.testRemove() {
        CategoryDataSourceSeriesType obj = dod.getRandomCategoryDataSourceSeriesType();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceSeriesType' failed to provide an identifier", id);
        obj = CategoryDataSourceSeriesType.findCategoryDataSourceSeriesType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategoryDataSourceSeriesType' with identifier '" + id + "'", CategoryDataSourceSeriesType.findCategoryDataSourceSeriesType(id));
    }
    
}
