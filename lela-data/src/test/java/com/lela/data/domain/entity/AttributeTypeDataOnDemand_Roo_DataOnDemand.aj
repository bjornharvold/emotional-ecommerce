// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AttributeType;
import com.lela.data.domain.entity.AttributeTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AttributeTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AttributeTypeDataOnDemand: @Component;
    
    private Random AttributeTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<AttributeType> AttributeTypeDataOnDemand.data;
    
    public void AttributeTypeDataOnDemand.setAccessory(AttributeType obj, int index) {
        Boolean accessory = Boolean.TRUE;
        obj.setAccessory(accessory);
    }
    
    public void AttributeTypeDataOnDemand.setAttributeName(AttributeType obj, int index) {
        String attributeName = "attributeName_" + index;
        obj.setAttributeName(attributeName);
    }
    
    public void AttributeTypeDataOnDemand.setAttributeSourceName(AttributeType obj, int index) {
        String attributeSourceName = "attributeSourceName_" + index;
        obj.setAttributeSourceName(attributeSourceName);
    }
    
    public void AttributeTypeDataOnDemand.setDateCreated(AttributeType obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void AttributeTypeDataOnDemand.setDateModified(AttributeType obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void AttributeTypeDataOnDemand.setLelaAttributeName(AttributeType obj, int index) {
        String lelaAttributeName = "lelaAttributeName_" + index;
        obj.setLelaAttributeName(lelaAttributeName);
    }
    
    public AttributeType AttributeTypeDataOnDemand.getSpecificAttributeType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AttributeType obj = data.get(index);
        Long id = obj.getId();
        return AttributeType.findAttributeType(id);
    }
    
    public AttributeType AttributeTypeDataOnDemand.getRandomAttributeType() {
        init();
        AttributeType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return AttributeType.findAttributeType(id);
    }
    
    public boolean AttributeTypeDataOnDemand.modifyAttributeType(AttributeType obj) {
        return false;
    }
    
    public void AttributeTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AttributeType.findAttributeTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AttributeType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AttributeType>();
        for (int i = 0; i < 10; i++) {
            AttributeType obj = getNewTransientAttributeType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
