// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Navbar;
import com.lela.data.domain.entity.NavbarDataOnDemand;
import com.lela.data.domain.entity.NavbarIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect NavbarIntegrationTest_Roo_IntegrationTest {
    
    declare @type: NavbarIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: NavbarIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: NavbarIntegrationTest: @Transactional;
    
    @Autowired
    NavbarDataOnDemand NavbarIntegrationTest.dod;
    
    @Test
    public void NavbarIntegrationTest.testCountNavbars() {
        Assert.assertNotNull("Data on demand for 'Navbar' failed to initialize correctly", dod.getRandomNavbar());
        long count = Navbar.countNavbars();
        Assert.assertTrue("Counter for 'Navbar' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void NavbarIntegrationTest.testFindNavbar() {
        Navbar obj = dod.getRandomNavbar();
        Assert.assertNotNull("Data on demand for 'Navbar' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Navbar' failed to provide an identifier", id);
        obj = Navbar.findNavbar(id);
        Assert.assertNotNull("Find method for 'Navbar' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Navbar' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void NavbarIntegrationTest.testFindAllNavbars() {
        Assert.assertNotNull("Data on demand for 'Navbar' failed to initialize correctly", dod.getRandomNavbar());
        long count = Navbar.countNavbars();
        Assert.assertTrue("Too expensive to perform a find all test for 'Navbar', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Navbar> result = Navbar.findAllNavbars();
        Assert.assertNotNull("Find all method for 'Navbar' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Navbar' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void NavbarIntegrationTest.testFindNavbarEntries() {
        Assert.assertNotNull("Data on demand for 'Navbar' failed to initialize correctly", dod.getRandomNavbar());
        long count = Navbar.countNavbars();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Navbar> result = Navbar.findNavbarEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Navbar' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Navbar' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void NavbarIntegrationTest.testFlush() {
        Navbar obj = dod.getRandomNavbar();
        Assert.assertNotNull("Data on demand for 'Navbar' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Navbar' failed to provide an identifier", id);
        obj = Navbar.findNavbar(id);
        Assert.assertNotNull("Find method for 'Navbar' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyNavbar(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Navbar' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void NavbarIntegrationTest.testMergeUpdate() {
        Navbar obj = dod.getRandomNavbar();
        Assert.assertNotNull("Data on demand for 'Navbar' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Navbar' failed to provide an identifier", id);
        obj = Navbar.findNavbar(id);
        boolean modified =  dod.modifyNavbar(obj);
        Integer currentVersion = obj.getVersion();
        Navbar merged = (Navbar)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Navbar' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void NavbarIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Navbar' failed to initialize correctly", dod.getRandomNavbar());
        Navbar obj = dod.getNewTransientNavbar(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Navbar' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Navbar' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Navbar' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void NavbarIntegrationTest.testRemove() {
        Navbar obj = dod.getRandomNavbar();
        Assert.assertNotNull("Data on demand for 'Navbar' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Navbar' failed to provide an identifier", id);
        obj = Navbar.findNavbar(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Navbar' with identifier '" + id + "'", Navbar.findNavbar(id));
    }
    
}
