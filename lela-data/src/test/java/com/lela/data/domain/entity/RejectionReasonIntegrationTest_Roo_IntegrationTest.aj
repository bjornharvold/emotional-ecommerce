// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.RejectionReason;
import com.lela.data.domain.entity.RejectionReasonDataOnDemand;
import com.lela.data.domain.entity.RejectionReasonIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect RejectionReasonIntegrationTest_Roo_IntegrationTest {
    
    declare @type: RejectionReasonIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: RejectionReasonIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: RejectionReasonIntegrationTest: @Transactional;
    
    @Autowired
    RejectionReasonDataOnDemand RejectionReasonIntegrationTest.dod;
    
    @Test
    public void RejectionReasonIntegrationTest.testCountRejectionReasons() {
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to initialize correctly", dod.getRandomRejectionReason());
        long count = RejectionReason.countRejectionReasons();
        Assert.assertTrue("Counter for 'RejectionReason' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void RejectionReasonIntegrationTest.testFindRejectionReason() {
        RejectionReason obj = dod.getRandomRejectionReason();
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to provide an identifier", id);
        obj = RejectionReason.findRejectionReason(id);
        Assert.assertNotNull("Find method for 'RejectionReason' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'RejectionReason' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void RejectionReasonIntegrationTest.testFindAllRejectionReasons() {
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to initialize correctly", dod.getRandomRejectionReason());
        long count = RejectionReason.countRejectionReasons();
        Assert.assertTrue("Too expensive to perform a find all test for 'RejectionReason', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<RejectionReason> result = RejectionReason.findAllRejectionReasons();
        Assert.assertNotNull("Find all method for 'RejectionReason' illegally returned null", result);
        Assert.assertTrue("Find all method for 'RejectionReason' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void RejectionReasonIntegrationTest.testFindRejectionReasonEntries() {
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to initialize correctly", dod.getRandomRejectionReason());
        long count = RejectionReason.countRejectionReasons();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<RejectionReason> result = RejectionReason.findRejectionReasonEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'RejectionReason' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'RejectionReason' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void RejectionReasonIntegrationTest.testFlush() {
        RejectionReason obj = dod.getRandomRejectionReason();
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to provide an identifier", id);
        obj = RejectionReason.findRejectionReason(id);
        Assert.assertNotNull("Find method for 'RejectionReason' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyRejectionReason(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'RejectionReason' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void RejectionReasonIntegrationTest.testMergeUpdate() {
        RejectionReason obj = dod.getRandomRejectionReason();
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to provide an identifier", id);
        obj = RejectionReason.findRejectionReason(id);
        boolean modified =  dod.modifyRejectionReason(obj);
        Integer currentVersion = obj.getVersion();
        RejectionReason merged = (RejectionReason)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'RejectionReason' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void RejectionReasonIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to initialize correctly", dod.getRandomRejectionReason());
        RejectionReason obj = dod.getNewTransientRejectionReason(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'RejectionReason' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'RejectionReason' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void RejectionReasonIntegrationTest.testRemove() {
        RejectionReason obj = dod.getRandomRejectionReason();
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RejectionReason' failed to provide an identifier", id);
        obj = RejectionReason.findRejectionReason(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'RejectionReason' with identifier '" + id + "'", RejectionReason.findRejectionReason(id));
    }
    
}
