// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductDetailSection;
import com.lela.data.domain.entity.ProductDetailSectionDataOnDemand;
import com.lela.data.domain.entity.ProductDetailSectionIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductDetailSectionIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductDetailSectionIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductDetailSectionIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductDetailSectionIntegrationTest: @Transactional;
    
    @Autowired
    ProductDetailSectionDataOnDemand ProductDetailSectionIntegrationTest.dod;
    
    @Test
    public void ProductDetailSectionIntegrationTest.testCountProductDetailSections() {
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to initialize correctly", dod.getRandomProductDetailSection());
        long count = ProductDetailSection.countProductDetailSections();
        Assert.assertTrue("Counter for 'ProductDetailSection' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductDetailSectionIntegrationTest.testFindProductDetailSection() {
        ProductDetailSection obj = dod.getRandomProductDetailSection();
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to provide an identifier", id);
        obj = ProductDetailSection.findProductDetailSection(id);
        Assert.assertNotNull("Find method for 'ProductDetailSection' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductDetailSection' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductDetailSectionIntegrationTest.testFindAllProductDetailSections() {
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to initialize correctly", dod.getRandomProductDetailSection());
        long count = ProductDetailSection.countProductDetailSections();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductDetailSection', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductDetailSection> result = ProductDetailSection.findAllProductDetailSections();
        Assert.assertNotNull("Find all method for 'ProductDetailSection' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductDetailSection' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductDetailSectionIntegrationTest.testFindProductDetailSectionEntries() {
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to initialize correctly", dod.getRandomProductDetailSection());
        long count = ProductDetailSection.countProductDetailSections();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductDetailSection> result = ProductDetailSection.findProductDetailSectionEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductDetailSection' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductDetailSection' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductDetailSectionIntegrationTest.testFlush() {
        ProductDetailSection obj = dod.getRandomProductDetailSection();
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to provide an identifier", id);
        obj = ProductDetailSection.findProductDetailSection(id);
        Assert.assertNotNull("Find method for 'ProductDetailSection' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProductDetailSection(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProductDetailSection' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductDetailSectionIntegrationTest.testMergeUpdate() {
        ProductDetailSection obj = dod.getRandomProductDetailSection();
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to provide an identifier", id);
        obj = ProductDetailSection.findProductDetailSection(id);
        boolean modified =  dod.modifyProductDetailSection(obj);
        Integer currentVersion = obj.getVersion();
        ProductDetailSection merged = (ProductDetailSection)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProductDetailSection' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductDetailSectionIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to initialize correctly", dod.getRandomProductDetailSection());
        ProductDetailSection obj = dod.getNewTransientProductDetailSection(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProductDetailSection' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductDetailSection' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductDetailSectionIntegrationTest.testRemove() {
        ProductDetailSection obj = dod.getRandomProductDetailSection();
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailSection' failed to provide an identifier", id);
        obj = ProductDetailSection.findProductDetailSection(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductDetailSection' with identifier '" + id + "'", ProductDetailSection.findProductDetailSection(id));
    }
    
}
