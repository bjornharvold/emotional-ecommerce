// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryDataSourceSeriesType;
import com.lela.data.domain.entity.CategoryDataSourceSeriesTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect CategoryDataSourceSeriesTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CategoryDataSourceSeriesTypeDataOnDemand: @Component;
    
    private Random CategoryDataSourceSeriesTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<CategoryDataSourceSeriesType> CategoryDataSourceSeriesTypeDataOnDemand.data;
    
    public CategoryDataSourceSeriesType CategoryDataSourceSeriesTypeDataOnDemand.getNewTransientCategoryDataSourceSeriesType(int index) {
        CategoryDataSourceSeriesType obj = new CategoryDataSourceSeriesType();
        setCategoryDataSource(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setEnableSeries(obj, index);
        setSeriesType(obj, index);
        return obj;
    }
    
    public void CategoryDataSourceSeriesTypeDataOnDemand.setDateCreated(CategoryDataSourceSeriesType obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void CategoryDataSourceSeriesTypeDataOnDemand.setDateModified(CategoryDataSourceSeriesType obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void CategoryDataSourceSeriesTypeDataOnDemand.setEnableSeries(CategoryDataSourceSeriesType obj, int index) {
        Boolean enableSeries = Boolean.TRUE;
        obj.setEnableSeries(enableSeries);
    }
    
    public CategoryDataSourceSeriesType CategoryDataSourceSeriesTypeDataOnDemand.getSpecificCategoryDataSourceSeriesType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CategoryDataSourceSeriesType obj = data.get(index);
        Long id = obj.getId();
        return CategoryDataSourceSeriesType.findCategoryDataSourceSeriesType(id);
    }
    
    public CategoryDataSourceSeriesType CategoryDataSourceSeriesTypeDataOnDemand.getRandomCategoryDataSourceSeriesType() {
        init();
        CategoryDataSourceSeriesType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CategoryDataSourceSeriesType.findCategoryDataSourceSeriesType(id);
    }
    
    public boolean CategoryDataSourceSeriesTypeDataOnDemand.modifyCategoryDataSourceSeriesType(CategoryDataSourceSeriesType obj) {
        return false;
    }
    
    public void CategoryDataSourceSeriesTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CategoryDataSourceSeriesType.findCategoryDataSourceSeriesTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CategoryDataSourceSeriesType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CategoryDataSourceSeriesType>();
        for (int i = 0; i < 10; i++) {
            CategoryDataSourceSeriesType obj = getNewTransientCategoryDataSourceSeriesType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
