// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemColorMerchant;
import com.lela.data.domain.entity.ItemColorMerchantDataOnDemand;
import com.lela.data.domain.entity.ItemColorMerchantIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ItemColorMerchantIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ItemColorMerchantIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ItemColorMerchantIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ItemColorMerchantIntegrationTest: @Transactional;
    
    @Autowired
    ItemColorMerchantDataOnDemand ItemColorMerchantIntegrationTest.dod;
    
    @Test
    public void ItemColorMerchantIntegrationTest.testCountItemColorMerchants() {
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to initialize correctly", dod.getRandomItemColorMerchant());
        long count = ItemColorMerchant.countItemColorMerchants();
        Assert.assertTrue("Counter for 'ItemColorMerchant' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ItemColorMerchantIntegrationTest.testFindItemColorMerchant() {
        ItemColorMerchant obj = dod.getRandomItemColorMerchant();
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to provide an identifier", id);
        obj = ItemColorMerchant.findItemColorMerchant(id);
        Assert.assertNotNull("Find method for 'ItemColorMerchant' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ItemColorMerchant' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ItemColorMerchantIntegrationTest.testFindAllItemColorMerchants() {
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to initialize correctly", dod.getRandomItemColorMerchant());
        long count = ItemColorMerchant.countItemColorMerchants();
        Assert.assertTrue("Too expensive to perform a find all test for 'ItemColorMerchant', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ItemColorMerchant> result = ItemColorMerchant.findAllItemColorMerchants();
        Assert.assertNotNull("Find all method for 'ItemColorMerchant' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ItemColorMerchant' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ItemColorMerchantIntegrationTest.testFindItemColorMerchantEntries() {
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to initialize correctly", dod.getRandomItemColorMerchant());
        long count = ItemColorMerchant.countItemColorMerchants();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ItemColorMerchant> result = ItemColorMerchant.findItemColorMerchantEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ItemColorMerchant' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ItemColorMerchant' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ItemColorMerchantIntegrationTest.testFlush() {
        ItemColorMerchant obj = dod.getRandomItemColorMerchant();
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to provide an identifier", id);
        obj = ItemColorMerchant.findItemColorMerchant(id);
        Assert.assertNotNull("Find method for 'ItemColorMerchant' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyItemColorMerchant(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ItemColorMerchant' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemColorMerchantIntegrationTest.testMergeUpdate() {
        ItemColorMerchant obj = dod.getRandomItemColorMerchant();
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to provide an identifier", id);
        obj = ItemColorMerchant.findItemColorMerchant(id);
        boolean modified =  dod.modifyItemColorMerchant(obj);
        Integer currentVersion = obj.getVersion();
        ItemColorMerchant merged = (ItemColorMerchant)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ItemColorMerchant' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemColorMerchantIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to initialize correctly", dod.getRandomItemColorMerchant());
        ItemColorMerchant obj = dod.getNewTransientItemColorMerchant(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ItemColorMerchant' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ItemColorMerchant' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ItemColorMerchantIntegrationTest.testRemove() {
        ItemColorMerchant obj = dod.getRandomItemColorMerchant();
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemColorMerchant' failed to provide an identifier", id);
        obj = ItemColorMerchant.findItemColorMerchant(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ItemColorMerchant' with identifier '" + id + "'", ItemColorMerchant.findItemColorMerchant(id));
    }
    
}
