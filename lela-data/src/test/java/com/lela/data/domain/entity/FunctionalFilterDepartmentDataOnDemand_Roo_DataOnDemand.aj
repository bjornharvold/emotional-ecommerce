// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.FunctionalFilterDepartment;
import com.lela.data.domain.entity.FunctionalFilterDepartmentDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect FunctionalFilterDepartmentDataOnDemand_Roo_DataOnDemand {
    
    declare @type: FunctionalFilterDepartmentDataOnDemand: @Component;
    
    private Random FunctionalFilterDepartmentDataOnDemand.rnd = new SecureRandom();
    
    private List<FunctionalFilterDepartment> FunctionalFilterDepartmentDataOnDemand.data;
    
    public FunctionalFilterDepartment FunctionalFilterDepartmentDataOnDemand.getNewTransientFunctionalFilterDepartment(int index) {
        FunctionalFilterDepartment obj = new FunctionalFilterDepartment();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setDepartment(obj, index);
        setFunctionalFilter(obj, index);
        setObjectId(obj, index);
        return obj;
    }
    
    public void FunctionalFilterDepartmentDataOnDemand.setDateCreated(FunctionalFilterDepartment obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void FunctionalFilterDepartmentDataOnDemand.setDateModified(FunctionalFilterDepartment obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void FunctionalFilterDepartmentDataOnDemand.setObjectId(FunctionalFilterDepartment obj, int index) {
        String objectId = "objectId_" + index;
        obj.setObjectId(objectId);
    }
    
    public FunctionalFilterDepartment FunctionalFilterDepartmentDataOnDemand.getSpecificFunctionalFilterDepartment(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        FunctionalFilterDepartment obj = data.get(index);
        Long id = obj.getId();
        return FunctionalFilterDepartment.findFunctionalFilterDepartment(id);
    }
    
    public FunctionalFilterDepartment FunctionalFilterDepartmentDataOnDemand.getRandomFunctionalFilterDepartment() {
        init();
        FunctionalFilterDepartment obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return FunctionalFilterDepartment.findFunctionalFilterDepartment(id);
    }
    
    public boolean FunctionalFilterDepartmentDataOnDemand.modifyFunctionalFilterDepartment(FunctionalFilterDepartment obj) {
        return false;
    }
    
    public void FunctionalFilterDepartmentDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = FunctionalFilterDepartment.findFunctionalFilterDepartmentEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'FunctionalFilterDepartment' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<FunctionalFilterDepartment>();
        for (int i = 0; i < 10; i++) {
            FunctionalFilterDepartment obj = getNewTransientFunctionalFilterDepartment(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
