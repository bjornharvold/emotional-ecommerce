// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.MerchantOfferKeyword;
import com.lela.data.domain.entity.MerchantOfferKeywordDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect MerchantOfferKeywordDataOnDemand_Roo_DataOnDemand {
    
    declare @type: MerchantOfferKeywordDataOnDemand: @Component;
    
    private Random MerchantOfferKeywordDataOnDemand.rnd = new SecureRandom();
    
    private List<MerchantOfferKeyword> MerchantOfferKeywordDataOnDemand.data;
    
    public MerchantOfferKeyword MerchantOfferKeywordDataOnDemand.getNewTransientMerchantOfferKeyword(int index) {
        MerchantOfferKeyword obj = new MerchantOfferKeyword();
        setActionType(obj, index);
        setCategory(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setOfferKeyword(obj, index);
        return obj;
    }
    
    public void MerchantOfferKeywordDataOnDemand.setDateCreated(MerchantOfferKeyword obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void MerchantOfferKeywordDataOnDemand.setDateModified(MerchantOfferKeyword obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void MerchantOfferKeywordDataOnDemand.setOfferKeyword(MerchantOfferKeyword obj, int index) {
        String offerKeyword = "offerKeyword_" + index;
        obj.setOfferKeyword(offerKeyword);
    }
    
    public MerchantOfferKeyword MerchantOfferKeywordDataOnDemand.getSpecificMerchantOfferKeyword(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        MerchantOfferKeyword obj = data.get(index);
        Long id = obj.getId();
        return MerchantOfferKeyword.findMerchantOfferKeyword(id);
    }
    
    public MerchantOfferKeyword MerchantOfferKeywordDataOnDemand.getRandomMerchantOfferKeyword() {
        init();
        MerchantOfferKeyword obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return MerchantOfferKeyword.findMerchantOfferKeyword(id);
    }
    
    public boolean MerchantOfferKeywordDataOnDemand.modifyMerchantOfferKeyword(MerchantOfferKeyword obj) {
        return false;
    }
    
    public void MerchantOfferKeywordDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = MerchantOfferKeyword.findMerchantOfferKeywordEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'MerchantOfferKeyword' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<MerchantOfferKeyword>();
        for (int i = 0; i < 10; i++) {
            MerchantOfferKeyword obj = getNewTransientMerchantOfferKeyword(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
