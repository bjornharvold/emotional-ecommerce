// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AffiliateReport;
import com.lela.data.domain.entity.AffiliateReportDataOnDemand;
import com.lela.data.domain.entity.AffiliateReportIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AffiliateReportIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AffiliateReportIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AffiliateReportIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AffiliateReportIntegrationTest: @Transactional;
    
    @Autowired
    AffiliateReportDataOnDemand AffiliateReportIntegrationTest.dod;
    
    @Test
    public void AffiliateReportIntegrationTest.testCountAffiliateReports() {
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to initialize correctly", dod.getRandomAffiliateReport());
        long count = AffiliateReport.countAffiliateReports();
        Assert.assertTrue("Counter for 'AffiliateReport' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AffiliateReportIntegrationTest.testFindAffiliateReport() {
        AffiliateReport obj = dod.getRandomAffiliateReport();
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to provide an identifier", id);
        obj = AffiliateReport.findAffiliateReport(id);
        Assert.assertNotNull("Find method for 'AffiliateReport' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AffiliateReport' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AffiliateReportIntegrationTest.testFindAllAffiliateReports() {
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to initialize correctly", dod.getRandomAffiliateReport());
        long count = AffiliateReport.countAffiliateReports();
        Assert.assertTrue("Too expensive to perform a find all test for 'AffiliateReport', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AffiliateReport> result = AffiliateReport.findAllAffiliateReports();
        Assert.assertNotNull("Find all method for 'AffiliateReport' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AffiliateReport' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AffiliateReportIntegrationTest.testFindAffiliateReportEntries() {
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to initialize correctly", dod.getRandomAffiliateReport());
        long count = AffiliateReport.countAffiliateReports();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AffiliateReport> result = AffiliateReport.findAffiliateReportEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AffiliateReport' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AffiliateReport' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AffiliateReportIntegrationTest.testFlush() {
        AffiliateReport obj = dod.getRandomAffiliateReport();
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to provide an identifier", id);
        obj = AffiliateReport.findAffiliateReport(id);
        Assert.assertNotNull("Find method for 'AffiliateReport' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAffiliateReport(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AffiliateReport' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AffiliateReportIntegrationTest.testMergeUpdate() {
        AffiliateReport obj = dod.getRandomAffiliateReport();
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to provide an identifier", id);
        obj = AffiliateReport.findAffiliateReport(id);
        boolean modified =  dod.modifyAffiliateReport(obj);
        Integer currentVersion = obj.getVersion();
        AffiliateReport merged = (AffiliateReport)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'AffiliateReport' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AffiliateReportIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to initialize correctly", dod.getRandomAffiliateReport());
        AffiliateReport obj = dod.getNewTransientAffiliateReport(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AffiliateReport' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AffiliateReport' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AffiliateReportIntegrationTest.testRemove() {
        AffiliateReport obj = dod.getRandomAffiliateReport();
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AffiliateReport' failed to provide an identifier", id);
        obj = AffiliateReport.findAffiliateReport(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AffiliateReport' with identifier '" + id + "'", AffiliateReport.findAffiliateReport(id));
    }
    
}
