// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.SiteStatus;
import com.lela.data.domain.entity.SiteStatusDataOnDemand;
import com.lela.data.domain.entity.SiteStatusIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SiteStatusIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SiteStatusIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SiteStatusIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SiteStatusIntegrationTest: @Transactional;
    
    @Autowired
    SiteStatusDataOnDemand SiteStatusIntegrationTest.dod;
    
    @Test
    public void SiteStatusIntegrationTest.testCountSiteStatuses() {
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to initialize correctly", dod.getRandomSiteStatus());
        long count = SiteStatus.countSiteStatuses();
        Assert.assertTrue("Counter for 'SiteStatus' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SiteStatusIntegrationTest.testFindSiteStatus() {
        SiteStatus obj = dod.getRandomSiteStatus();
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to provide an identifier", id);
        obj = SiteStatus.findSiteStatus(id);
        Assert.assertNotNull("Find method for 'SiteStatus' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SiteStatus' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SiteStatusIntegrationTest.testFindAllSiteStatuses() {
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to initialize correctly", dod.getRandomSiteStatus());
        long count = SiteStatus.countSiteStatuses();
        Assert.assertTrue("Too expensive to perform a find all test for 'SiteStatus', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SiteStatus> result = SiteStatus.findAllSiteStatuses();
        Assert.assertNotNull("Find all method for 'SiteStatus' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SiteStatus' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SiteStatusIntegrationTest.testFindSiteStatusEntries() {
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to initialize correctly", dod.getRandomSiteStatus());
        long count = SiteStatus.countSiteStatuses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SiteStatus> result = SiteStatus.findSiteStatusEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SiteStatus' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SiteStatus' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SiteStatusIntegrationTest.testFlush() {
        SiteStatus obj = dod.getRandomSiteStatus();
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to provide an identifier", id);
        obj = SiteStatus.findSiteStatus(id);
        Assert.assertNotNull("Find method for 'SiteStatus' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifySiteStatus(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'SiteStatus' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SiteStatusIntegrationTest.testMergeUpdate() {
        SiteStatus obj = dod.getRandomSiteStatus();
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to provide an identifier", id);
        obj = SiteStatus.findSiteStatus(id);
        boolean modified =  dod.modifySiteStatus(obj);
        Integer currentVersion = obj.getVersion();
        SiteStatus merged = (SiteStatus)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'SiteStatus' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SiteStatusIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to initialize correctly", dod.getRandomSiteStatus());
        SiteStatus obj = dod.getNewTransientSiteStatus(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SiteStatus' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'SiteStatus' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SiteStatusIntegrationTest.testRemove() {
        SiteStatus obj = dod.getRandomSiteStatus();
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SiteStatus' failed to provide an identifier", id);
        obj = SiteStatus.findSiteStatus(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SiteStatus' with identifier '" + id + "'", SiteStatus.findSiteStatus(id));
    }
    
}
