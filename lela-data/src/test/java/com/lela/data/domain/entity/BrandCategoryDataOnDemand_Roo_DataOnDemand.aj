// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.BrandCategory;
import com.lela.data.domain.entity.BrandCategoryDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect BrandCategoryDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BrandCategoryDataOnDemand: @Component;
    
    private Random BrandCategoryDataOnDemand.rnd = new SecureRandom();
    
    private List<BrandCategory> BrandCategoryDataOnDemand.data;
    
    public BrandCategory BrandCategoryDataOnDemand.getNewTransientBrandCategory(int index) {
        BrandCategory obj = new BrandCategory();
        setBrand(obj, index);
        setCategory(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setEnableLoad(obj, index);
        return obj;
    }
    
    public void BrandCategoryDataOnDemand.setDateCreated(BrandCategory obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void BrandCategoryDataOnDemand.setDateModified(BrandCategory obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void BrandCategoryDataOnDemand.setEnableLoad(BrandCategory obj, int index) {
        Boolean enableLoad = Boolean.TRUE;
        obj.setEnableLoad(enableLoad);
    }
    
    public BrandCategory BrandCategoryDataOnDemand.getSpecificBrandCategory(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        BrandCategory obj = data.get(index);
        Long id = obj.getId();
        return BrandCategory.findBrandCategory(id);
    }
    
    public BrandCategory BrandCategoryDataOnDemand.getRandomBrandCategory() {
        init();
        BrandCategory obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return BrandCategory.findBrandCategory(id);
    }
    
    public boolean BrandCategoryDataOnDemand.modifyBrandCategory(BrandCategory obj) {
        return false;
    }
    
    public void BrandCategoryDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = BrandCategory.findBrandCategoryEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'BrandCategory' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<BrandCategory>();
        for (int i = 0; i < 10; i++) {
            BrandCategory obj = getNewTransientBrandCategory(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
