// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryGroup;
import com.lela.data.domain.entity.CategoryGroupDataOnDemand;
import com.lela.data.domain.entity.CategoryGroupIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoryGroupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoryGroupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoryGroupIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoryGroupIntegrationTest: @Transactional;
    
    @Autowired
    CategoryGroupDataOnDemand CategoryGroupIntegrationTest.dod;
    
    @Test
    public void CategoryGroupIntegrationTest.testCountCategoryGroups() {
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to initialize correctly", dod.getRandomCategoryGroup());
        long count = CategoryGroup.countCategoryGroups();
        Assert.assertTrue("Counter for 'CategoryGroup' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoryGroupIntegrationTest.testFindCategoryGroup() {
        CategoryGroup obj = dod.getRandomCategoryGroup();
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to provide an identifier", id);
        obj = CategoryGroup.findCategoryGroup(id);
        Assert.assertNotNull("Find method for 'CategoryGroup' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategoryGroup' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoryGroupIntegrationTest.testFindAllCategoryGroups() {
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to initialize correctly", dod.getRandomCategoryGroup());
        long count = CategoryGroup.countCategoryGroups();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategoryGroup', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategoryGroup> result = CategoryGroup.findAllCategoryGroups();
        Assert.assertNotNull("Find all method for 'CategoryGroup' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategoryGroup' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoryGroupIntegrationTest.testFindCategoryGroupEntries() {
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to initialize correctly", dod.getRandomCategoryGroup());
        long count = CategoryGroup.countCategoryGroups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategoryGroup> result = CategoryGroup.findCategoryGroupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategoryGroup' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategoryGroup' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoryGroupIntegrationTest.testFlush() {
        CategoryGroup obj = dod.getRandomCategoryGroup();
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to provide an identifier", id);
        obj = CategoryGroup.findCategoryGroup(id);
        Assert.assertNotNull("Find method for 'CategoryGroup' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategoryGroup(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategoryGroup' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryGroupIntegrationTest.testMergeUpdate() {
        CategoryGroup obj = dod.getRandomCategoryGroup();
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to provide an identifier", id);
        obj = CategoryGroup.findCategoryGroup(id);
        boolean modified =  dod.modifyCategoryGroup(obj);
        Integer currentVersion = obj.getVersion();
        CategoryGroup merged = (CategoryGroup)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategoryGroup' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryGroupIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to initialize correctly", dod.getRandomCategoryGroup());
        CategoryGroup obj = dod.getNewTransientCategoryGroup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CategoryGroup' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'CategoryGroup' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CategoryGroupIntegrationTest.testRemove() {
        CategoryGroup obj = dod.getRandomCategoryGroup();
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryGroup' failed to provide an identifier", id);
        obj = CategoryGroup.findCategoryGroup(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategoryGroup' with identifier '" + id + "'", CategoryGroup.findCategoryGroup(id));
    }
    
}
