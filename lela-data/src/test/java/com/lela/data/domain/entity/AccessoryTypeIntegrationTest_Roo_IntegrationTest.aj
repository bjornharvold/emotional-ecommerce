// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AccessoryType;
import com.lela.data.domain.entity.AccessoryTypeDataOnDemand;
import com.lela.data.domain.entity.AccessoryTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AccessoryTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AccessoryTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AccessoryTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AccessoryTypeIntegrationTest: @Transactional;
    
    @Autowired
    AccessoryTypeDataOnDemand AccessoryTypeIntegrationTest.dod;
    
    @Test
    public void AccessoryTypeIntegrationTest.testCountAccessoryTypes() {
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to initialize correctly", dod.getRandomAccessoryType());
        long count = AccessoryType.countAccessoryTypes();
        Assert.assertTrue("Counter for 'AccessoryType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AccessoryTypeIntegrationTest.testFindAccessoryType() {
        AccessoryType obj = dod.getRandomAccessoryType();
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to provide an identifier", id);
        obj = AccessoryType.findAccessoryType(id);
        Assert.assertNotNull("Find method for 'AccessoryType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AccessoryType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AccessoryTypeIntegrationTest.testFindAllAccessoryTypes() {
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to initialize correctly", dod.getRandomAccessoryType());
        long count = AccessoryType.countAccessoryTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'AccessoryType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AccessoryType> result = AccessoryType.findAllAccessoryTypes();
        Assert.assertNotNull("Find all method for 'AccessoryType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AccessoryType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AccessoryTypeIntegrationTest.testFindAccessoryTypeEntries() {
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to initialize correctly", dod.getRandomAccessoryType());
        long count = AccessoryType.countAccessoryTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AccessoryType> result = AccessoryType.findAccessoryTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AccessoryType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AccessoryType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AccessoryTypeIntegrationTest.testFlush() {
        AccessoryType obj = dod.getRandomAccessoryType();
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to provide an identifier", id);
        obj = AccessoryType.findAccessoryType(id);
        Assert.assertNotNull("Find method for 'AccessoryType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAccessoryType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AccessoryType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AccessoryTypeIntegrationTest.testMergeUpdate() {
        AccessoryType obj = dod.getRandomAccessoryType();
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to provide an identifier", id);
        obj = AccessoryType.findAccessoryType(id);
        boolean modified =  dod.modifyAccessoryType(obj);
        Integer currentVersion = obj.getVersion();
        AccessoryType merged = (AccessoryType)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'AccessoryType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AccessoryTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to initialize correctly", dod.getRandomAccessoryType());
        AccessoryType obj = dod.getNewTransientAccessoryType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AccessoryType' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AccessoryType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AccessoryTypeIntegrationTest.testRemove() {
        AccessoryType obj = dod.getRandomAccessoryType();
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AccessoryType' failed to provide an identifier", id);
        obj = AccessoryType.findAccessoryType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AccessoryType' with identifier '" + id + "'", AccessoryType.findAccessoryType(id));
    }
    
}
