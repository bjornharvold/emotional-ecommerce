// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.VideoType;
import com.lela.data.domain.entity.VideoTypeDataOnDemand;
import com.lela.data.domain.entity.VideoTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect VideoTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: VideoTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: VideoTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: VideoTypeIntegrationTest: @Transactional;
    
    @Autowired
    VideoTypeDataOnDemand VideoTypeIntegrationTest.dod;
    
    @Test
    public void VideoTypeIntegrationTest.testCountVideoTypes() {
        Assert.assertNotNull("Data on demand for 'VideoType' failed to initialize correctly", dod.getRandomVideoType());
        long count = VideoType.countVideoTypes();
        Assert.assertTrue("Counter for 'VideoType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void VideoTypeIntegrationTest.testFindVideoType() {
        VideoType obj = dod.getRandomVideoType();
        Assert.assertNotNull("Data on demand for 'VideoType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'VideoType' failed to provide an identifier", id);
        obj = VideoType.findVideoType(id);
        Assert.assertNotNull("Find method for 'VideoType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'VideoType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void VideoTypeIntegrationTest.testFindAllVideoTypes() {
        Assert.assertNotNull("Data on demand for 'VideoType' failed to initialize correctly", dod.getRandomVideoType());
        long count = VideoType.countVideoTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'VideoType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<VideoType> result = VideoType.findAllVideoTypes();
        Assert.assertNotNull("Find all method for 'VideoType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'VideoType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void VideoTypeIntegrationTest.testFindVideoTypeEntries() {
        Assert.assertNotNull("Data on demand for 'VideoType' failed to initialize correctly", dod.getRandomVideoType());
        long count = VideoType.countVideoTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<VideoType> result = VideoType.findVideoTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'VideoType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'VideoType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void VideoTypeIntegrationTest.testFlush() {
        VideoType obj = dod.getRandomVideoType();
        Assert.assertNotNull("Data on demand for 'VideoType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'VideoType' failed to provide an identifier", id);
        obj = VideoType.findVideoType(id);
        Assert.assertNotNull("Find method for 'VideoType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyVideoType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'VideoType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void VideoTypeIntegrationTest.testMergeUpdate() {
        VideoType obj = dod.getRandomVideoType();
        Assert.assertNotNull("Data on demand for 'VideoType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'VideoType' failed to provide an identifier", id);
        obj = VideoType.findVideoType(id);
        boolean modified =  dod.modifyVideoType(obj);
        Integer currentVersion = obj.getVersion();
        VideoType merged = (VideoType)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'VideoType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void VideoTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'VideoType' failed to initialize correctly", dod.getRandomVideoType());
        VideoType obj = dod.getNewTransientVideoType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'VideoType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'VideoType' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'VideoType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void VideoTypeIntegrationTest.testRemove() {
        VideoType obj = dod.getRandomVideoType();
        Assert.assertNotNull("Data on demand for 'VideoType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'VideoType' failed to provide an identifier", id);
        obj = VideoType.findVideoType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'VideoType' with identifier '" + id + "'", VideoType.findVideoType(id));
    }
    
}
