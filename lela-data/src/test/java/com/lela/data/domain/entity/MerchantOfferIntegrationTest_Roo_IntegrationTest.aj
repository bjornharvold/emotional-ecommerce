// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.MerchantOffer;
import com.lela.data.domain.entity.MerchantOfferDataOnDemand;
import com.lela.data.domain.entity.MerchantOfferIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect MerchantOfferIntegrationTest_Roo_IntegrationTest {
    
    declare @type: MerchantOfferIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: MerchantOfferIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: MerchantOfferIntegrationTest: @Transactional;
    
    @Autowired
    MerchantOfferDataOnDemand MerchantOfferIntegrationTest.dod;
    
    @Test
    public void MerchantOfferIntegrationTest.testCountMerchantOffers() {
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to initialize correctly", dod.getRandomMerchantOffer());
        long count = MerchantOffer.countMerchantOffers();
        Assert.assertTrue("Counter for 'MerchantOffer' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void MerchantOfferIntegrationTest.testFindMerchantOffer() {
        MerchantOffer obj = dod.getRandomMerchantOffer();
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to provide an identifier", id);
        obj = MerchantOffer.findMerchantOffer(id);
        Assert.assertNotNull("Find method for 'MerchantOffer' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'MerchantOffer' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void MerchantOfferIntegrationTest.testFindAllMerchantOffers() {
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to initialize correctly", dod.getRandomMerchantOffer());
        long count = MerchantOffer.countMerchantOffers();
        Assert.assertTrue("Too expensive to perform a find all test for 'MerchantOffer', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<MerchantOffer> result = MerchantOffer.findAllMerchantOffers();
        Assert.assertNotNull("Find all method for 'MerchantOffer' illegally returned null", result);
        Assert.assertTrue("Find all method for 'MerchantOffer' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void MerchantOfferIntegrationTest.testFindMerchantOfferEntries() {
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to initialize correctly", dod.getRandomMerchantOffer());
        long count = MerchantOffer.countMerchantOffers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<MerchantOffer> result = MerchantOffer.findMerchantOfferEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'MerchantOffer' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'MerchantOffer' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void MerchantOfferIntegrationTest.testFlush() {
        MerchantOffer obj = dod.getRandomMerchantOffer();
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to provide an identifier", id);
        obj = MerchantOffer.findMerchantOffer(id);
        Assert.assertNotNull("Find method for 'MerchantOffer' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyMerchantOffer(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'MerchantOffer' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MerchantOfferIntegrationTest.testMergeUpdate() {
        MerchantOffer obj = dod.getRandomMerchantOffer();
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to provide an identifier", id);
        obj = MerchantOffer.findMerchantOffer(id);
        boolean modified =  dod.modifyMerchantOffer(obj);
        Integer currentVersion = obj.getVersion();
        MerchantOffer merged = (MerchantOffer)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'MerchantOffer' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MerchantOfferIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to initialize correctly", dod.getRandomMerchantOffer());
        MerchantOffer obj = dod.getNewTransientMerchantOffer(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'MerchantOffer' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'MerchantOffer' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void MerchantOfferIntegrationTest.testRemove() {
        MerchantOffer obj = dod.getRandomMerchantOffer();
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantOffer' failed to provide an identifier", id);
        obj = MerchantOffer.findMerchantOffer(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'MerchantOffer' with identifier '" + id + "'", MerchantOffer.findMerchantOffer(id));
    }
    
}
