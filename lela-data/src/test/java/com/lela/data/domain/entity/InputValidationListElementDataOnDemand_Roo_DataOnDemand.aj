// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.InputValidationListElement;
import com.lela.data.domain.entity.InputValidationListElementDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect InputValidationListElementDataOnDemand_Roo_DataOnDemand {
    
    declare @type: InputValidationListElementDataOnDemand: @Component;
    
    private Random InputValidationListElementDataOnDemand.rnd = new SecureRandom();
    
    private List<InputValidationListElement> InputValidationListElementDataOnDemand.data;
    
    public InputValidationListElement InputValidationListElementDataOnDemand.getNewTransientInputValidationListElement(int index) {
        InputValidationListElement obj = new InputValidationListElement();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setInputValidationList(obj, index);
        setInputValue(obj, index);
        setValidForNew(obj, index);
        return obj;
    }
    
    public void InputValidationListElementDataOnDemand.setDateCreated(InputValidationListElement obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void InputValidationListElementDataOnDemand.setDateModified(InputValidationListElement obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void InputValidationListElementDataOnDemand.setInputValue(InputValidationListElement obj, int index) {
        String inputValue = "inputValue_" + index;
        obj.setInputValue(inputValue);
    }
    
    public void InputValidationListElementDataOnDemand.setValidForNew(InputValidationListElement obj, int index) {
        Boolean validForNew = Boolean.TRUE;
        obj.setValidForNew(validForNew);
    }
    
    public InputValidationListElement InputValidationListElementDataOnDemand.getSpecificInputValidationListElement(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        InputValidationListElement obj = data.get(index);
        Long id = obj.getId();
        return InputValidationListElement.findInputValidationListElement(id);
    }
    
    public InputValidationListElement InputValidationListElementDataOnDemand.getRandomInputValidationListElement() {
        init();
        InputValidationListElement obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return InputValidationListElement.findInputValidationListElement(id);
    }
    
    public boolean InputValidationListElementDataOnDemand.modifyInputValidationListElement(InputValidationListElement obj) {
        return false;
    }
    
    public void InputValidationListElementDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = InputValidationListElement.findInputValidationListElementEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'InputValidationListElement' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<InputValidationListElement>();
        for (int i = 0; i < 10; i++) {
            InputValidationListElement obj = getNewTransientInputValidationListElement(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
