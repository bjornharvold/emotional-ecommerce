// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemVideo;
import com.lela.data.domain.entity.ItemVideoDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ItemVideoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ItemVideoDataOnDemand: @Component;
    
    private Random ItemVideoDataOnDemand.rnd = new SecureRandom();
    
    private List<ItemVideo> ItemVideoDataOnDemand.data;
    
    public ItemVideo ItemVideoDataOnDemand.getNewTransientItemVideo(int index) {
        ItemVideo obj = new ItemVideo();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setItem(obj, index);
        setReviewStatus(obj, index);
        setVideoSubscribers(obj, index);
        setVideoTitle(obj, index);
        setVideoType(obj, index);
        setVideoUrl(obj, index);
        setVideoViews(obj, index);
        return obj;
    }
    
    public void ItemVideoDataOnDemand.setDateCreated(ItemVideo obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ItemVideoDataOnDemand.setDateModified(ItemVideo obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ItemVideoDataOnDemand.setVideoSubscribers(ItemVideo obj, int index) {
        Integer videoSubscribers = new Integer(index);
        obj.setVideoSubscribers(videoSubscribers);
    }
    
    public void ItemVideoDataOnDemand.setVideoTitle(ItemVideo obj, int index) {
        String videoTitle = "videoTitle_" + index;
        obj.setVideoTitle(videoTitle);
    }
    
    public void ItemVideoDataOnDemand.setVideoUrl(ItemVideo obj, int index) {
        String videoUrl = "videoUrl_" + index;
        obj.setVideoUrl(videoUrl);
    }
    
    public void ItemVideoDataOnDemand.setVideoViews(ItemVideo obj, int index) {
        Integer videoViews = new Integer(index);
        obj.setVideoViews(videoViews);
    }
    
    public ItemVideo ItemVideoDataOnDemand.getSpecificItemVideo(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ItemVideo obj = data.get(index);
        Long id = obj.getId();
        return ItemVideo.findItemVideo(id);
    }
    
    public ItemVideo ItemVideoDataOnDemand.getRandomItemVideo() {
        init();
        ItemVideo obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ItemVideo.findItemVideo(id);
    }
    
    public boolean ItemVideoDataOnDemand.modifyItemVideo(ItemVideo obj) {
        return false;
    }
    
    public void ItemVideoDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ItemVideo.findItemVideoEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ItemVideo' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ItemVideo>();
        for (int i = 0; i < 10; i++) {
            ItemVideo obj = getNewTransientItemVideo(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
