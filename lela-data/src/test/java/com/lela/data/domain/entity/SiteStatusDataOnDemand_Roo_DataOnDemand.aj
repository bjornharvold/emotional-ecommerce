// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.SiteStatus;
import com.lela.data.domain.entity.SiteStatusDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect SiteStatusDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SiteStatusDataOnDemand: @Component;
    
    private Random SiteStatusDataOnDemand.rnd = new SecureRandom();
    
    private List<SiteStatus> SiteStatusDataOnDemand.data;
    
    public SiteStatus SiteStatusDataOnDemand.getNewTransientSiteStatus(int index) {
        SiteStatus obj = new SiteStatus();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setSiteStatusName(obj, index);
        return obj;
    }
    
    public void SiteStatusDataOnDemand.setDateCreated(SiteStatus obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void SiteStatusDataOnDemand.setDateModified(SiteStatus obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void SiteStatusDataOnDemand.setSiteStatusName(SiteStatus obj, int index) {
        String siteStatusName = "siteStatusName_" + index;
        obj.setSiteStatusName(siteStatusName);
    }
    
    public SiteStatus SiteStatusDataOnDemand.getSpecificSiteStatus(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        SiteStatus obj = data.get(index);
        Long id = obj.getId();
        return SiteStatus.findSiteStatus(id);
    }
    
    public SiteStatus SiteStatusDataOnDemand.getRandomSiteStatus() {
        init();
        SiteStatus obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return SiteStatus.findSiteStatus(id);
    }
    
    public boolean SiteStatusDataOnDemand.modifySiteStatus(SiteStatus obj) {
        return false;
    }
    
    public void SiteStatusDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = SiteStatus.findSiteStatusEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'SiteStatus' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<SiteStatus>();
        for (int i = 0; i < 10; i++) {
            SiteStatus obj = getNewTransientSiteStatus(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
