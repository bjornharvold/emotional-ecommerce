// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Category;
import com.lela.data.domain.entity.CategoryCategoryGroup;
import com.lela.data.domain.entity.CategoryCategoryGroupDataOnDemand;
import com.lela.data.domain.entity.CategoryGroup;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect CategoryCategoryGroupDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CategoryCategoryGroupDataOnDemand: @Component;
    
    private Random CategoryCategoryGroupDataOnDemand.rnd = new SecureRandom();
    
    private List<CategoryCategoryGroup> CategoryCategoryGroupDataOnDemand.data;
    
    public CategoryCategoryGroup CategoryCategoryGroupDataOnDemand.getNewTransientCategoryCategoryGroup(int index) {
        CategoryCategoryGroup obj = new CategoryCategoryGroup();
        setCategory(obj, index);
        setCategoryGroup(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        return obj;
    }
    
    public void CategoryCategoryGroupDataOnDemand.setCategory(CategoryCategoryGroup obj, int index) {
        Category category = null;
        obj.setCategory(category);
    }
    
    public void CategoryCategoryGroupDataOnDemand.setCategoryGroup(CategoryCategoryGroup obj, int index) {
        CategoryGroup categoryGroup = null;
        obj.setCategoryGroup(categoryGroup);
    }
    
    public void CategoryCategoryGroupDataOnDemand.setDateCreated(CategoryCategoryGroup obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void CategoryCategoryGroupDataOnDemand.setDateModified(CategoryCategoryGroup obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public CategoryCategoryGroup CategoryCategoryGroupDataOnDemand.getSpecificCategoryCategoryGroup(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CategoryCategoryGroup obj = data.get(index);
        Long id = obj.getId();
        return CategoryCategoryGroup.findCategoryCategoryGroup(id);
    }
    
    public CategoryCategoryGroup CategoryCategoryGroupDataOnDemand.getRandomCategoryCategoryGroup() {
        init();
        CategoryCategoryGroup obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CategoryCategoryGroup.findCategoryCategoryGroup(id);
    }
    
    public boolean CategoryCategoryGroupDataOnDemand.modifyCategoryCategoryGroup(CategoryCategoryGroup obj) {
        return false;
    }
    
    public void CategoryCategoryGroupDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CategoryCategoryGroup.findCategoryCategoryGroupEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CategoryCategoryGroup' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CategoryCategoryGroup>();
        for (int i = 0; i < 10; i++) {
            CategoryCategoryGroup obj = getNewTransientCategoryCategoryGroup(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
