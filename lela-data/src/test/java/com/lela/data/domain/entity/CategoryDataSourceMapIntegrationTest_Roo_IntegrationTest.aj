// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryDataSourceMap;
import com.lela.data.domain.entity.CategoryDataSourceMapDataOnDemand;
import com.lela.data.domain.entity.CategoryDataSourceMapIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoryDataSourceMapIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoryDataSourceMapIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoryDataSourceMapIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoryDataSourceMapIntegrationTest: @Transactional;
    
    @Autowired
    CategoryDataSourceMapDataOnDemand CategoryDataSourceMapIntegrationTest.dod;
    
    @Test
    public void CategoryDataSourceMapIntegrationTest.testCountCategoryDataSourceMaps() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to initialize correctly", dod.getRandomCategoryDataSourceMap());
        long count = CategoryDataSourceMap.countCategoryDataSourceMaps();
        Assert.assertTrue("Counter for 'CategoryDataSourceMap' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoryDataSourceMapIntegrationTest.testFindCategoryDataSourceMap() {
        CategoryDataSourceMap obj = dod.getRandomCategoryDataSourceMap();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to provide an identifier", id);
        obj = CategoryDataSourceMap.findCategoryDataSourceMap(id);
        Assert.assertNotNull("Find method for 'CategoryDataSourceMap' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategoryDataSourceMap' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoryDataSourceMapIntegrationTest.testFindAllCategoryDataSourceMaps() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to initialize correctly", dod.getRandomCategoryDataSourceMap());
        long count = CategoryDataSourceMap.countCategoryDataSourceMaps();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategoryDataSourceMap', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategoryDataSourceMap> result = CategoryDataSourceMap.findAllCategoryDataSourceMaps();
        Assert.assertNotNull("Find all method for 'CategoryDataSourceMap' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategoryDataSourceMap' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoryDataSourceMapIntegrationTest.testFindCategoryDataSourceMapEntries() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to initialize correctly", dod.getRandomCategoryDataSourceMap());
        long count = CategoryDataSourceMap.countCategoryDataSourceMaps();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategoryDataSourceMap> result = CategoryDataSourceMap.findCategoryDataSourceMapEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategoryDataSourceMap' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategoryDataSourceMap' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoryDataSourceMapIntegrationTest.testFlush() {
        CategoryDataSourceMap obj = dod.getRandomCategoryDataSourceMap();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to provide an identifier", id);
        obj = CategoryDataSourceMap.findCategoryDataSourceMap(id);
        Assert.assertNotNull("Find method for 'CategoryDataSourceMap' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategoryDataSourceMap(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategoryDataSourceMap' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryDataSourceMapIntegrationTest.testMergeUpdate() {
        CategoryDataSourceMap obj = dod.getRandomCategoryDataSourceMap();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to provide an identifier", id);
        obj = CategoryDataSourceMap.findCategoryDataSourceMap(id);
        boolean modified =  dod.modifyCategoryDataSourceMap(obj);
        Integer currentVersion = obj.getVersion();
        CategoryDataSourceMap merged = (CategoryDataSourceMap)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategoryDataSourceMap' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryDataSourceMapIntegrationTest.testRemove() {
        CategoryDataSourceMap obj = dod.getRandomCategoryDataSourceMap();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSourceMap' failed to provide an identifier", id);
        obj = CategoryDataSourceMap.findCategoryDataSourceMap(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategoryDataSourceMap' with identifier '" + id + "'", CategoryDataSourceMap.findCategoryDataSourceMap(id));
    }
    
}
