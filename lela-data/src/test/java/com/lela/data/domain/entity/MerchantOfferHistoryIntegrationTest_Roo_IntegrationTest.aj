// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.MerchantOfferHistory;
import com.lela.data.domain.entity.MerchantOfferHistoryDataOnDemand;
import com.lela.data.domain.entity.MerchantOfferHistoryIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect MerchantOfferHistoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: MerchantOfferHistoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: MerchantOfferHistoryIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: MerchantOfferHistoryIntegrationTest: @Transactional;
    
    @Autowired
    MerchantOfferHistoryDataOnDemand MerchantOfferHistoryIntegrationTest.dod;
    
    @Test
    public void MerchantOfferHistoryIntegrationTest.testCountMerchantOfferHistorys() {
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to initialize correctly", dod.getRandomMerchantOfferHistory());
        long count = MerchantOfferHistory.countMerchantOfferHistorys();
        Assert.assertTrue("Counter for 'MerchantOfferHistory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void MerchantOfferHistoryIntegrationTest.testFindMerchantOfferHistory() {
        MerchantOfferHistory obj = dod.getRandomMerchantOfferHistory();
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to provide an identifier", id);
        obj = MerchantOfferHistory.findMerchantOfferHistory(id);
        Assert.assertNotNull("Find method for 'MerchantOfferHistory' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'MerchantOfferHistory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void MerchantOfferHistoryIntegrationTest.testFindAllMerchantOfferHistorys() {
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to initialize correctly", dod.getRandomMerchantOfferHistory());
        long count = MerchantOfferHistory.countMerchantOfferHistorys();
        Assert.assertTrue("Too expensive to perform a find all test for 'MerchantOfferHistory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<MerchantOfferHistory> result = MerchantOfferHistory.findAllMerchantOfferHistorys();
        Assert.assertNotNull("Find all method for 'MerchantOfferHistory' illegally returned null", result);
        Assert.assertTrue("Find all method for 'MerchantOfferHistory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void MerchantOfferHistoryIntegrationTest.testFindMerchantOfferHistoryEntries() {
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to initialize correctly", dod.getRandomMerchantOfferHistory());
        long count = MerchantOfferHistory.countMerchantOfferHistorys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<MerchantOfferHistory> result = MerchantOfferHistory.findMerchantOfferHistoryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'MerchantOfferHistory' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'MerchantOfferHistory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void MerchantOfferHistoryIntegrationTest.testFlush() {
        MerchantOfferHistory obj = dod.getRandomMerchantOfferHistory();
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to provide an identifier", id);
        obj = MerchantOfferHistory.findMerchantOfferHistory(id);
        Assert.assertNotNull("Find method for 'MerchantOfferHistory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyMerchantOfferHistory(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'MerchantOfferHistory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MerchantOfferHistoryIntegrationTest.testMergeUpdate() {
        MerchantOfferHistory obj = dod.getRandomMerchantOfferHistory();
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to provide an identifier", id);
        obj = MerchantOfferHistory.findMerchantOfferHistory(id);
        boolean modified =  dod.modifyMerchantOfferHistory(obj);
        Integer currentVersion = obj.getVersion();
        MerchantOfferHistory merged = (MerchantOfferHistory)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'MerchantOfferHistory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MerchantOfferHistoryIntegrationTest.testRemove() {
        MerchantOfferHistory obj = dod.getRandomMerchantOfferHistory();
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantOfferHistory' failed to provide an identifier", id);
        obj = MerchantOfferHistory.findMerchantOfferHistory(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'MerchantOfferHistory' with identifier '" + id + "'", MerchantOfferHistory.findMerchantOfferHistory(id));
    }
    
}
