// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.MultiValuedType;
import com.lela.data.domain.entity.MultiValuedTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect MultiValuedTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: MultiValuedTypeDataOnDemand: @Component;
    
    private Random MultiValuedTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<MultiValuedType> MultiValuedTypeDataOnDemand.data;
    
    public MultiValuedType MultiValuedTypeDataOnDemand.getNewTransientMultiValuedType(int index) {
        MultiValuedType obj = new MultiValuedType();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setMultiValuedTypeName(obj, index);
        return obj;
    }
    
    public void MultiValuedTypeDataOnDemand.setDateCreated(MultiValuedType obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void MultiValuedTypeDataOnDemand.setDateModified(MultiValuedType obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void MultiValuedTypeDataOnDemand.setMultiValuedTypeName(MultiValuedType obj, int index) {
        String multiValuedTypeName = "multiValuedTypeName_" + index;
        if (multiValuedTypeName.length() > 255) {
            multiValuedTypeName = multiValuedTypeName.substring(0, 255);
        }
        obj.setMultiValuedTypeName(multiValuedTypeName);
    }
    
    public MultiValuedType MultiValuedTypeDataOnDemand.getSpecificMultiValuedType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        MultiValuedType obj = data.get(index);
        Long id = obj.getId();
        return MultiValuedType.findMultiValuedType(id);
    }
    
    public MultiValuedType MultiValuedTypeDataOnDemand.getRandomMultiValuedType() {
        init();
        MultiValuedType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return MultiValuedType.findMultiValuedType(id);
    }
    
    public boolean MultiValuedTypeDataOnDemand.modifyMultiValuedType(MultiValuedType obj) {
        return false;
    }
    
    public void MultiValuedTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = MultiValuedType.findMultiValuedTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'MultiValuedType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<MultiValuedType>();
        for (int i = 0; i < 10; i++) {
            MultiValuedType obj = getNewTransientMultiValuedType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
