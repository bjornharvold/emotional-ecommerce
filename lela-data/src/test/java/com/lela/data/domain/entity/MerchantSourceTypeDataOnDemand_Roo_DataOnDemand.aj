// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.MerchantSourceType;
import com.lela.data.domain.entity.MerchantSourceTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect MerchantSourceTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: MerchantSourceTypeDataOnDemand: @Component;
    
    private Random MerchantSourceTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<MerchantSourceType> MerchantSourceTypeDataOnDemand.data;
    
    public MerchantSourceType MerchantSourceTypeDataOnDemand.getNewTransientMerchantSourceType(int index) {
        MerchantSourceType obj = new MerchantSourceType();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setSourceTypeName(obj, index);
        return obj;
    }
    
    public void MerchantSourceTypeDataOnDemand.setDateCreated(MerchantSourceType obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void MerchantSourceTypeDataOnDemand.setDateModified(MerchantSourceType obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void MerchantSourceTypeDataOnDemand.setSourceTypeName(MerchantSourceType obj, int index) {
        String sourceTypeName = "sourceTypeName_" + index;
        if (sourceTypeName.length() > 255) {
            sourceTypeName = sourceTypeName.substring(0, 255);
        }
        obj.setSourceTypeName(sourceTypeName);
    }
    
    public MerchantSourceType MerchantSourceTypeDataOnDemand.getSpecificMerchantSourceType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        MerchantSourceType obj = data.get(index);
        Long id = obj.getId();
        return MerchantSourceType.findMerchantSourceType(id);
    }
    
    public MerchantSourceType MerchantSourceTypeDataOnDemand.getRandomMerchantSourceType() {
        init();
        MerchantSourceType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return MerchantSourceType.findMerchantSourceType(id);
    }
    
    public boolean MerchantSourceTypeDataOnDemand.modifyMerchantSourceType(MerchantSourceType obj) {
        return false;
    }
    
    public void MerchantSourceTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = MerchantSourceType.findMerchantSourceTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'MerchantSourceType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<MerchantSourceType>();
        for (int i = 0; i < 10; i++) {
            MerchantSourceType obj = getNewTransientMerchantSourceType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
