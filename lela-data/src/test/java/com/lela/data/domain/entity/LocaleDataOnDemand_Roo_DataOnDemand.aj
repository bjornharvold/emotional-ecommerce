// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Locale;
import com.lela.data.domain.entity.LocaleDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect LocaleDataOnDemand_Roo_DataOnDemand {
    
    declare @type: LocaleDataOnDemand: @Component;
    
    private Random LocaleDataOnDemand.rnd = new SecureRandom();
    
    private List<Locale> LocaleDataOnDemand.data;
    
    public Locale LocaleDataOnDemand.getNewTransientLocale(int index) {
        Locale obj = new Locale();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setLocaleName(obj, index);
        return obj;
    }
    
    public void LocaleDataOnDemand.setDateCreated(Locale obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void LocaleDataOnDemand.setDateModified(Locale obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void LocaleDataOnDemand.setLocaleName(Locale obj, int index) {
        String localeName = "localeName_" + index;
        if (localeName.length() > 255) {
            localeName = localeName.substring(0, 255);
        }
        obj.setLocaleName(localeName);
    }
    
    public Locale LocaleDataOnDemand.getSpecificLocale(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Locale obj = data.get(index);
        Long id = obj.getId();
        return Locale.findLocale(id);
    }
    
    public Locale LocaleDataOnDemand.getRandomLocale() {
        init();
        Locale obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Locale.findLocale(id);
    }
    
    public boolean LocaleDataOnDemand.modifyLocale(Locale obj) {
        return false;
    }
    
    public void LocaleDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Locale.findLocaleEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Locale' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Locale>();
        for (int i = 0; i < 10; i++) {
            Locale obj = getNewTransientLocale(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
