// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryLoadRule;
import com.lela.data.domain.entity.CategoryLoadRuleDataOnDemand;
import com.lela.data.domain.entity.CategoryLoadRuleIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoryLoadRuleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoryLoadRuleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoryLoadRuleIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoryLoadRuleIntegrationTest: @Transactional;
    
    @Autowired
    CategoryLoadRuleDataOnDemand CategoryLoadRuleIntegrationTest.dod;
    
    @Test
    public void CategoryLoadRuleIntegrationTest.testCountCategoryLoadRules() {
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to initialize correctly", dod.getRandomCategoryLoadRule());
        long count = CategoryLoadRule.countCategoryLoadRules();
        Assert.assertTrue("Counter for 'CategoryLoadRule' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoryLoadRuleIntegrationTest.testFindCategoryLoadRule() {
        CategoryLoadRule obj = dod.getRandomCategoryLoadRule();
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to provide an identifier", id);
        obj = CategoryLoadRule.findCategoryLoadRule(id);
        Assert.assertNotNull("Find method for 'CategoryLoadRule' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategoryLoadRule' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoryLoadRuleIntegrationTest.testFindAllCategoryLoadRules() {
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to initialize correctly", dod.getRandomCategoryLoadRule());
        long count = CategoryLoadRule.countCategoryLoadRules();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategoryLoadRule', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategoryLoadRule> result = CategoryLoadRule.findAllCategoryLoadRules();
        Assert.assertNotNull("Find all method for 'CategoryLoadRule' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategoryLoadRule' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoryLoadRuleIntegrationTest.testFindCategoryLoadRuleEntries() {
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to initialize correctly", dod.getRandomCategoryLoadRule());
        long count = CategoryLoadRule.countCategoryLoadRules();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategoryLoadRule> result = CategoryLoadRule.findCategoryLoadRuleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategoryLoadRule' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategoryLoadRule' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoryLoadRuleIntegrationTest.testFlush() {
        CategoryLoadRule obj = dod.getRandomCategoryLoadRule();
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to provide an identifier", id);
        obj = CategoryLoadRule.findCategoryLoadRule(id);
        Assert.assertNotNull("Find method for 'CategoryLoadRule' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategoryLoadRule(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategoryLoadRule' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryLoadRuleIntegrationTest.testMergeUpdate() {
        CategoryLoadRule obj = dod.getRandomCategoryLoadRule();
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to provide an identifier", id);
        obj = CategoryLoadRule.findCategoryLoadRule(id);
        boolean modified =  dod.modifyCategoryLoadRule(obj);
        Integer currentVersion = obj.getVersion();
        CategoryLoadRule merged = (CategoryLoadRule)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategoryLoadRule' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryLoadRuleIntegrationTest.testRemove() {
        CategoryLoadRule obj = dod.getRandomCategoryLoadRule();
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryLoadRule' failed to provide an identifier", id);
        obj = CategoryLoadRule.findCategoryLoadRule(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategoryLoadRule' with identifier '" + id + "'", CategoryLoadRule.findCategoryLoadRule(id));
    }
    
}
