// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.MerchantDeal;
import com.lela.data.domain.entity.MerchantDealDataOnDemand;
import com.lela.data.domain.entity.MerchantDealIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect MerchantDealIntegrationTest_Roo_IntegrationTest {
    
    declare @type: MerchantDealIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: MerchantDealIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: MerchantDealIntegrationTest: @Transactional;
    
    @Autowired
    MerchantDealDataOnDemand MerchantDealIntegrationTest.dod;
    
    @Test
    public void MerchantDealIntegrationTest.testCountMerchantDeals() {
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to initialize correctly", dod.getRandomMerchantDeal());
        long count = MerchantDeal.countMerchantDeals();
        Assert.assertTrue("Counter for 'MerchantDeal' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void MerchantDealIntegrationTest.testFindMerchantDeal() {
        MerchantDeal obj = dod.getRandomMerchantDeal();
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to provide an identifier", id);
        obj = MerchantDeal.findMerchantDeal(id);
        Assert.assertNotNull("Find method for 'MerchantDeal' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'MerchantDeal' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void MerchantDealIntegrationTest.testFindAllMerchantDeals() {
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to initialize correctly", dod.getRandomMerchantDeal());
        long count = MerchantDeal.countMerchantDeals();
        Assert.assertTrue("Too expensive to perform a find all test for 'MerchantDeal', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<MerchantDeal> result = MerchantDeal.findAllMerchantDeals();
        Assert.assertNotNull("Find all method for 'MerchantDeal' illegally returned null", result);
        Assert.assertTrue("Find all method for 'MerchantDeal' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void MerchantDealIntegrationTest.testFindMerchantDealEntries() {
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to initialize correctly", dod.getRandomMerchantDeal());
        long count = MerchantDeal.countMerchantDeals();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<MerchantDeal> result = MerchantDeal.findMerchantDealEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'MerchantDeal' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'MerchantDeal' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void MerchantDealIntegrationTest.testFlush() {
        MerchantDeal obj = dod.getRandomMerchantDeal();
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to provide an identifier", id);
        obj = MerchantDeal.findMerchantDeal(id);
        Assert.assertNotNull("Find method for 'MerchantDeal' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyMerchantDeal(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'MerchantDeal' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MerchantDealIntegrationTest.testMergeUpdate() {
        MerchantDeal obj = dod.getRandomMerchantDeal();
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to provide an identifier", id);
        obj = MerchantDeal.findMerchantDeal(id);
        boolean modified =  dod.modifyMerchantDeal(obj);
        Integer currentVersion = obj.getVersion();
        MerchantDeal merged = (MerchantDeal)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'MerchantDeal' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MerchantDealIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to initialize correctly", dod.getRandomMerchantDeal());
        MerchantDeal obj = dod.getNewTransientMerchantDeal(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'MerchantDeal' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'MerchantDeal' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void MerchantDealIntegrationTest.testRemove() {
        MerchantDeal obj = dod.getRandomMerchantDeal();
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantDeal' failed to provide an identifier", id);
        obj = MerchantDeal.findMerchantDeal(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'MerchantDeal' with identifier '" + id + "'", MerchantDeal.findMerchantDeal(id));
    }
    
}
