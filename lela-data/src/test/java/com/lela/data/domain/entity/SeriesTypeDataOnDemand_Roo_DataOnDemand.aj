// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.SeriesType;
import com.lela.data.domain.entity.SeriesTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect SeriesTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SeriesTypeDataOnDemand: @Component;
    
    private Random SeriesTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<SeriesType> SeriesTypeDataOnDemand.data;
    
    public SeriesType SeriesTypeDataOnDemand.getNewTransientSeriesType(int index) {
        SeriesType obj = new SeriesType();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setSeriesTypeName(obj, index);
        return obj;
    }
    
    public void SeriesTypeDataOnDemand.setDateCreated(SeriesType obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void SeriesTypeDataOnDemand.setDateModified(SeriesType obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void SeriesTypeDataOnDemand.setSeriesTypeName(SeriesType obj, int index) {
        String seriesTypeName = "seriesTypeName_" + index;
        obj.setSeriesTypeName(seriesTypeName);
    }
    
    public SeriesType SeriesTypeDataOnDemand.getSpecificSeriesType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        SeriesType obj = data.get(index);
        Long id = obj.getId();
        return SeriesType.findSeriesType(id);
    }
    
    public SeriesType SeriesTypeDataOnDemand.getRandomSeriesType() {
        init();
        SeriesType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return SeriesType.findSeriesType(id);
    }
    
    public boolean SeriesTypeDataOnDemand.modifySeriesType(SeriesType obj) {
        return false;
    }
    
    public void SeriesTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = SeriesType.findSeriesTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'SeriesType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<SeriesType>();
        for (int i = 0; i < 10; i++) {
            SeriesType obj = getNewTransientSeriesType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
