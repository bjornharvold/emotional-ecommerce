// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryAttributeRejectionFilter;
import com.lela.data.domain.entity.CategoryAttributeRejectionFilterDataOnDemand;
import com.lela.data.domain.entity.CategoryAttributeRejectionFilterIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoryAttributeRejectionFilterIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoryAttributeRejectionFilterIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoryAttributeRejectionFilterIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoryAttributeRejectionFilterIntegrationTest: @Transactional;
    
    @Autowired
    CategoryAttributeRejectionFilterDataOnDemand CategoryAttributeRejectionFilterIntegrationTest.dod;
    
    @Test
    public void CategoryAttributeRejectionFilterIntegrationTest.testCountCategoryAttributeRejectionFilters() {
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to initialize correctly", dod.getRandomCategoryAttributeRejectionFilter());
        long count = CategoryAttributeRejectionFilter.countCategoryAttributeRejectionFilters();
        Assert.assertTrue("Counter for 'CategoryAttributeRejectionFilter' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoryAttributeRejectionFilterIntegrationTest.testFindCategoryAttributeRejectionFilter() {
        CategoryAttributeRejectionFilter obj = dod.getRandomCategoryAttributeRejectionFilter();
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to provide an identifier", id);
        obj = CategoryAttributeRejectionFilter.findCategoryAttributeRejectionFilter(id);
        Assert.assertNotNull("Find method for 'CategoryAttributeRejectionFilter' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategoryAttributeRejectionFilter' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoryAttributeRejectionFilterIntegrationTest.testFindAllCategoryAttributeRejectionFilters() {
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to initialize correctly", dod.getRandomCategoryAttributeRejectionFilter());
        long count = CategoryAttributeRejectionFilter.countCategoryAttributeRejectionFilters();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategoryAttributeRejectionFilter', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategoryAttributeRejectionFilter> result = CategoryAttributeRejectionFilter.findAllCategoryAttributeRejectionFilters();
        Assert.assertNotNull("Find all method for 'CategoryAttributeRejectionFilter' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategoryAttributeRejectionFilter' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoryAttributeRejectionFilterIntegrationTest.testFindCategoryAttributeRejectionFilterEntries() {
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to initialize correctly", dod.getRandomCategoryAttributeRejectionFilter());
        long count = CategoryAttributeRejectionFilter.countCategoryAttributeRejectionFilters();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategoryAttributeRejectionFilter> result = CategoryAttributeRejectionFilter.findCategoryAttributeRejectionFilterEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategoryAttributeRejectionFilter' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategoryAttributeRejectionFilter' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoryAttributeRejectionFilterIntegrationTest.testFlush() {
        CategoryAttributeRejectionFilter obj = dod.getRandomCategoryAttributeRejectionFilter();
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to provide an identifier", id);
        obj = CategoryAttributeRejectionFilter.findCategoryAttributeRejectionFilter(id);
        Assert.assertNotNull("Find method for 'CategoryAttributeRejectionFilter' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategoryAttributeRejectionFilter(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategoryAttributeRejectionFilter' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryAttributeRejectionFilterIntegrationTest.testMergeUpdate() {
        CategoryAttributeRejectionFilter obj = dod.getRandomCategoryAttributeRejectionFilter();
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to provide an identifier", id);
        obj = CategoryAttributeRejectionFilter.findCategoryAttributeRejectionFilter(id);
        boolean modified =  dod.modifyCategoryAttributeRejectionFilter(obj);
        Integer currentVersion = obj.getVersion();
        CategoryAttributeRejectionFilter merged = (CategoryAttributeRejectionFilter)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategoryAttributeRejectionFilter' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryAttributeRejectionFilterIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to initialize correctly", dod.getRandomCategoryAttributeRejectionFilter());
        CategoryAttributeRejectionFilter obj = dod.getNewTransientCategoryAttributeRejectionFilter(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CategoryAttributeRejectionFilter' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'CategoryAttributeRejectionFilter' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CategoryAttributeRejectionFilterIntegrationTest.testRemove() {
        CategoryAttributeRejectionFilter obj = dod.getRandomCategoryAttributeRejectionFilter();
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryAttributeRejectionFilter' failed to provide an identifier", id);
        obj = CategoryAttributeRejectionFilter.findCategoryAttributeRejectionFilter(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategoryAttributeRejectionFilter' with identifier '" + id + "'", CategoryAttributeRejectionFilter.findCategoryAttributeRejectionFilter(id));
    }
    
}
