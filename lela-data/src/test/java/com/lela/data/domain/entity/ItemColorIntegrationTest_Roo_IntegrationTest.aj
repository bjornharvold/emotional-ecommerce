// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemColor;
import com.lela.data.domain.entity.ItemColorDataOnDemand;
import com.lela.data.domain.entity.ItemColorIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ItemColorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ItemColorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ItemColorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ItemColorIntegrationTest: @Transactional;
    
    @Autowired
    ItemColorDataOnDemand ItemColorIntegrationTest.dod;
    
    @Test
    public void ItemColorIntegrationTest.testCountItemColors() {
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to initialize correctly", dod.getRandomItemColor());
        long count = ItemColor.countItemColors();
        Assert.assertTrue("Counter for 'ItemColor' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ItemColorIntegrationTest.testFindItemColor() {
        ItemColor obj = dod.getRandomItemColor();
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to provide an identifier", id);
        obj = ItemColor.findItemColor(id);
        Assert.assertNotNull("Find method for 'ItemColor' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ItemColor' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ItemColorIntegrationTest.testFindAllItemColors() {
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to initialize correctly", dod.getRandomItemColor());
        long count = ItemColor.countItemColors();
        Assert.assertTrue("Too expensive to perform a find all test for 'ItemColor', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ItemColor> result = ItemColor.findAllItemColors();
        Assert.assertNotNull("Find all method for 'ItemColor' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ItemColor' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ItemColorIntegrationTest.testFindItemColorEntries() {
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to initialize correctly", dod.getRandomItemColor());
        long count = ItemColor.countItemColors();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ItemColor> result = ItemColor.findItemColorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ItemColor' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ItemColor' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ItemColorIntegrationTest.testFlush() {
        ItemColor obj = dod.getRandomItemColor();
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to provide an identifier", id);
        obj = ItemColor.findItemColor(id);
        Assert.assertNotNull("Find method for 'ItemColor' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyItemColor(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ItemColor' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemColorIntegrationTest.testMergeUpdate() {
        ItemColor obj = dod.getRandomItemColor();
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to provide an identifier", id);
        obj = ItemColor.findItemColor(id);
        boolean modified =  dod.modifyItemColor(obj);
        Integer currentVersion = obj.getVersion();
        ItemColor merged = (ItemColor)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ItemColor' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemColorIntegrationTest.testRemove() {
        ItemColor obj = dod.getRandomItemColor();
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemColor' failed to provide an identifier", id);
        obj = ItemColor.findItemColor(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ItemColor' with identifier '" + id + "'", ItemColor.findItemColor(id));
    }
    
}
