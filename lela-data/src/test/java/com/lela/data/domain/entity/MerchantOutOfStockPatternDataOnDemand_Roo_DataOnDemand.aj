// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Merchant;
import com.lela.data.domain.entity.MerchantDataOnDemand;
import com.lela.data.domain.entity.MerchantOutOfStockPattern;
import com.lela.data.domain.entity.MerchantOutOfStockPatternDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect MerchantOutOfStockPatternDataOnDemand_Roo_DataOnDemand {
    
    declare @type: MerchantOutOfStockPatternDataOnDemand: @Component;
    
    private Random MerchantOutOfStockPatternDataOnDemand.rnd = new SecureRandom();
    
    private List<MerchantOutOfStockPattern> MerchantOutOfStockPatternDataOnDemand.data;
    
    @Autowired
    MerchantDataOnDemand MerchantOutOfStockPatternDataOnDemand.merchantDataOnDemand;
    
    public MerchantOutOfStockPattern MerchantOutOfStockPatternDataOnDemand.getNewTransientMerchantOutOfStockPattern(int index) {
        MerchantOutOfStockPattern obj = new MerchantOutOfStockPattern();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setEnabled(obj, index);
        setMerchant(obj, index);
        setPattern(obj, index);
        return obj;
    }
    
    public void MerchantOutOfStockPatternDataOnDemand.setDateCreated(MerchantOutOfStockPattern obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void MerchantOutOfStockPatternDataOnDemand.setDateModified(MerchantOutOfStockPattern obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void MerchantOutOfStockPatternDataOnDemand.setEnabled(MerchantOutOfStockPattern obj, int index) {
        Boolean enabled = Boolean.TRUE;
        obj.setEnabled(enabled);
    }
    
    public void MerchantOutOfStockPatternDataOnDemand.setMerchant(MerchantOutOfStockPattern obj, int index) {
        Merchant merchant = merchantDataOnDemand.getRandomMerchant();
        obj.setMerchant(merchant);
    }
    
    public void MerchantOutOfStockPatternDataOnDemand.setPattern(MerchantOutOfStockPattern obj, int index) {
        String pattern = "pattern_" + index;
        if (pattern.length() > 512) {
            pattern = pattern.substring(0, 512);
        }
        obj.setPattern(pattern);
    }
    
    public MerchantOutOfStockPattern MerchantOutOfStockPatternDataOnDemand.getSpecificMerchantOutOfStockPattern(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        MerchantOutOfStockPattern obj = data.get(index);
        Long id = obj.getId();
        return MerchantOutOfStockPattern.findMerchantOutOfStockPattern(id);
    }
    
    public MerchantOutOfStockPattern MerchantOutOfStockPatternDataOnDemand.getRandomMerchantOutOfStockPattern() {
        init();
        MerchantOutOfStockPattern obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return MerchantOutOfStockPattern.findMerchantOutOfStockPattern(id);
    }
    
    public boolean MerchantOutOfStockPatternDataOnDemand.modifyMerchantOutOfStockPattern(MerchantOutOfStockPattern obj) {
        return false;
    }
    
    public void MerchantOutOfStockPatternDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = MerchantOutOfStockPattern.findMerchantOutOfStockPatternEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'MerchantOutOfStockPattern' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<MerchantOutOfStockPattern>();
        for (int i = 0; i < 10; i++) {
            MerchantOutOfStockPattern obj = getNewTransientMerchantOutOfStockPattern(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
