// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategorySubdepartment;
import com.lela.data.domain.entity.CategorySubdepartmentDataOnDemand;
import com.lela.data.domain.entity.CategorySubdepartmentIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategorySubdepartmentIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategorySubdepartmentIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategorySubdepartmentIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategorySubdepartmentIntegrationTest: @Transactional;
    
    @Autowired
    CategorySubdepartmentDataOnDemand CategorySubdepartmentIntegrationTest.dod;
    
    @Test
    public void CategorySubdepartmentIntegrationTest.testCountCategorySubdepartments() {
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to initialize correctly", dod.getRandomCategorySubdepartment());
        long count = CategorySubdepartment.countCategorySubdepartments();
        Assert.assertTrue("Counter for 'CategorySubdepartment' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategorySubdepartmentIntegrationTest.testFindCategorySubdepartment() {
        CategorySubdepartment obj = dod.getRandomCategorySubdepartment();
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to provide an identifier", id);
        obj = CategorySubdepartment.findCategorySubdepartment(id);
        Assert.assertNotNull("Find method for 'CategorySubdepartment' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategorySubdepartment' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategorySubdepartmentIntegrationTest.testFindAllCategorySubdepartments() {
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to initialize correctly", dod.getRandomCategorySubdepartment());
        long count = CategorySubdepartment.countCategorySubdepartments();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategorySubdepartment', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategorySubdepartment> result = CategorySubdepartment.findAllCategorySubdepartments();
        Assert.assertNotNull("Find all method for 'CategorySubdepartment' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategorySubdepartment' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategorySubdepartmentIntegrationTest.testFindCategorySubdepartmentEntries() {
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to initialize correctly", dod.getRandomCategorySubdepartment());
        long count = CategorySubdepartment.countCategorySubdepartments();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategorySubdepartment> result = CategorySubdepartment.findCategorySubdepartmentEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategorySubdepartment' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategorySubdepartment' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategorySubdepartmentIntegrationTest.testFlush() {
        CategorySubdepartment obj = dod.getRandomCategorySubdepartment();
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to provide an identifier", id);
        obj = CategorySubdepartment.findCategorySubdepartment(id);
        Assert.assertNotNull("Find method for 'CategorySubdepartment' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategorySubdepartment(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategorySubdepartment' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategorySubdepartmentIntegrationTest.testMergeUpdate() {
        CategorySubdepartment obj = dod.getRandomCategorySubdepartment();
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to provide an identifier", id);
        obj = CategorySubdepartment.findCategorySubdepartment(id);
        boolean modified =  dod.modifyCategorySubdepartment(obj);
        Integer currentVersion = obj.getVersion();
        CategorySubdepartment merged = (CategorySubdepartment)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategorySubdepartment' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategorySubdepartmentIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to initialize correctly", dod.getRandomCategorySubdepartment());
        CategorySubdepartment obj = dod.getNewTransientCategorySubdepartment(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CategorySubdepartment' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'CategorySubdepartment' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CategorySubdepartmentIntegrationTest.testRemove() {
        CategorySubdepartment obj = dod.getRandomCategorySubdepartment();
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategorySubdepartment' failed to provide an identifier", id);
        obj = CategorySubdepartment.findCategorySubdepartment(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategorySubdepartment' with identifier '" + id + "'", CategorySubdepartment.findCategorySubdepartment(id));
    }
    
}
