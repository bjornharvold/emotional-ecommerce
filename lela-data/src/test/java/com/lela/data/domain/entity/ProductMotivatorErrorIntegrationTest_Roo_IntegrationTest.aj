// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductMotivatorError;
import com.lela.data.domain.entity.ProductMotivatorErrorDataOnDemand;
import com.lela.data.domain.entity.ProductMotivatorErrorIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductMotivatorErrorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductMotivatorErrorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductMotivatorErrorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductMotivatorErrorIntegrationTest: @Transactional;
    
    @Autowired
    ProductMotivatorErrorDataOnDemand ProductMotivatorErrorIntegrationTest.dod;
    
    @Test
    public void ProductMotivatorErrorIntegrationTest.testCountProductMotivatorErrors() {
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to initialize correctly", dod.getRandomProductMotivatorError());
        long count = ProductMotivatorError.countProductMotivatorErrors();
        Assert.assertTrue("Counter for 'ProductMotivatorError' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductMotivatorErrorIntegrationTest.testFindProductMotivatorError() {
        ProductMotivatorError obj = dod.getRandomProductMotivatorError();
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to provide an identifier", id);
        obj = ProductMotivatorError.findProductMotivatorError(id);
        Assert.assertNotNull("Find method for 'ProductMotivatorError' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductMotivatorError' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductMotivatorErrorIntegrationTest.testFindAllProductMotivatorErrors() {
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to initialize correctly", dod.getRandomProductMotivatorError());
        long count = ProductMotivatorError.countProductMotivatorErrors();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductMotivatorError', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductMotivatorError> result = ProductMotivatorError.findAllProductMotivatorErrors();
        Assert.assertNotNull("Find all method for 'ProductMotivatorError' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductMotivatorError' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductMotivatorErrorIntegrationTest.testFindProductMotivatorErrorEntries() {
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to initialize correctly", dod.getRandomProductMotivatorError());
        long count = ProductMotivatorError.countProductMotivatorErrors();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductMotivatorError> result = ProductMotivatorError.findProductMotivatorErrorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductMotivatorError' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductMotivatorError' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductMotivatorErrorIntegrationTest.testFlush() {
        ProductMotivatorError obj = dod.getRandomProductMotivatorError();
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to provide an identifier", id);
        obj = ProductMotivatorError.findProductMotivatorError(id);
        Assert.assertNotNull("Find method for 'ProductMotivatorError' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProductMotivatorError(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProductMotivatorError' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductMotivatorErrorIntegrationTest.testMergeUpdate() {
        ProductMotivatorError obj = dod.getRandomProductMotivatorError();
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to provide an identifier", id);
        obj = ProductMotivatorError.findProductMotivatorError(id);
        boolean modified =  dod.modifyProductMotivatorError(obj);
        Integer currentVersion = obj.getVersion();
        ProductMotivatorError merged = (ProductMotivatorError)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProductMotivatorError' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductMotivatorErrorIntegrationTest.testRemove() {
        ProductMotivatorError obj = dod.getRandomProductMotivatorError();
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductMotivatorError' failed to provide an identifier", id);
        obj = ProductMotivatorError.findProductMotivatorError(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductMotivatorError' with identifier '" + id + "'", ProductMotivatorError.findProductMotivatorError(id));
    }
    
}
