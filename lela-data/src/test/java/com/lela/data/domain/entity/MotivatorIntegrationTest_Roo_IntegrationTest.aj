// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Motivator;
import com.lela.data.domain.entity.MotivatorDataOnDemand;
import com.lela.data.domain.entity.MotivatorIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect MotivatorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: MotivatorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: MotivatorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: MotivatorIntegrationTest: @Transactional;
    
    @Autowired
    MotivatorDataOnDemand MotivatorIntegrationTest.dod;
    
    @Test
    public void MotivatorIntegrationTest.testCountMotivators() {
        Assert.assertNotNull("Data on demand for 'Motivator' failed to initialize correctly", dod.getRandomMotivator());
        long count = Motivator.countMotivators();
        Assert.assertTrue("Counter for 'Motivator' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void MotivatorIntegrationTest.testFindMotivator() {
        Motivator obj = dod.getRandomMotivator();
        Assert.assertNotNull("Data on demand for 'Motivator' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Motivator' failed to provide an identifier", id);
        obj = Motivator.findMotivator(id);
        Assert.assertNotNull("Find method for 'Motivator' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Motivator' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void MotivatorIntegrationTest.testFindAllMotivators() {
        Assert.assertNotNull("Data on demand for 'Motivator' failed to initialize correctly", dod.getRandomMotivator());
        long count = Motivator.countMotivators();
        Assert.assertTrue("Too expensive to perform a find all test for 'Motivator', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Motivator> result = Motivator.findAllMotivators();
        Assert.assertNotNull("Find all method for 'Motivator' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Motivator' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void MotivatorIntegrationTest.testFindMotivatorEntries() {
        Assert.assertNotNull("Data on demand for 'Motivator' failed to initialize correctly", dod.getRandomMotivator());
        long count = Motivator.countMotivators();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Motivator> result = Motivator.findMotivatorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Motivator' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Motivator' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void MotivatorIntegrationTest.testFlush() {
        Motivator obj = dod.getRandomMotivator();
        Assert.assertNotNull("Data on demand for 'Motivator' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Motivator' failed to provide an identifier", id);
        obj = Motivator.findMotivator(id);
        Assert.assertNotNull("Find method for 'Motivator' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyMotivator(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Motivator' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MotivatorIntegrationTest.testMergeUpdate() {
        Motivator obj = dod.getRandomMotivator();
        Assert.assertNotNull("Data on demand for 'Motivator' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Motivator' failed to provide an identifier", id);
        obj = Motivator.findMotivator(id);
        boolean modified =  dod.modifyMotivator(obj);
        Integer currentVersion = obj.getVersion();
        Motivator merged = (Motivator)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Motivator' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MotivatorIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Motivator' failed to initialize correctly", dod.getRandomMotivator());
        Motivator obj = dod.getNewTransientMotivator(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Motivator' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Motivator' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Motivator' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void MotivatorIntegrationTest.testRemove() {
        Motivator obj = dod.getRandomMotivator();
        Assert.assertNotNull("Data on demand for 'Motivator' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Motivator' failed to provide an identifier", id);
        obj = Motivator.findMotivator(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Motivator' with identifier '" + id + "'", Motivator.findMotivator(id));
    }
    
}
