// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AttributeTypeHistory;
import com.lela.data.domain.entity.AttributeTypeHistoryDataOnDemand;
import com.lela.data.domain.entity.AttributeTypeHistoryIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AttributeTypeHistoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AttributeTypeHistoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AttributeTypeHistoryIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AttributeTypeHistoryIntegrationTest: @Transactional;
    
    @Autowired
    AttributeTypeHistoryDataOnDemand AttributeTypeHistoryIntegrationTest.dod;
    
    @Test
    public void AttributeTypeHistoryIntegrationTest.testCountAttributeTypeHistorys() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to initialize correctly", dod.getRandomAttributeTypeHistory());
        long count = AttributeTypeHistory.countAttributeTypeHistorys();
        Assert.assertTrue("Counter for 'AttributeTypeHistory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AttributeTypeHistoryIntegrationTest.testFindAttributeTypeHistory() {
        AttributeTypeHistory obj = dod.getRandomAttributeTypeHistory();
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to provide an identifier", id);
        obj = AttributeTypeHistory.findAttributeTypeHistory(id);
        Assert.assertNotNull("Find method for 'AttributeTypeHistory' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AttributeTypeHistory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AttributeTypeHistoryIntegrationTest.testFindAllAttributeTypeHistorys() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to initialize correctly", dod.getRandomAttributeTypeHistory());
        long count = AttributeTypeHistory.countAttributeTypeHistorys();
        Assert.assertTrue("Too expensive to perform a find all test for 'AttributeTypeHistory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AttributeTypeHistory> result = AttributeTypeHistory.findAllAttributeTypeHistorys();
        Assert.assertNotNull("Find all method for 'AttributeTypeHistory' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AttributeTypeHistory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AttributeTypeHistoryIntegrationTest.testFindAttributeTypeHistoryEntries() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to initialize correctly", dod.getRandomAttributeTypeHistory());
        long count = AttributeTypeHistory.countAttributeTypeHistorys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AttributeTypeHistory> result = AttributeTypeHistory.findAttributeTypeHistoryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AttributeTypeHistory' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AttributeTypeHistory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AttributeTypeHistoryIntegrationTest.testFlush() {
        AttributeTypeHistory obj = dod.getRandomAttributeTypeHistory();
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to provide an identifier", id);
        obj = AttributeTypeHistory.findAttributeTypeHistory(id);
        Assert.assertNotNull("Find method for 'AttributeTypeHistory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAttributeTypeHistory(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AttributeTypeHistory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AttributeTypeHistoryIntegrationTest.testMergeUpdate() {
        AttributeTypeHistory obj = dod.getRandomAttributeTypeHistory();
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to provide an identifier", id);
        obj = AttributeTypeHistory.findAttributeTypeHistory(id);
        boolean modified =  dod.modifyAttributeTypeHistory(obj);
        Integer currentVersion = obj.getVersion();
        AttributeTypeHistory merged = (AttributeTypeHistory)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'AttributeTypeHistory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AttributeTypeHistoryIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to initialize correctly", dod.getRandomAttributeTypeHistory());
        AttributeTypeHistory obj = dod.getNewTransientAttributeTypeHistory(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AttributeTypeHistory' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AttributeTypeHistory' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AttributeTypeHistoryIntegrationTest.testRemove() {
        AttributeTypeHistory obj = dod.getRandomAttributeTypeHistory();
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeHistory' failed to provide an identifier", id);
        obj = AttributeTypeHistory.findAttributeTypeHistory(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AttributeTypeHistory' with identifier '" + id + "'", AttributeTypeHistory.findAttributeTypeHistory(id));
    }
    
}
