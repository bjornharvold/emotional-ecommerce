// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.SubdepartmentDepartment;
import com.lela.data.domain.entity.SubdepartmentDepartmentDataOnDemand;
import com.lela.data.domain.entity.SubdepartmentDepartmentIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SubdepartmentDepartmentIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SubdepartmentDepartmentIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SubdepartmentDepartmentIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SubdepartmentDepartmentIntegrationTest: @Transactional;
    
    @Autowired
    SubdepartmentDepartmentDataOnDemand SubdepartmentDepartmentIntegrationTest.dod;
    
    @Test
    public void SubdepartmentDepartmentIntegrationTest.testCountSubdepartmentDepartments() {
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to initialize correctly", dod.getRandomSubdepartmentDepartment());
        long count = SubdepartmentDepartment.countSubdepartmentDepartments();
        Assert.assertTrue("Counter for 'SubdepartmentDepartment' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SubdepartmentDepartmentIntegrationTest.testFindSubdepartmentDepartment() {
        SubdepartmentDepartment obj = dod.getRandomSubdepartmentDepartment();
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to provide an identifier", id);
        obj = SubdepartmentDepartment.findSubdepartmentDepartment(id);
        Assert.assertNotNull("Find method for 'SubdepartmentDepartment' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SubdepartmentDepartment' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SubdepartmentDepartmentIntegrationTest.testFindAllSubdepartmentDepartments() {
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to initialize correctly", dod.getRandomSubdepartmentDepartment());
        long count = SubdepartmentDepartment.countSubdepartmentDepartments();
        Assert.assertTrue("Too expensive to perform a find all test for 'SubdepartmentDepartment', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SubdepartmentDepartment> result = SubdepartmentDepartment.findAllSubdepartmentDepartments();
        Assert.assertNotNull("Find all method for 'SubdepartmentDepartment' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SubdepartmentDepartment' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SubdepartmentDepartmentIntegrationTest.testFindSubdepartmentDepartmentEntries() {
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to initialize correctly", dod.getRandomSubdepartmentDepartment());
        long count = SubdepartmentDepartment.countSubdepartmentDepartments();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SubdepartmentDepartment> result = SubdepartmentDepartment.findSubdepartmentDepartmentEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SubdepartmentDepartment' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SubdepartmentDepartment' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SubdepartmentDepartmentIntegrationTest.testFlush() {
        SubdepartmentDepartment obj = dod.getRandomSubdepartmentDepartment();
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to provide an identifier", id);
        obj = SubdepartmentDepartment.findSubdepartmentDepartment(id);
        Assert.assertNotNull("Find method for 'SubdepartmentDepartment' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifySubdepartmentDepartment(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'SubdepartmentDepartment' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SubdepartmentDepartmentIntegrationTest.testMergeUpdate() {
        SubdepartmentDepartment obj = dod.getRandomSubdepartmentDepartment();
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to provide an identifier", id);
        obj = SubdepartmentDepartment.findSubdepartmentDepartment(id);
        boolean modified =  dod.modifySubdepartmentDepartment(obj);
        Integer currentVersion = obj.getVersion();
        SubdepartmentDepartment merged = (SubdepartmentDepartment)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'SubdepartmentDepartment' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SubdepartmentDepartmentIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to initialize correctly", dod.getRandomSubdepartmentDepartment());
        SubdepartmentDepartment obj = dod.getNewTransientSubdepartmentDepartment(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SubdepartmentDepartment' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'SubdepartmentDepartment' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SubdepartmentDepartmentIntegrationTest.testRemove() {
        SubdepartmentDepartment obj = dod.getRandomSubdepartmentDepartment();
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SubdepartmentDepartment' failed to provide an identifier", id);
        obj = SubdepartmentDepartment.findSubdepartmentDepartment(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SubdepartmentDepartment' with identifier '" + id + "'", SubdepartmentDepartment.findSubdepartmentDepartment(id));
    }
    
}
