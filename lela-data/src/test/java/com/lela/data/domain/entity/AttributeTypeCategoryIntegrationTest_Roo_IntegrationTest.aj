// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AttributeTypeCategory;
import com.lela.data.domain.entity.AttributeTypeCategoryDataOnDemand;
import com.lela.data.domain.entity.AttributeTypeCategoryIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AttributeTypeCategoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AttributeTypeCategoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AttributeTypeCategoryIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AttributeTypeCategoryIntegrationTest: @Transactional;
    
    @Autowired
    AttributeTypeCategoryDataOnDemand AttributeTypeCategoryIntegrationTest.dod;
    
    @Test
    public void AttributeTypeCategoryIntegrationTest.testCountAttributeTypeCategorys() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to initialize correctly", dod.getRandomAttributeTypeCategory());
        long count = AttributeTypeCategory.countAttributeTypeCategorys();
        Assert.assertTrue("Counter for 'AttributeTypeCategory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AttributeTypeCategoryIntegrationTest.testFindAttributeTypeCategory() {
        AttributeTypeCategory obj = dod.getRandomAttributeTypeCategory();
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to provide an identifier", id);
        obj = AttributeTypeCategory.findAttributeTypeCategory(id);
        Assert.assertNotNull("Find method for 'AttributeTypeCategory' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AttributeTypeCategory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AttributeTypeCategoryIntegrationTest.testFindAllAttributeTypeCategorys() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to initialize correctly", dod.getRandomAttributeTypeCategory());
        long count = AttributeTypeCategory.countAttributeTypeCategorys();
        Assert.assertTrue("Too expensive to perform a find all test for 'AttributeTypeCategory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AttributeTypeCategory> result = AttributeTypeCategory.findAllAttributeTypeCategorys();
        Assert.assertNotNull("Find all method for 'AttributeTypeCategory' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AttributeTypeCategory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AttributeTypeCategoryIntegrationTest.testFindAttributeTypeCategoryEntries() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to initialize correctly", dod.getRandomAttributeTypeCategory());
        long count = AttributeTypeCategory.countAttributeTypeCategorys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AttributeTypeCategory> result = AttributeTypeCategory.findAttributeTypeCategoryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AttributeTypeCategory' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AttributeTypeCategory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AttributeTypeCategoryIntegrationTest.testFlush() {
        AttributeTypeCategory obj = dod.getRandomAttributeTypeCategory();
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to provide an identifier", id);
        obj = AttributeTypeCategory.findAttributeTypeCategory(id);
        Assert.assertNotNull("Find method for 'AttributeTypeCategory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAttributeTypeCategory(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AttributeTypeCategory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AttributeTypeCategoryIntegrationTest.testMergeUpdate() {
        AttributeTypeCategory obj = dod.getRandomAttributeTypeCategory();
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to provide an identifier", id);
        obj = AttributeTypeCategory.findAttributeTypeCategory(id);
        boolean modified =  dod.modifyAttributeTypeCategory(obj);
        Integer currentVersion = obj.getVersion();
        AttributeTypeCategory merged = (AttributeTypeCategory)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'AttributeTypeCategory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AttributeTypeCategoryIntegrationTest.testRemove() {
        AttributeTypeCategory obj = dod.getRandomAttributeTypeCategory();
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeCategory' failed to provide an identifier", id);
        obj = AttributeTypeCategory.findAttributeTypeCategory(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AttributeTypeCategory' with identifier '" + id + "'", AttributeTypeCategory.findAttributeTypeCategory(id));
    }
    
}
