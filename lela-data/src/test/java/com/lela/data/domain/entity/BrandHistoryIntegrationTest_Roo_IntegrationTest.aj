// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.BrandHistory;
import com.lela.data.domain.entity.BrandHistoryDataOnDemand;
import com.lela.data.domain.entity.BrandHistoryIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect BrandHistoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: BrandHistoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: BrandHistoryIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: BrandHistoryIntegrationTest: @Transactional;
    
    @Autowired
    BrandHistoryDataOnDemand BrandHistoryIntegrationTest.dod;
    
    @Test
    public void BrandHistoryIntegrationTest.testCountBrandHistorys() {
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to initialize correctly", dod.getRandomBrandHistory());
        long count = BrandHistory.countBrandHistorys();
        Assert.assertTrue("Counter for 'BrandHistory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void BrandHistoryIntegrationTest.testFindBrandHistory() {
        BrandHistory obj = dod.getRandomBrandHistory();
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to provide an identifier", id);
        obj = BrandHistory.findBrandHistory(id);
        Assert.assertNotNull("Find method for 'BrandHistory' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'BrandHistory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void BrandHistoryIntegrationTest.testFindAllBrandHistorys() {
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to initialize correctly", dod.getRandomBrandHistory());
        long count = BrandHistory.countBrandHistorys();
        Assert.assertTrue("Too expensive to perform a find all test for 'BrandHistory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<BrandHistory> result = BrandHistory.findAllBrandHistorys();
        Assert.assertNotNull("Find all method for 'BrandHistory' illegally returned null", result);
        Assert.assertTrue("Find all method for 'BrandHistory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void BrandHistoryIntegrationTest.testFindBrandHistoryEntries() {
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to initialize correctly", dod.getRandomBrandHistory());
        long count = BrandHistory.countBrandHistorys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<BrandHistory> result = BrandHistory.findBrandHistoryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'BrandHistory' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'BrandHistory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void BrandHistoryIntegrationTest.testFlush() {
        BrandHistory obj = dod.getRandomBrandHistory();
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to provide an identifier", id);
        obj = BrandHistory.findBrandHistory(id);
        Assert.assertNotNull("Find method for 'BrandHistory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyBrandHistory(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'BrandHistory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void BrandHistoryIntegrationTest.testMergeUpdate() {
        BrandHistory obj = dod.getRandomBrandHistory();
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to provide an identifier", id);
        obj = BrandHistory.findBrandHistory(id);
        boolean modified =  dod.modifyBrandHistory(obj);
        Integer currentVersion = obj.getVersion();
        BrandHistory merged = (BrandHistory)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'BrandHistory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void BrandHistoryIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to initialize correctly", dod.getRandomBrandHistory());
        BrandHistory obj = dod.getNewTransientBrandHistory(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'BrandHistory' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'BrandHistory' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void BrandHistoryIntegrationTest.testRemove() {
        BrandHistory obj = dod.getRandomBrandHistory();
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandHistory' failed to provide an identifier", id);
        obj = BrandHistory.findBrandHistory(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'BrandHistory' with identifier '" + id + "'", BrandHistory.findBrandHistory(id));
    }
    
}
