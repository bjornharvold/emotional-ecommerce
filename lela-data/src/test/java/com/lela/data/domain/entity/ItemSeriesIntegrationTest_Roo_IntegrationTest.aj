// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemSeries;
import com.lela.data.domain.entity.ItemSeriesDataOnDemand;
import com.lela.data.domain.entity.ItemSeriesIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ItemSeriesIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ItemSeriesIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ItemSeriesIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ItemSeriesIntegrationTest: @Transactional;
    
    @Autowired
    ItemSeriesDataOnDemand ItemSeriesIntegrationTest.dod;
    
    @Test
    public void ItemSeriesIntegrationTest.testCountItemSerieses() {
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to initialize correctly", dod.getRandomItemSeries());
        long count = ItemSeries.countItemSerieses();
        Assert.assertTrue("Counter for 'ItemSeries' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ItemSeriesIntegrationTest.testFindItemSeries() {
        ItemSeries obj = dod.getRandomItemSeries();
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to provide an identifier", id);
        obj = ItemSeries.findItemSeries(id);
        Assert.assertNotNull("Find method for 'ItemSeries' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ItemSeries' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ItemSeriesIntegrationTest.testFindAllItemSerieses() {
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to initialize correctly", dod.getRandomItemSeries());
        long count = ItemSeries.countItemSerieses();
        Assert.assertTrue("Too expensive to perform a find all test for 'ItemSeries', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ItemSeries> result = ItemSeries.findAllItemSerieses();
        Assert.assertNotNull("Find all method for 'ItemSeries' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ItemSeries' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ItemSeriesIntegrationTest.testFindItemSeriesEntries() {
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to initialize correctly", dod.getRandomItemSeries());
        long count = ItemSeries.countItemSerieses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ItemSeries> result = ItemSeries.findItemSeriesEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ItemSeries' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ItemSeries' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ItemSeriesIntegrationTest.testFlush() {
        ItemSeries obj = dod.getRandomItemSeries();
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to provide an identifier", id);
        obj = ItemSeries.findItemSeries(id);
        Assert.assertNotNull("Find method for 'ItemSeries' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyItemSeries(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ItemSeries' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemSeriesIntegrationTest.testMergeUpdate() {
        ItemSeries obj = dod.getRandomItemSeries();
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to provide an identifier", id);
        obj = ItemSeries.findItemSeries(id);
        boolean modified =  dod.modifyItemSeries(obj);
        Integer currentVersion = obj.getVersion();
        ItemSeries merged = (ItemSeries)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ItemSeries' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemSeriesIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to initialize correctly", dod.getRandomItemSeries());
        ItemSeries obj = dod.getNewTransientItemSeries(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ItemSeries' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ItemSeries' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ItemSeriesIntegrationTest.testRemove() {
        ItemSeries obj = dod.getRandomItemSeries();
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemSeries' failed to provide an identifier", id);
        obj = ItemSeries.findItemSeries(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ItemSeries' with identifier '" + id + "'", ItemSeries.findItemSeries(id));
    }
    
}
