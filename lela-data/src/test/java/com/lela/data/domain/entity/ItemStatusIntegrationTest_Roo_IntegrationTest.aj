// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemStatus;
import com.lela.data.domain.entity.ItemStatusDataOnDemand;
import com.lela.data.domain.entity.ItemStatusIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ItemStatusIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ItemStatusIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ItemStatusIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ItemStatusIntegrationTest: @Transactional;
    
    @Autowired
    ItemStatusDataOnDemand ItemStatusIntegrationTest.dod;
    
    @Test
    public void ItemStatusIntegrationTest.testCountItemStatuses() {
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to initialize correctly", dod.getRandomItemStatus());
        long count = ItemStatus.countItemStatuses();
        Assert.assertTrue("Counter for 'ItemStatus' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ItemStatusIntegrationTest.testFindItemStatus() {
        ItemStatus obj = dod.getRandomItemStatus();
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to provide an identifier", id);
        obj = ItemStatus.findItemStatus(id);
        Assert.assertNotNull("Find method for 'ItemStatus' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ItemStatus' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ItemStatusIntegrationTest.testFindAllItemStatuses() {
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to initialize correctly", dod.getRandomItemStatus());
        long count = ItemStatus.countItemStatuses();
        Assert.assertTrue("Too expensive to perform a find all test for 'ItemStatus', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ItemStatus> result = ItemStatus.findAllItemStatuses();
        Assert.assertNotNull("Find all method for 'ItemStatus' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ItemStatus' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ItemStatusIntegrationTest.testFindItemStatusEntries() {
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to initialize correctly", dod.getRandomItemStatus());
        long count = ItemStatus.countItemStatuses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ItemStatus> result = ItemStatus.findItemStatusEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ItemStatus' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ItemStatus' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ItemStatusIntegrationTest.testFlush() {
        ItemStatus obj = dod.getRandomItemStatus();
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to provide an identifier", id);
        obj = ItemStatus.findItemStatus(id);
        Assert.assertNotNull("Find method for 'ItemStatus' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyItemStatus(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ItemStatus' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemStatusIntegrationTest.testMergeUpdate() {
        ItemStatus obj = dod.getRandomItemStatus();
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to provide an identifier", id);
        obj = ItemStatus.findItemStatus(id);
        boolean modified =  dod.modifyItemStatus(obj);
        Integer currentVersion = obj.getVersion();
        ItemStatus merged = (ItemStatus)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ItemStatus' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemStatusIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to initialize correctly", dod.getRandomItemStatus());
        ItemStatus obj = dod.getNewTransientItemStatus(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ItemStatus' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ItemStatus' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ItemStatusIntegrationTest.testRemove() {
        ItemStatus obj = dod.getRandomItemStatus();
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemStatus' failed to provide an identifier", id);
        obj = ItemStatus.findItemStatus(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ItemStatus' with identifier '" + id + "'", ItemStatus.findItemStatus(id));
    }
    
}
