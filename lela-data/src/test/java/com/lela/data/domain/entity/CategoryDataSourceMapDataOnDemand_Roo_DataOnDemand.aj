// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryDataOnDemand;
import com.lela.data.domain.entity.CategoryDataSourceMap;
import com.lela.data.domain.entity.CategoryDataSourceMapDataOnDemand;
import com.lela.data.domain.entity.DataSourceTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect CategoryDataSourceMapDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CategoryDataSourceMapDataOnDemand: @Component;
    
    private Random CategoryDataSourceMapDataOnDemand.rnd = new SecureRandom();
    
    private List<CategoryDataSourceMap> CategoryDataSourceMapDataOnDemand.data;
    
    @Autowired
    CategoryDataOnDemand CategoryDataSourceMapDataOnDemand.categoryDataOnDemand;
    
    @Autowired
    DataSourceTypeDataOnDemand CategoryDataSourceMapDataOnDemand.dataSourceTypeDataOnDemand;
    
    public CategoryDataSourceMap CategoryDataSourceMapDataOnDemand.getNewTransientCategoryDataSourceMap(int index) {
        CategoryDataSourceMap obj = new CategoryDataSourceMap();
        setAttributeName(obj, index);
        setAttributeValue(obj, index);
        setDataSoureceString(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        return obj;
    }
    
    public void CategoryDataSourceMapDataOnDemand.setAttributeName(CategoryDataSourceMap obj, int index) {
        String attributeName = "attributeName_" + index;
        obj.setAttributeName(attributeName);
    }
    
    public void CategoryDataSourceMapDataOnDemand.setAttributeValue(CategoryDataSourceMap obj, int index) {
        String attributeValue = "attributeValue_" + index;
        obj.setAttributeValue(attributeValue);
    }
    
    public void CategoryDataSourceMapDataOnDemand.setDataSoureceString(CategoryDataSourceMap obj, int index) {
        String dataSoureceString = "dataSoureceString_" + index;
        obj.setDataSoureceString(dataSoureceString);
    }
    
    public void CategoryDataSourceMapDataOnDemand.setDateCreated(CategoryDataSourceMap obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void CategoryDataSourceMapDataOnDemand.setDateModified(CategoryDataSourceMap obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public CategoryDataSourceMap CategoryDataSourceMapDataOnDemand.getSpecificCategoryDataSourceMap(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CategoryDataSourceMap obj = data.get(index);
        Long id = obj.getId();
        return CategoryDataSourceMap.findCategoryDataSourceMap(id);
    }
    
    public CategoryDataSourceMap CategoryDataSourceMapDataOnDemand.getRandomCategoryDataSourceMap() {
        init();
        CategoryDataSourceMap obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CategoryDataSourceMap.findCategoryDataSourceMap(id);
    }
    
    public boolean CategoryDataSourceMapDataOnDemand.modifyCategoryDataSourceMap(CategoryDataSourceMap obj) {
        return false;
    }
    
    public void CategoryDataSourceMapDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CategoryDataSourceMap.findCategoryDataSourceMapEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CategoryDataSourceMap' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CategoryDataSourceMap>();
        for (int i = 0; i < 10; i++) {
            CategoryDataSourceMap obj = getNewTransientCategoryDataSourceMap(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
