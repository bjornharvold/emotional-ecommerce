// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AttributeTypeNormalization;
import com.lela.data.domain.entity.AttributeTypeNormalizationDataOnDemand;
import com.lela.data.domain.entity.AttributeTypeNormalizationIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AttributeTypeNormalizationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AttributeTypeNormalizationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AttributeTypeNormalizationIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AttributeTypeNormalizationIntegrationTest: @Transactional;
    
    @Autowired
    AttributeTypeNormalizationDataOnDemand AttributeTypeNormalizationIntegrationTest.dod;
    
    @Test
    public void AttributeTypeNormalizationIntegrationTest.testCountAttributeTypeNormalizations() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to initialize correctly", dod.getRandomAttributeTypeNormalization());
        long count = AttributeTypeNormalization.countAttributeTypeNormalizations();
        Assert.assertTrue("Counter for 'AttributeTypeNormalization' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AttributeTypeNormalizationIntegrationTest.testFindAttributeTypeNormalization() {
        AttributeTypeNormalization obj = dod.getRandomAttributeTypeNormalization();
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to provide an identifier", id);
        obj = AttributeTypeNormalization.findAttributeTypeNormalization(id);
        Assert.assertNotNull("Find method for 'AttributeTypeNormalization' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AttributeTypeNormalization' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AttributeTypeNormalizationIntegrationTest.testFindAllAttributeTypeNormalizations() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to initialize correctly", dod.getRandomAttributeTypeNormalization());
        long count = AttributeTypeNormalization.countAttributeTypeNormalizations();
        Assert.assertTrue("Too expensive to perform a find all test for 'AttributeTypeNormalization', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AttributeTypeNormalization> result = AttributeTypeNormalization.findAllAttributeTypeNormalizations();
        Assert.assertNotNull("Find all method for 'AttributeTypeNormalization' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AttributeTypeNormalization' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AttributeTypeNormalizationIntegrationTest.testFindAttributeTypeNormalizationEntries() {
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to initialize correctly", dod.getRandomAttributeTypeNormalization());
        long count = AttributeTypeNormalization.countAttributeTypeNormalizations();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AttributeTypeNormalization> result = AttributeTypeNormalization.findAttributeTypeNormalizationEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AttributeTypeNormalization' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AttributeTypeNormalization' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AttributeTypeNormalizationIntegrationTest.testFlush() {
        AttributeTypeNormalization obj = dod.getRandomAttributeTypeNormalization();
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to provide an identifier", id);
        obj = AttributeTypeNormalization.findAttributeTypeNormalization(id);
        Assert.assertNotNull("Find method for 'AttributeTypeNormalization' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAttributeTypeNormalization(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AttributeTypeNormalization' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AttributeTypeNormalizationIntegrationTest.testMergeUpdate() {
        AttributeTypeNormalization obj = dod.getRandomAttributeTypeNormalization();
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to provide an identifier", id);
        obj = AttributeTypeNormalization.findAttributeTypeNormalization(id);
        boolean modified =  dod.modifyAttributeTypeNormalization(obj);
        Integer currentVersion = obj.getVersion();
        AttributeTypeNormalization merged = (AttributeTypeNormalization)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'AttributeTypeNormalization' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AttributeTypeNormalizationIntegrationTest.testRemove() {
        AttributeTypeNormalization obj = dod.getRandomAttributeTypeNormalization();
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AttributeTypeNormalization' failed to provide an identifier", id);
        obj = AttributeTypeNormalization.findAttributeTypeNormalization(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AttributeTypeNormalization' with identifier '" + id + "'", AttributeTypeNormalization.findAttributeTypeNormalization(id));
    }
    
}
