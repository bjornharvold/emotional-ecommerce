// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ImageSourceType;
import com.lela.data.domain.entity.ImageSourceTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ImageSourceTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ImageSourceTypeDataOnDemand: @Component;
    
    private Random ImageSourceTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<ImageSourceType> ImageSourceTypeDataOnDemand.data;
    
    public ImageSourceType ImageSourceTypeDataOnDemand.getNewTransientImageSourceType(int index) {
        ImageSourceType obj = new ImageSourceType();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setSourceTypeName(obj, index);
        return obj;
    }
    
    public void ImageSourceTypeDataOnDemand.setDateCreated(ImageSourceType obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ImageSourceTypeDataOnDemand.setDateModified(ImageSourceType obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ImageSourceTypeDataOnDemand.setSourceTypeName(ImageSourceType obj, int index) {
        String sourceTypeName = "sourceTypeName_" + index;
        if (sourceTypeName.length() > 255) {
            sourceTypeName = sourceTypeName.substring(0, 255);
        }
        obj.setSourceTypeName(sourceTypeName);
    }
    
    public ImageSourceType ImageSourceTypeDataOnDemand.getSpecificImageSourceType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ImageSourceType obj = data.get(index);
        Long id = obj.getId();
        return ImageSourceType.findImageSourceType(id);
    }
    
    public ImageSourceType ImageSourceTypeDataOnDemand.getRandomImageSourceType() {
        init();
        ImageSourceType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ImageSourceType.findImageSourceType(id);
    }
    
    public boolean ImageSourceTypeDataOnDemand.modifyImageSourceType(ImageSourceType obj) {
        return false;
    }
    
    public void ImageSourceTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ImageSourceType.findImageSourceTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ImageSourceType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ImageSourceType>();
        for (int i = 0; i < 10; i++) {
            ImageSourceType obj = getNewTransientImageSourceType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
