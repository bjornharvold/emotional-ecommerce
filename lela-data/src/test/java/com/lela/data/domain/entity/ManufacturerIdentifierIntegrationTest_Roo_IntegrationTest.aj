// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ManufacturerIdentifier;
import com.lela.data.domain.entity.ManufacturerIdentifierDataOnDemand;
import com.lela.data.domain.entity.ManufacturerIdentifierIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ManufacturerIdentifierIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ManufacturerIdentifierIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ManufacturerIdentifierIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ManufacturerIdentifierIntegrationTest: @Transactional;
    
    @Autowired
    ManufacturerIdentifierDataOnDemand ManufacturerIdentifierIntegrationTest.dod;
    
    @Test
    public void ManufacturerIdentifierIntegrationTest.testCountManufacturerIdentifiers() {
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to initialize correctly", dod.getRandomManufacturerIdentifier());
        long count = ManufacturerIdentifier.countManufacturerIdentifiers();
        Assert.assertTrue("Counter for 'ManufacturerIdentifier' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ManufacturerIdentifierIntegrationTest.testFindManufacturerIdentifier() {
        ManufacturerIdentifier obj = dod.getRandomManufacturerIdentifier();
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to provide an identifier", id);
        obj = ManufacturerIdentifier.findManufacturerIdentifier(id);
        Assert.assertNotNull("Find method for 'ManufacturerIdentifier' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ManufacturerIdentifier' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ManufacturerIdentifierIntegrationTest.testFindAllManufacturerIdentifiers() {
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to initialize correctly", dod.getRandomManufacturerIdentifier());
        long count = ManufacturerIdentifier.countManufacturerIdentifiers();
        Assert.assertTrue("Too expensive to perform a find all test for 'ManufacturerIdentifier', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ManufacturerIdentifier> result = ManufacturerIdentifier.findAllManufacturerIdentifiers();
        Assert.assertNotNull("Find all method for 'ManufacturerIdentifier' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ManufacturerIdentifier' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ManufacturerIdentifierIntegrationTest.testFindManufacturerIdentifierEntries() {
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to initialize correctly", dod.getRandomManufacturerIdentifier());
        long count = ManufacturerIdentifier.countManufacturerIdentifiers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ManufacturerIdentifier> result = ManufacturerIdentifier.findManufacturerIdentifierEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ManufacturerIdentifier' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ManufacturerIdentifier' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ManufacturerIdentifierIntegrationTest.testFlush() {
        ManufacturerIdentifier obj = dod.getRandomManufacturerIdentifier();
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to provide an identifier", id);
        obj = ManufacturerIdentifier.findManufacturerIdentifier(id);
        Assert.assertNotNull("Find method for 'ManufacturerIdentifier' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyManufacturerIdentifier(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ManufacturerIdentifier' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ManufacturerIdentifierIntegrationTest.testMergeUpdate() {
        ManufacturerIdentifier obj = dod.getRandomManufacturerIdentifier();
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to provide an identifier", id);
        obj = ManufacturerIdentifier.findManufacturerIdentifier(id);
        boolean modified =  dod.modifyManufacturerIdentifier(obj);
        Integer currentVersion = obj.getVersion();
        ManufacturerIdentifier merged = (ManufacturerIdentifier)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ManufacturerIdentifier' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ManufacturerIdentifierIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to initialize correctly", dod.getRandomManufacturerIdentifier());
        ManufacturerIdentifier obj = dod.getNewTransientManufacturerIdentifier(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ManufacturerIdentifier' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ManufacturerIdentifier' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ManufacturerIdentifierIntegrationTest.testRemove() {
        ManufacturerIdentifier obj = dod.getRandomManufacturerIdentifier();
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ManufacturerIdentifier' failed to provide an identifier", id);
        obj = ManufacturerIdentifier.findManufacturerIdentifier(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ManufacturerIdentifier' with identifier '" + id + "'", ManufacturerIdentifier.findManufacturerIdentifier(id));
    }
    
}
