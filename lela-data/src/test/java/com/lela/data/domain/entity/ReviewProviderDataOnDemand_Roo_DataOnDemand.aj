// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ReviewProvider;
import com.lela.data.domain.entity.ReviewProviderDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ReviewProviderDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ReviewProviderDataOnDemand: @Component;
    
    private Random ReviewProviderDataOnDemand.rnd = new SecureRandom();
    
    private List<ReviewProvider> ReviewProviderDataOnDemand.data;
    
    public ReviewProvider ReviewProviderDataOnDemand.getNewTransientReviewProvider(int index) {
        ReviewProvider obj = new ReviewProvider();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setReviewProviderName(obj, index);
        return obj;
    }
    
    public void ReviewProviderDataOnDemand.setDateCreated(ReviewProvider obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ReviewProviderDataOnDemand.setDateModified(ReviewProvider obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ReviewProviderDataOnDemand.setReviewProviderName(ReviewProvider obj, int index) {
        String reviewProviderName = "reviewProviderName_" + index;
        obj.setReviewProviderName(reviewProviderName);
    }
    
    public ReviewProvider ReviewProviderDataOnDemand.getSpecificReviewProvider(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ReviewProvider obj = data.get(index);
        Long id = obj.getId();
        return ReviewProvider.findReviewProvider(id);
    }
    
    public ReviewProvider ReviewProviderDataOnDemand.getRandomReviewProvider() {
        init();
        ReviewProvider obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ReviewProvider.findReviewProvider(id);
    }
    
    public boolean ReviewProviderDataOnDemand.modifyReviewProvider(ReviewProvider obj) {
        return false;
    }
    
    public void ReviewProviderDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ReviewProvider.findReviewProviderEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ReviewProvider' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ReviewProvider>();
        for (int i = 0; i < 10; i++) {
            ReviewProvider obj = getNewTransientReviewProvider(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
