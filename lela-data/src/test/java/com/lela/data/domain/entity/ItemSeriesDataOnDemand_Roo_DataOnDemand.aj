// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Item;
import com.lela.data.domain.entity.ItemDataOnDemand;
import com.lela.data.domain.entity.ItemSeries;
import com.lela.data.domain.entity.ItemSeriesDataOnDemand;
import com.lela.data.domain.entity.Series;
import com.lela.data.domain.entity.SeriesDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ItemSeriesDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ItemSeriesDataOnDemand: @Component;
    
    private Random ItemSeriesDataOnDemand.rnd = new SecureRandom();
    
    private List<ItemSeries> ItemSeriesDataOnDemand.data;
    
    @Autowired
    ItemDataOnDemand ItemSeriesDataOnDemand.itemDataOnDemand;
    
    @Autowired
    SeriesDataOnDemand ItemSeriesDataOnDemand.seriesDataOnDemand;
    
    public ItemSeries ItemSeriesDataOnDemand.getNewTransientItemSeries(int index) {
        ItemSeries obj = new ItemSeries();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setItem(obj, index);
        setPrimaryFlag(obj, index);
        setSeries(obj, index);
        return obj;
    }
    
    public void ItemSeriesDataOnDemand.setDateCreated(ItemSeries obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ItemSeriesDataOnDemand.setDateModified(ItemSeries obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ItemSeriesDataOnDemand.setItem(ItemSeries obj, int index) {
        Item item = itemDataOnDemand.getRandomItem();
        obj.setItem(item);
    }
    
    public void ItemSeriesDataOnDemand.setPrimaryFlag(ItemSeries obj, int index) {
        Boolean primaryFlag = Boolean.TRUE;
        obj.setPrimaryFlag(primaryFlag);
    }
    
    public void ItemSeriesDataOnDemand.setSeries(ItemSeries obj, int index) {
        Series series = seriesDataOnDemand.getRandomSeries();
        obj.setSeries(series);
    }
    
    public ItemSeries ItemSeriesDataOnDemand.getSpecificItemSeries(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ItemSeries obj = data.get(index);
        Long id = obj.getId();
        return ItemSeries.findItemSeries(id);
    }
    
    public ItemSeries ItemSeriesDataOnDemand.getRandomItemSeries() {
        init();
        ItemSeries obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ItemSeries.findItemSeries(id);
    }
    
    public boolean ItemSeriesDataOnDemand.modifyItemSeries(ItemSeries obj) {
        return false;
    }
    
    public void ItemSeriesDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ItemSeries.findItemSeriesEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ItemSeries' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ItemSeries>();
        for (int i = 0; i < 10; i++) {
            ItemSeries obj = getNewTransientItemSeries(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
