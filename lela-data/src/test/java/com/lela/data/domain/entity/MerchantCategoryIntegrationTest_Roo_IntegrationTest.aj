// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.MerchantCategory;
import com.lela.data.domain.entity.MerchantCategoryDataOnDemand;
import com.lela.data.domain.entity.MerchantCategoryIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect MerchantCategoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: MerchantCategoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: MerchantCategoryIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: MerchantCategoryIntegrationTest: @Transactional;
    
    @Autowired
    MerchantCategoryDataOnDemand MerchantCategoryIntegrationTest.dod;
    
    @Test
    public void MerchantCategoryIntegrationTest.testCountMerchantCategorys() {
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to initialize correctly", dod.getRandomMerchantCategory());
        long count = MerchantCategory.countMerchantCategorys();
        Assert.assertTrue("Counter for 'MerchantCategory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void MerchantCategoryIntegrationTest.testFindMerchantCategory() {
        MerchantCategory obj = dod.getRandomMerchantCategory();
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to provide an identifier", id);
        obj = MerchantCategory.findMerchantCategory(id);
        Assert.assertNotNull("Find method for 'MerchantCategory' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'MerchantCategory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void MerchantCategoryIntegrationTest.testFindAllMerchantCategorys() {
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to initialize correctly", dod.getRandomMerchantCategory());
        long count = MerchantCategory.countMerchantCategorys();
        Assert.assertTrue("Too expensive to perform a find all test for 'MerchantCategory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<MerchantCategory> result = MerchantCategory.findAllMerchantCategorys();
        Assert.assertNotNull("Find all method for 'MerchantCategory' illegally returned null", result);
        Assert.assertTrue("Find all method for 'MerchantCategory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void MerchantCategoryIntegrationTest.testFindMerchantCategoryEntries() {
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to initialize correctly", dod.getRandomMerchantCategory());
        long count = MerchantCategory.countMerchantCategorys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<MerchantCategory> result = MerchantCategory.findMerchantCategoryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'MerchantCategory' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'MerchantCategory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void MerchantCategoryIntegrationTest.testFlush() {
        MerchantCategory obj = dod.getRandomMerchantCategory();
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to provide an identifier", id);
        obj = MerchantCategory.findMerchantCategory(id);
        Assert.assertNotNull("Find method for 'MerchantCategory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyMerchantCategory(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'MerchantCategory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MerchantCategoryIntegrationTest.testMergeUpdate() {
        MerchantCategory obj = dod.getRandomMerchantCategory();
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to provide an identifier", id);
        obj = MerchantCategory.findMerchantCategory(id);
        boolean modified =  dod.modifyMerchantCategory(obj);
        Integer currentVersion = obj.getVersion();
        MerchantCategory merged = (MerchantCategory)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'MerchantCategory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MerchantCategoryIntegrationTest.testRemove() {
        MerchantCategory obj = dod.getRandomMerchantCategory();
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MerchantCategory' failed to provide an identifier", id);
        obj = MerchantCategory.findMerchantCategory(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'MerchantCategory' with identifier '" + id + "'", MerchantCategory.findMerchantCategory(id));
    }
    
}
