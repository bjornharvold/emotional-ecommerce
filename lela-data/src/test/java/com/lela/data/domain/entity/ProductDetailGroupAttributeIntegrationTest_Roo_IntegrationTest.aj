// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductDetailGroupAttribute;
import com.lela.data.domain.entity.ProductDetailGroupAttributeDataOnDemand;
import com.lela.data.domain.entity.ProductDetailGroupAttributeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductDetailGroupAttributeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductDetailGroupAttributeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductDetailGroupAttributeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductDetailGroupAttributeIntegrationTest: @Transactional;
    
    @Autowired
    ProductDetailGroupAttributeDataOnDemand ProductDetailGroupAttributeIntegrationTest.dod;
    
    @Test
    public void ProductDetailGroupAttributeIntegrationTest.testCountProductDetailGroupAttributes() {
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to initialize correctly", dod.getRandomProductDetailGroupAttribute());
        long count = ProductDetailGroupAttribute.countProductDetailGroupAttributes();
        Assert.assertTrue("Counter for 'ProductDetailGroupAttribute' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductDetailGroupAttributeIntegrationTest.testFindProductDetailGroupAttribute() {
        ProductDetailGroupAttribute obj = dod.getRandomProductDetailGroupAttribute();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to provide an identifier", id);
        obj = ProductDetailGroupAttribute.findProductDetailGroupAttribute(id);
        Assert.assertNotNull("Find method for 'ProductDetailGroupAttribute' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductDetailGroupAttribute' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductDetailGroupAttributeIntegrationTest.testFindAllProductDetailGroupAttributes() {
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to initialize correctly", dod.getRandomProductDetailGroupAttribute());
        long count = ProductDetailGroupAttribute.countProductDetailGroupAttributes();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductDetailGroupAttribute', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductDetailGroupAttribute> result = ProductDetailGroupAttribute.findAllProductDetailGroupAttributes();
        Assert.assertNotNull("Find all method for 'ProductDetailGroupAttribute' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductDetailGroupAttribute' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductDetailGroupAttributeIntegrationTest.testFindProductDetailGroupAttributeEntries() {
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to initialize correctly", dod.getRandomProductDetailGroupAttribute());
        long count = ProductDetailGroupAttribute.countProductDetailGroupAttributes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductDetailGroupAttribute> result = ProductDetailGroupAttribute.findProductDetailGroupAttributeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductDetailGroupAttribute' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductDetailGroupAttribute' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductDetailGroupAttributeIntegrationTest.testFlush() {
        ProductDetailGroupAttribute obj = dod.getRandomProductDetailGroupAttribute();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to provide an identifier", id);
        obj = ProductDetailGroupAttribute.findProductDetailGroupAttribute(id);
        Assert.assertNotNull("Find method for 'ProductDetailGroupAttribute' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProductDetailGroupAttribute(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProductDetailGroupAttribute' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductDetailGroupAttributeIntegrationTest.testMergeUpdate() {
        ProductDetailGroupAttribute obj = dod.getRandomProductDetailGroupAttribute();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to provide an identifier", id);
        obj = ProductDetailGroupAttribute.findProductDetailGroupAttribute(id);
        boolean modified =  dod.modifyProductDetailGroupAttribute(obj);
        Integer currentVersion = obj.getVersion();
        ProductDetailGroupAttribute merged = (ProductDetailGroupAttribute)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProductDetailGroupAttribute' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductDetailGroupAttributeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to initialize correctly", dod.getRandomProductDetailGroupAttribute());
        ProductDetailGroupAttribute obj = dod.getNewTransientProductDetailGroupAttribute(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProductDetailGroupAttribute' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductDetailGroupAttribute' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductDetailGroupAttributeIntegrationTest.testRemove() {
        ProductDetailGroupAttribute obj = dod.getRandomProductDetailGroupAttribute();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroupAttribute' failed to provide an identifier", id);
        obj = ProductDetailGroupAttribute.findProductDetailGroupAttribute(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductDetailGroupAttribute' with identifier '" + id + "'", ProductDetailGroupAttribute.findProductDetailGroupAttribute(id));
    }
    
}
