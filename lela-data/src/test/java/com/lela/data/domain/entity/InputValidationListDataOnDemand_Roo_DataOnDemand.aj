// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.InputValidationList;
import com.lela.data.domain.entity.InputValidationListDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect InputValidationListDataOnDemand_Roo_DataOnDemand {
    
    declare @type: InputValidationListDataOnDemand: @Component;
    
    private Random InputValidationListDataOnDemand.rnd = new SecureRandom();
    
    private List<InputValidationList> InputValidationListDataOnDemand.data;
    
    public InputValidationList InputValidationListDataOnDemand.getNewTransientInputValidationList(int index) {
        InputValidationList obj = new InputValidationList();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setListName(obj, index);
        return obj;
    }
    
    public void InputValidationListDataOnDemand.setDateCreated(InputValidationList obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void InputValidationListDataOnDemand.setDateModified(InputValidationList obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void InputValidationListDataOnDemand.setListName(InputValidationList obj, int index) {
        String listName = "listName_" + index;
        if (listName.length() > 255) {
            listName = listName.substring(0, 255);
        }
        obj.setListName(listName);
    }
    
    public InputValidationList InputValidationListDataOnDemand.getSpecificInputValidationList(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        InputValidationList obj = data.get(index);
        Long id = obj.getId();
        return InputValidationList.findInputValidationList(id);
    }
    
    public InputValidationList InputValidationListDataOnDemand.getRandomInputValidationList() {
        init();
        InputValidationList obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return InputValidationList.findInputValidationList(id);
    }
    
    public boolean InputValidationListDataOnDemand.modifyInputValidationList(InputValidationList obj) {
        return false;
    }
    
    public void InputValidationListDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = InputValidationList.findInputValidationListEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'InputValidationList' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<InputValidationList>();
        for (int i = 0; i < 10; i++) {
            InputValidationList obj = getNewTransientInputValidationList(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
