// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ValidationType;
import com.lela.data.domain.entity.ValidationTypeDataOnDemand;
import com.lela.data.domain.entity.ValidationTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ValidationTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ValidationTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ValidationTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ValidationTypeIntegrationTest: @Transactional;
    
    @Autowired
    ValidationTypeDataOnDemand ValidationTypeIntegrationTest.dod;
    
    @Test
    public void ValidationTypeIntegrationTest.testCountValidationTypes() {
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to initialize correctly", dod.getRandomValidationType());
        long count = ValidationType.countValidationTypes();
        Assert.assertTrue("Counter for 'ValidationType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ValidationTypeIntegrationTest.testFindValidationType() {
        ValidationType obj = dod.getRandomValidationType();
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to provide an identifier", id);
        obj = ValidationType.findValidationType(id);
        Assert.assertNotNull("Find method for 'ValidationType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ValidationType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ValidationTypeIntegrationTest.testFindAllValidationTypes() {
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to initialize correctly", dod.getRandomValidationType());
        long count = ValidationType.countValidationTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'ValidationType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ValidationType> result = ValidationType.findAllValidationTypes();
        Assert.assertNotNull("Find all method for 'ValidationType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ValidationType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ValidationTypeIntegrationTest.testFindValidationTypeEntries() {
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to initialize correctly", dod.getRandomValidationType());
        long count = ValidationType.countValidationTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ValidationType> result = ValidationType.findValidationTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ValidationType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ValidationType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ValidationTypeIntegrationTest.testFlush() {
        ValidationType obj = dod.getRandomValidationType();
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to provide an identifier", id);
        obj = ValidationType.findValidationType(id);
        Assert.assertNotNull("Find method for 'ValidationType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyValidationType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ValidationType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ValidationTypeIntegrationTest.testMergeUpdate() {
        ValidationType obj = dod.getRandomValidationType();
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to provide an identifier", id);
        obj = ValidationType.findValidationType(id);
        boolean modified =  dod.modifyValidationType(obj);
        Integer currentVersion = obj.getVersion();
        ValidationType merged = (ValidationType)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ValidationType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ValidationTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to initialize correctly", dod.getRandomValidationType());
        ValidationType obj = dod.getNewTransientValidationType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ValidationType' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ValidationType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ValidationTypeIntegrationTest.testRemove() {
        ValidationType obj = dod.getRandomValidationType();
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ValidationType' failed to provide an identifier", id);
        obj = ValidationType.findValidationType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ValidationType' with identifier '" + id + "'", ValidationType.findValidationType(id));
    }
    
}
