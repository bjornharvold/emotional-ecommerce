// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AttributeFormulaAttributeType;
import com.lela.data.domain.entity.AttributeFormulaAttributeTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AttributeFormulaAttributeTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AttributeFormulaAttributeTypeDataOnDemand: @Component;
    
    private Random AttributeFormulaAttributeTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<AttributeFormulaAttributeType> AttributeFormulaAttributeTypeDataOnDemand.data;
    
    public AttributeFormulaAttributeType AttributeFormulaAttributeTypeDataOnDemand.getNewTransientAttributeFormulaAttributeType(int index) {
        AttributeFormulaAttributeType obj = new AttributeFormulaAttributeType();
        setAttributeFormula(obj, index);
        setAttributeSequence(obj, index);
        setAttributeType(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setOuterJoin(obj, index);
        return obj;
    }
    
    public void AttributeFormulaAttributeTypeDataOnDemand.setAttributeSequence(AttributeFormulaAttributeType obj, int index) {
        Integer attributeSequence = new Integer(index);
        obj.setAttributeSequence(attributeSequence);
    }
    
    public void AttributeFormulaAttributeTypeDataOnDemand.setDateCreated(AttributeFormulaAttributeType obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void AttributeFormulaAttributeTypeDataOnDemand.setDateModified(AttributeFormulaAttributeType obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void AttributeFormulaAttributeTypeDataOnDemand.setOuterJoin(AttributeFormulaAttributeType obj, int index) {
        Boolean outerJoin = Boolean.TRUE;
        obj.setOuterJoin(outerJoin);
    }
    
    public AttributeFormulaAttributeType AttributeFormulaAttributeTypeDataOnDemand.getSpecificAttributeFormulaAttributeType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AttributeFormulaAttributeType obj = data.get(index);
        Long id = obj.getId();
        return AttributeFormulaAttributeType.findAttributeFormulaAttributeType(id);
    }
    
    public AttributeFormulaAttributeType AttributeFormulaAttributeTypeDataOnDemand.getRandomAttributeFormulaAttributeType() {
        init();
        AttributeFormulaAttributeType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return AttributeFormulaAttributeType.findAttributeFormulaAttributeType(id);
    }
    
    public boolean AttributeFormulaAttributeTypeDataOnDemand.modifyAttributeFormulaAttributeType(AttributeFormulaAttributeType obj) {
        return false;
    }
    
    public void AttributeFormulaAttributeTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AttributeFormulaAttributeType.findAttributeFormulaAttributeTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AttributeFormulaAttributeType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AttributeFormulaAttributeType>();
        for (int i = 0; i < 10; i++) {
            AttributeFormulaAttributeType obj = getNewTransientAttributeFormulaAttributeType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
