// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ColorPrimaryColor;
import com.lela.data.domain.entity.ColorPrimaryColorDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ColorPrimaryColorDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ColorPrimaryColorDataOnDemand: @Component;
    
    private Random ColorPrimaryColorDataOnDemand.rnd = new SecureRandom();
    
    private List<ColorPrimaryColor> ColorPrimaryColorDataOnDemand.data;
    
    public ColorPrimaryColor ColorPrimaryColorDataOnDemand.getNewTransientColorPrimaryColor(int index) {
        ColorPrimaryColor obj = new ColorPrimaryColor();
        setCategory(obj, index);
        setColor(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setPrimaryColor(obj, index);
        return obj;
    }
    
    public void ColorPrimaryColorDataOnDemand.setDateCreated(ColorPrimaryColor obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ColorPrimaryColorDataOnDemand.setDateModified(ColorPrimaryColor obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public ColorPrimaryColor ColorPrimaryColorDataOnDemand.getSpecificColorPrimaryColor(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ColorPrimaryColor obj = data.get(index);
        Long id = obj.getId();
        return ColorPrimaryColor.findColorPrimaryColor(id);
    }
    
    public ColorPrimaryColor ColorPrimaryColorDataOnDemand.getRandomColorPrimaryColor() {
        init();
        ColorPrimaryColor obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ColorPrimaryColor.findColorPrimaryColor(id);
    }
    
    public boolean ColorPrimaryColorDataOnDemand.modifyColorPrimaryColor(ColorPrimaryColor obj) {
        return false;
    }
    
    public void ColorPrimaryColorDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ColorPrimaryColor.findColorPrimaryColorEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ColorPrimaryColor' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ColorPrimaryColor>();
        for (int i = 0; i < 10; i++) {
            ColorPrimaryColor obj = getNewTransientColorPrimaryColor(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
