// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AttributeTypeCategory;
import com.lela.data.domain.entity.AttributeTypeCategoryDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AttributeTypeCategoryDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AttributeTypeCategoryDataOnDemand: @Component;
    
    private Random AttributeTypeCategoryDataOnDemand.rnd = new SecureRandom();
    
    private List<AttributeTypeCategory> AttributeTypeCategoryDataOnDemand.data;
    
    public AttributeTypeCategory AttributeTypeCategoryDataOnDemand.getNewTransientAttributeTypeCategory(int index) {
        AttributeTypeCategory obj = new AttributeTypeCategory();
        setAttributeType(obj, index);
        setCategory(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setDefaultValue(obj, index);
        setInputType(obj, index);
        setInputValidationList(obj, index);
        setSuppressUnknown(obj, index);
        return obj;
    }
    
    public void AttributeTypeCategoryDataOnDemand.setDateCreated(AttributeTypeCategory obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void AttributeTypeCategoryDataOnDemand.setDateModified(AttributeTypeCategory obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void AttributeTypeCategoryDataOnDemand.setDefaultValue(AttributeTypeCategory obj, int index) {
        String defaultValue = "defaultValue_" + index;
        obj.setDefaultValue(defaultValue);
    }
    
    public void AttributeTypeCategoryDataOnDemand.setSuppressUnknown(AttributeTypeCategory obj, int index) {
        Boolean suppressUnknown = Boolean.TRUE;
        obj.setSuppressUnknown(suppressUnknown);
    }
    
    public AttributeTypeCategory AttributeTypeCategoryDataOnDemand.getSpecificAttributeTypeCategory(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AttributeTypeCategory obj = data.get(index);
        Long id = obj.getId();
        return AttributeTypeCategory.findAttributeTypeCategory(id);
    }
    
    public AttributeTypeCategory AttributeTypeCategoryDataOnDemand.getRandomAttributeTypeCategory() {
        init();
        AttributeTypeCategory obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return AttributeTypeCategory.findAttributeTypeCategory(id);
    }
    
    public boolean AttributeTypeCategoryDataOnDemand.modifyAttributeTypeCategory(AttributeTypeCategory obj) {
        return false;
    }
    
    public void AttributeTypeCategoryDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AttributeTypeCategory.findAttributeTypeCategoryEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AttributeTypeCategory' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AttributeTypeCategory>();
        for (int i = 0; i < 10; i++) {
            AttributeTypeCategory obj = getNewTransientAttributeTypeCategory(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
