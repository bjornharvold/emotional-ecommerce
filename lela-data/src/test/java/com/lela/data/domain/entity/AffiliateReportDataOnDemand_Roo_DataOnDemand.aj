// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AffiliateReport;
import com.lela.data.domain.entity.AffiliateReportDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AffiliateReportDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AffiliateReportDataOnDemand: @Component;
    
    private Random AffiliateReportDataOnDemand.rnd = new SecureRandom();
    
    private List<AffiliateReport> AffiliateReportDataOnDemand.data;
    
    public AffiliateReport AffiliateReportDataOnDemand.getNewTransientAffiliateReport(int index) {
        AffiliateReport obj = new AffiliateReport();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setFileName(obj, index);
        setFilePath(obj, index);
        setReceivedDate(obj, index);
        return obj;
    }
    
    public void AffiliateReportDataOnDemand.setDateCreated(AffiliateReport obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void AffiliateReportDataOnDemand.setDateModified(AffiliateReport obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void AffiliateReportDataOnDemand.setFileName(AffiliateReport obj, int index) {
        String fileName = "fileName_" + index;
        obj.setFileName(fileName);
    }
    
    public void AffiliateReportDataOnDemand.setFilePath(AffiliateReport obj, int index) {
        String filePath = "filePath_" + index;
        obj.setFilePath(filePath);
    }
    
    public void AffiliateReportDataOnDemand.setReceivedDate(AffiliateReport obj, int index) {
        Date receivedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setReceivedDate(receivedDate);
    }
    
    public AffiliateReport AffiliateReportDataOnDemand.getSpecificAffiliateReport(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AffiliateReport obj = data.get(index);
        Long id = obj.getId();
        return AffiliateReport.findAffiliateReport(id);
    }
    
    public AffiliateReport AffiliateReportDataOnDemand.getRandomAffiliateReport() {
        init();
        AffiliateReport obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return AffiliateReport.findAffiliateReport(id);
    }
    
    public boolean AffiliateReportDataOnDemand.modifyAffiliateReport(AffiliateReport obj) {
        return false;
    }
    
    public void AffiliateReportDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AffiliateReport.findAffiliateReportEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AffiliateReport' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AffiliateReport>();
        for (int i = 0; i < 10; i++) {
            AffiliateReport obj = getNewTransientAffiliateReport(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
