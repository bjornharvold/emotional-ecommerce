// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryLoadRule;
import com.lela.data.domain.entity.CategoryLoadRuleDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect CategoryLoadRuleDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CategoryLoadRuleDataOnDemand: @Component;
    
    private Random CategoryLoadRuleDataOnDemand.rnd = new SecureRandom();
    
    private List<CategoryLoadRule> CategoryLoadRuleDataOnDemand.data;
    
    public CategoryLoadRule CategoryLoadRuleDataOnDemand.getNewTransientCategoryLoadRule(int index) {
        CategoryLoadRule obj = new CategoryLoadRule();
        setAttributeType(obj, index);
        setAttributeValue(obj, index);
        setCategory(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setLoadFlag(obj, index);
        return obj;
    }
    
    public void CategoryLoadRuleDataOnDemand.setAttributeValue(CategoryLoadRule obj, int index) {
        String attributeValue = "attributeValue_" + index;
        obj.setAttributeValue(attributeValue);
    }
    
    public void CategoryLoadRuleDataOnDemand.setDateCreated(CategoryLoadRule obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void CategoryLoadRuleDataOnDemand.setDateModified(CategoryLoadRule obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void CategoryLoadRuleDataOnDemand.setLoadFlag(CategoryLoadRule obj, int index) {
        Boolean loadFlag = Boolean.TRUE;
        obj.setLoadFlag(loadFlag);
    }
    
    public CategoryLoadRule CategoryLoadRuleDataOnDemand.getSpecificCategoryLoadRule(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CategoryLoadRule obj = data.get(index);
        Long id = obj.getId();
        return CategoryLoadRule.findCategoryLoadRule(id);
    }
    
    public CategoryLoadRule CategoryLoadRuleDataOnDemand.getRandomCategoryLoadRule() {
        init();
        CategoryLoadRule obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CategoryLoadRule.findCategoryLoadRule(id);
    }
    
    public boolean CategoryLoadRuleDataOnDemand.modifyCategoryLoadRule(CategoryLoadRule obj) {
        return false;
    }
    
    public void CategoryLoadRuleDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CategoryLoadRule.findCategoryLoadRuleEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CategoryLoadRule' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CategoryLoadRule>();
        for (int i = 0; i < 10; i++) {
            CategoryLoadRule obj = getNewTransientCategoryLoadRule(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
