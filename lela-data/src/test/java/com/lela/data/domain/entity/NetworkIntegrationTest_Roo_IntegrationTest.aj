// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Network;
import com.lela.data.domain.entity.NetworkDataOnDemand;
import com.lela.data.domain.entity.NetworkIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect NetworkIntegrationTest_Roo_IntegrationTest {
    
    declare @type: NetworkIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: NetworkIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: NetworkIntegrationTest: @Transactional;
    
    @Autowired
    NetworkDataOnDemand NetworkIntegrationTest.dod;
    
    @Test
    public void NetworkIntegrationTest.testCountNetworks() {
        Assert.assertNotNull("Data on demand for 'Network' failed to initialize correctly", dod.getRandomNetwork());
        long count = Network.countNetworks();
        Assert.assertTrue("Counter for 'Network' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void NetworkIntegrationTest.testFindNetwork() {
        Network obj = dod.getRandomNetwork();
        Assert.assertNotNull("Data on demand for 'Network' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Network' failed to provide an identifier", id);
        obj = Network.findNetwork(id);
        Assert.assertNotNull("Find method for 'Network' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Network' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void NetworkIntegrationTest.testFindAllNetworks() {
        Assert.assertNotNull("Data on demand for 'Network' failed to initialize correctly", dod.getRandomNetwork());
        long count = Network.countNetworks();
        Assert.assertTrue("Too expensive to perform a find all test for 'Network', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Network> result = Network.findAllNetworks();
        Assert.assertNotNull("Find all method for 'Network' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Network' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void NetworkIntegrationTest.testFindNetworkEntries() {
        Assert.assertNotNull("Data on demand for 'Network' failed to initialize correctly", dod.getRandomNetwork());
        long count = Network.countNetworks();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Network> result = Network.findNetworkEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Network' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Network' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void NetworkIntegrationTest.testFlush() {
        Network obj = dod.getRandomNetwork();
        Assert.assertNotNull("Data on demand for 'Network' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Network' failed to provide an identifier", id);
        obj = Network.findNetwork(id);
        Assert.assertNotNull("Find method for 'Network' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyNetwork(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Network' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void NetworkIntegrationTest.testMergeUpdate() {
        Network obj = dod.getRandomNetwork();
        Assert.assertNotNull("Data on demand for 'Network' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Network' failed to provide an identifier", id);
        obj = Network.findNetwork(id);
        boolean modified =  dod.modifyNetwork(obj);
        Integer currentVersion = obj.getVersion();
        Network merged = (Network)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Network' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void NetworkIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Network' failed to initialize correctly", dod.getRandomNetwork());
        Network obj = dod.getNewTransientNetwork(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Network' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Network' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Network' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void NetworkIntegrationTest.testRemove() {
        Network obj = dod.getRandomNetwork();
        Assert.assertNotNull("Data on demand for 'Network' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Network' failed to provide an identifier", id);
        obj = Network.findNetwork(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Network' with identifier '" + id + "'", Network.findNetwork(id));
    }
    
}
