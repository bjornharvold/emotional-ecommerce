// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.InputType;
import com.lela.data.domain.entity.InputTypeDataOnDemand;
import com.lela.data.domain.entity.InputTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect InputTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: InputTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: InputTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: InputTypeIntegrationTest: @Transactional;
    
    @Autowired
    InputTypeDataOnDemand InputTypeIntegrationTest.dod;
    
    @Test
    public void InputTypeIntegrationTest.testCountInputTypes() {
        Assert.assertNotNull("Data on demand for 'InputType' failed to initialize correctly", dod.getRandomInputType());
        long count = InputType.countInputTypes();
        Assert.assertTrue("Counter for 'InputType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void InputTypeIntegrationTest.testFindInputType() {
        InputType obj = dod.getRandomInputType();
        Assert.assertNotNull("Data on demand for 'InputType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'InputType' failed to provide an identifier", id);
        obj = InputType.findInputType(id);
        Assert.assertNotNull("Find method for 'InputType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'InputType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void InputTypeIntegrationTest.testFindAllInputTypes() {
        Assert.assertNotNull("Data on demand for 'InputType' failed to initialize correctly", dod.getRandomInputType());
        long count = InputType.countInputTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'InputType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<InputType> result = InputType.findAllInputTypes();
        Assert.assertNotNull("Find all method for 'InputType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'InputType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void InputTypeIntegrationTest.testFindInputTypeEntries() {
        Assert.assertNotNull("Data on demand for 'InputType' failed to initialize correctly", dod.getRandomInputType());
        long count = InputType.countInputTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<InputType> result = InputType.findInputTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'InputType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'InputType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void InputTypeIntegrationTest.testFlush() {
        InputType obj = dod.getRandomInputType();
        Assert.assertNotNull("Data on demand for 'InputType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'InputType' failed to provide an identifier", id);
        obj = InputType.findInputType(id);
        Assert.assertNotNull("Find method for 'InputType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyInputType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'InputType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void InputTypeIntegrationTest.testMergeUpdate() {
        InputType obj = dod.getRandomInputType();
        Assert.assertNotNull("Data on demand for 'InputType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'InputType' failed to provide an identifier", id);
        obj = InputType.findInputType(id);
        boolean modified =  dod.modifyInputType(obj);
        Integer currentVersion = obj.getVersion();
        InputType merged = (InputType)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'InputType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void InputTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'InputType' failed to initialize correctly", dod.getRandomInputType());
        InputType obj = dod.getNewTransientInputType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'InputType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'InputType' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'InputType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void InputTypeIntegrationTest.testRemove() {
        InputType obj = dod.getRandomInputType();
        Assert.assertNotNull("Data on demand for 'InputType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'InputType' failed to provide an identifier", id);
        obj = InputType.findInputType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'InputType' with identifier '" + id + "'", InputType.findInputType(id));
    }
    
}
