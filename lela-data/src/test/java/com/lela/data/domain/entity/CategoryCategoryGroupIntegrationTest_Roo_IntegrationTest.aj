// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryCategoryGroup;
import com.lela.data.domain.entity.CategoryCategoryGroupDataOnDemand;
import com.lela.data.domain.entity.CategoryCategoryGroupIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoryCategoryGroupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoryCategoryGroupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoryCategoryGroupIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoryCategoryGroupIntegrationTest: @Transactional;
    
    @Autowired
    CategoryCategoryGroupDataOnDemand CategoryCategoryGroupIntegrationTest.dod;
    
    @Test
    public void CategoryCategoryGroupIntegrationTest.testCountCategoryCategoryGroups() {
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to initialize correctly", dod.getRandomCategoryCategoryGroup());
        long count = CategoryCategoryGroup.countCategoryCategoryGroups();
        Assert.assertTrue("Counter for 'CategoryCategoryGroup' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoryCategoryGroupIntegrationTest.testFindCategoryCategoryGroup() {
        CategoryCategoryGroup obj = dod.getRandomCategoryCategoryGroup();
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to provide an identifier", id);
        obj = CategoryCategoryGroup.findCategoryCategoryGroup(id);
        Assert.assertNotNull("Find method for 'CategoryCategoryGroup' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategoryCategoryGroup' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoryCategoryGroupIntegrationTest.testFindAllCategoryCategoryGroups() {
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to initialize correctly", dod.getRandomCategoryCategoryGroup());
        long count = CategoryCategoryGroup.countCategoryCategoryGroups();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategoryCategoryGroup', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategoryCategoryGroup> result = CategoryCategoryGroup.findAllCategoryCategoryGroups();
        Assert.assertNotNull("Find all method for 'CategoryCategoryGroup' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategoryCategoryGroup' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoryCategoryGroupIntegrationTest.testFindCategoryCategoryGroupEntries() {
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to initialize correctly", dod.getRandomCategoryCategoryGroup());
        long count = CategoryCategoryGroup.countCategoryCategoryGroups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategoryCategoryGroup> result = CategoryCategoryGroup.findCategoryCategoryGroupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategoryCategoryGroup' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategoryCategoryGroup' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoryCategoryGroupIntegrationTest.testFlush() {
        CategoryCategoryGroup obj = dod.getRandomCategoryCategoryGroup();
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to provide an identifier", id);
        obj = CategoryCategoryGroup.findCategoryCategoryGroup(id);
        Assert.assertNotNull("Find method for 'CategoryCategoryGroup' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategoryCategoryGroup(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategoryCategoryGroup' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryCategoryGroupIntegrationTest.testMergeUpdate() {
        CategoryCategoryGroup obj = dod.getRandomCategoryCategoryGroup();
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to provide an identifier", id);
        obj = CategoryCategoryGroup.findCategoryCategoryGroup(id);
        boolean modified =  dod.modifyCategoryCategoryGroup(obj);
        Integer currentVersion = obj.getVersion();
        CategoryCategoryGroup merged = (CategoryCategoryGroup)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategoryCategoryGroup' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryCategoryGroupIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to initialize correctly", dod.getRandomCategoryCategoryGroup());
        CategoryCategoryGroup obj = dod.getNewTransientCategoryCategoryGroup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CategoryCategoryGroup' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'CategoryCategoryGroup' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CategoryCategoryGroupIntegrationTest.testRemove() {
        CategoryCategoryGroup obj = dod.getRandomCategoryCategoryGroup();
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryCategoryGroup' failed to provide an identifier", id);
        obj = CategoryCategoryGroup.findCategoryCategoryGroup(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategoryCategoryGroup' with identifier '" + id + "'", CategoryCategoryGroup.findCategoryCategoryGroup(id));
    }
    
}
