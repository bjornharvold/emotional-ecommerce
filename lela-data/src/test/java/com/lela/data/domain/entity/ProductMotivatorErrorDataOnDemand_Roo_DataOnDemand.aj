// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductMotivatorError;
import com.lela.data.domain.entity.ProductMotivatorErrorDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ProductMotivatorErrorDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProductMotivatorErrorDataOnDemand: @Component;
    
    private Random ProductMotivatorErrorDataOnDemand.rnd = new SecureRandom();
    
    private List<ProductMotivatorError> ProductMotivatorErrorDataOnDemand.data;
    
    public ProductMotivatorError ProductMotivatorErrorDataOnDemand.getNewTransientProductMotivatorError(int index) {
        ProductMotivatorError obj = new ProductMotivatorError();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setError(obj, index);
        setItem(obj, index);
        setMotivator(obj, index);
        setStackTrace(obj, index);
        return obj;
    }
    
    public void ProductMotivatorErrorDataOnDemand.setDateCreated(ProductMotivatorError obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ProductMotivatorErrorDataOnDemand.setDateModified(ProductMotivatorError obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ProductMotivatorErrorDataOnDemand.setError(ProductMotivatorError obj, int index) {
        String error = "error_" + index;
        obj.setError(error);
    }
    
    public void ProductMotivatorErrorDataOnDemand.setStackTrace(ProductMotivatorError obj, int index) {
        String stackTrace = "stackTrace_" + index;
        obj.setStackTrace(stackTrace);
    }
    
    public ProductMotivatorError ProductMotivatorErrorDataOnDemand.getSpecificProductMotivatorError(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ProductMotivatorError obj = data.get(index);
        Long id = obj.getId();
        return ProductMotivatorError.findProductMotivatorError(id);
    }
    
    public ProductMotivatorError ProductMotivatorErrorDataOnDemand.getRandomProductMotivatorError() {
        init();
        ProductMotivatorError obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ProductMotivatorError.findProductMotivatorError(id);
    }
    
    public boolean ProductMotivatorErrorDataOnDemand.modifyProductMotivatorError(ProductMotivatorError obj) {
        return false;
    }
    
    public void ProductMotivatorErrorDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ProductMotivatorError.findProductMotivatorErrorEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ProductMotivatorError' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ProductMotivatorError>();
        for (int i = 0; i < 10; i++) {
            ProductMotivatorError obj = getNewTransientProductMotivatorError(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
