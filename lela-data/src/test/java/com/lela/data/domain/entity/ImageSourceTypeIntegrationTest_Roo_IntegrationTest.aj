// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ImageSourceType;
import com.lela.data.domain.entity.ImageSourceTypeDataOnDemand;
import com.lela.data.domain.entity.ImageSourceTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ImageSourceTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ImageSourceTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ImageSourceTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ImageSourceTypeIntegrationTest: @Transactional;
    
    @Autowired
    ImageSourceTypeDataOnDemand ImageSourceTypeIntegrationTest.dod;
    
    @Test
    public void ImageSourceTypeIntegrationTest.testCountImageSourceTypes() {
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to initialize correctly", dod.getRandomImageSourceType());
        long count = ImageSourceType.countImageSourceTypes();
        Assert.assertTrue("Counter for 'ImageSourceType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ImageSourceTypeIntegrationTest.testFindImageSourceType() {
        ImageSourceType obj = dod.getRandomImageSourceType();
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to provide an identifier", id);
        obj = ImageSourceType.findImageSourceType(id);
        Assert.assertNotNull("Find method for 'ImageSourceType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ImageSourceType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ImageSourceTypeIntegrationTest.testFindAllImageSourceTypes() {
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to initialize correctly", dod.getRandomImageSourceType());
        long count = ImageSourceType.countImageSourceTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'ImageSourceType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ImageSourceType> result = ImageSourceType.findAllImageSourceTypes();
        Assert.assertNotNull("Find all method for 'ImageSourceType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ImageSourceType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ImageSourceTypeIntegrationTest.testFindImageSourceTypeEntries() {
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to initialize correctly", dod.getRandomImageSourceType());
        long count = ImageSourceType.countImageSourceTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ImageSourceType> result = ImageSourceType.findImageSourceTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ImageSourceType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ImageSourceType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ImageSourceTypeIntegrationTest.testFlush() {
        ImageSourceType obj = dod.getRandomImageSourceType();
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to provide an identifier", id);
        obj = ImageSourceType.findImageSourceType(id);
        Assert.assertNotNull("Find method for 'ImageSourceType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyImageSourceType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ImageSourceType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ImageSourceTypeIntegrationTest.testMergeUpdate() {
        ImageSourceType obj = dod.getRandomImageSourceType();
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to provide an identifier", id);
        obj = ImageSourceType.findImageSourceType(id);
        boolean modified =  dod.modifyImageSourceType(obj);
        Integer currentVersion = obj.getVersion();
        ImageSourceType merged = (ImageSourceType)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ImageSourceType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ImageSourceTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to initialize correctly", dod.getRandomImageSourceType());
        ImageSourceType obj = dod.getNewTransientImageSourceType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ImageSourceType' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ImageSourceType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ImageSourceTypeIntegrationTest.testRemove() {
        ImageSourceType obj = dod.getRandomImageSourceType();
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ImageSourceType' failed to provide an identifier", id);
        obj = ImageSourceType.findImageSourceType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ImageSourceType' with identifier '" + id + "'", ImageSourceType.findImageSourceType(id));
    }
    
}
