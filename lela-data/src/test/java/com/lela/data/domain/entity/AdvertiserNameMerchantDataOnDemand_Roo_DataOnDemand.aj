// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.AdvertiserNameMerchant;
import com.lela.data.domain.entity.AdvertiserNameMerchantDataOnDemand;
import com.lela.data.domain.entity.MerchantDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect AdvertiserNameMerchantDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AdvertiserNameMerchantDataOnDemand: @Component;
    
    private Random AdvertiserNameMerchantDataOnDemand.rnd = new SecureRandom();
    
    private List<AdvertiserNameMerchant> AdvertiserNameMerchantDataOnDemand.data;
    
    @Autowired
    MerchantDataOnDemand AdvertiserNameMerchantDataOnDemand.merchantDataOnDemand;
    
    public AdvertiserNameMerchant AdvertiserNameMerchantDataOnDemand.getNewTransientAdvertiserNameMerchant(int index) {
        AdvertiserNameMerchant obj = new AdvertiserNameMerchant();
        setAdvertiserName(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        return obj;
    }
    
    public void AdvertiserNameMerchantDataOnDemand.setAdvertiserName(AdvertiserNameMerchant obj, int index) {
        String advertiserName = "advertiserName_" + index;
        obj.setAdvertiserName(advertiserName);
    }
    
    public void AdvertiserNameMerchantDataOnDemand.setDateCreated(AdvertiserNameMerchant obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void AdvertiserNameMerchantDataOnDemand.setDateModified(AdvertiserNameMerchant obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public AdvertiserNameMerchant AdvertiserNameMerchantDataOnDemand.getSpecificAdvertiserNameMerchant(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AdvertiserNameMerchant obj = data.get(index);
        Long id = obj.getId();
        return AdvertiserNameMerchant.findAdvertiserNameMerchant(id);
    }
    
    public AdvertiserNameMerchant AdvertiserNameMerchantDataOnDemand.getRandomAdvertiserNameMerchant() {
        init();
        AdvertiserNameMerchant obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return AdvertiserNameMerchant.findAdvertiserNameMerchant(id);
    }
    
    public boolean AdvertiserNameMerchantDataOnDemand.modifyAdvertiserNameMerchant(AdvertiserNameMerchant obj) {
        return false;
    }
    
    public void AdvertiserNameMerchantDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AdvertiserNameMerchant.findAdvertiserNameMerchantEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AdvertiserNameMerchant' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AdvertiserNameMerchant>();
        for (int i = 0; i < 10; i++) {
            AdvertiserNameMerchant obj = getNewTransientAdvertiserNameMerchant(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
