// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CleanseRule;
import com.lela.data.domain.entity.CleanseRuleDataOnDemand;
import com.lela.data.domain.entity.CleanseRuleIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CleanseRuleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CleanseRuleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CleanseRuleIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CleanseRuleIntegrationTest: @Transactional;
    
    @Autowired
    CleanseRuleDataOnDemand CleanseRuleIntegrationTest.dod;
    
    @Test
    public void CleanseRuleIntegrationTest.testCountCleanseRules() {
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to initialize correctly", dod.getRandomCleanseRule());
        long count = CleanseRule.countCleanseRules();
        Assert.assertTrue("Counter for 'CleanseRule' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CleanseRuleIntegrationTest.testFindCleanseRule() {
        CleanseRule obj = dod.getRandomCleanseRule();
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to provide an identifier", id);
        obj = CleanseRule.findCleanseRule(id);
        Assert.assertNotNull("Find method for 'CleanseRule' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CleanseRule' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CleanseRuleIntegrationTest.testFindAllCleanseRules() {
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to initialize correctly", dod.getRandomCleanseRule());
        long count = CleanseRule.countCleanseRules();
        Assert.assertTrue("Too expensive to perform a find all test for 'CleanseRule', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CleanseRule> result = CleanseRule.findAllCleanseRules();
        Assert.assertNotNull("Find all method for 'CleanseRule' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CleanseRule' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CleanseRuleIntegrationTest.testFindCleanseRuleEntries() {
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to initialize correctly", dod.getRandomCleanseRule());
        long count = CleanseRule.countCleanseRules();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CleanseRule> result = CleanseRule.findCleanseRuleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CleanseRule' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CleanseRule' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CleanseRuleIntegrationTest.testFlush() {
        CleanseRule obj = dod.getRandomCleanseRule();
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to provide an identifier", id);
        obj = CleanseRule.findCleanseRule(id);
        Assert.assertNotNull("Find method for 'CleanseRule' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCleanseRule(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CleanseRule' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CleanseRuleIntegrationTest.testMergeUpdate() {
        CleanseRule obj = dod.getRandomCleanseRule();
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to provide an identifier", id);
        obj = CleanseRule.findCleanseRule(id);
        boolean modified =  dod.modifyCleanseRule(obj);
        Integer currentVersion = obj.getVersion();
        CleanseRule merged = (CleanseRule)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CleanseRule' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CleanseRuleIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to initialize correctly", dod.getRandomCleanseRule());
        CleanseRule obj = dod.getNewTransientCleanseRule(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CleanseRule' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'CleanseRule' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CleanseRuleIntegrationTest.testRemove() {
        CleanseRule obj = dod.getRandomCleanseRule();
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CleanseRule' failed to provide an identifier", id);
        obj = CleanseRule.findCleanseRule(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CleanseRule' with identifier '" + id + "'", CleanseRule.findCleanseRule(id));
    }
    
}
