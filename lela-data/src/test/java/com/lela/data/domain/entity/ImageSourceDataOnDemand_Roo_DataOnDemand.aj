// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ImageSource;
import com.lela.data.domain.entity.ImageSourceDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ImageSourceDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ImageSourceDataOnDemand: @Component;
    
    private Random ImageSourceDataOnDemand.rnd = new SecureRandom();
    
    private List<ImageSource> ImageSourceDataOnDemand.data;
    
    public ImageSource ImageSourceDataOnDemand.getNewTransientImageSource(int index) {
        ImageSource obj = new ImageSource();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setImageSourceType(obj, index);
        setSourceName(obj, index);
        return obj;
    }
    
    public void ImageSourceDataOnDemand.setDateCreated(ImageSource obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ImageSourceDataOnDemand.setDateModified(ImageSource obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ImageSourceDataOnDemand.setSourceName(ImageSource obj, int index) {
        String sourceName = "sourceName_" + index;
        obj.setSourceName(sourceName);
    }
    
    public ImageSource ImageSourceDataOnDemand.getSpecificImageSource(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ImageSource obj = data.get(index);
        Long id = obj.getId();
        return ImageSource.findImageSource(id);
    }
    
    public ImageSource ImageSourceDataOnDemand.getRandomImageSource() {
        init();
        ImageSource obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ImageSource.findImageSource(id);
    }
    
    public boolean ImageSourceDataOnDemand.modifyImageSource(ImageSource obj) {
        return false;
    }
    
    public void ImageSourceDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ImageSource.findImageSourceEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ImageSource' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ImageSource>();
        for (int i = 0; i < 10; i++) {
            ImageSource obj = getNewTransientImageSource(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
