// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductImageItemStatus;
import com.lela.data.domain.entity.ProductImageItemStatusDataOnDemand;
import com.lela.data.domain.entity.ProductImageItemStatusIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductImageItemStatusIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductImageItemStatusIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductImageItemStatusIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductImageItemStatusIntegrationTest: @Transactional;
    
    @Autowired
    ProductImageItemStatusDataOnDemand ProductImageItemStatusIntegrationTest.dod;
    
    @Test
    public void ProductImageItemStatusIntegrationTest.testCountProductImageItemStatuses() {
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to initialize correctly", dod.getRandomProductImageItemStatus());
        long count = ProductImageItemStatus.countProductImageItemStatuses();
        Assert.assertTrue("Counter for 'ProductImageItemStatus' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductImageItemStatusIntegrationTest.testFindProductImageItemStatus() {
        ProductImageItemStatus obj = dod.getRandomProductImageItemStatus();
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to provide an identifier", id);
        obj = ProductImageItemStatus.findProductImageItemStatus(id);
        Assert.assertNotNull("Find method for 'ProductImageItemStatus' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductImageItemStatus' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductImageItemStatusIntegrationTest.testFindAllProductImageItemStatuses() {
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to initialize correctly", dod.getRandomProductImageItemStatus());
        long count = ProductImageItemStatus.countProductImageItemStatuses();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductImageItemStatus', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductImageItemStatus> result = ProductImageItemStatus.findAllProductImageItemStatuses();
        Assert.assertNotNull("Find all method for 'ProductImageItemStatus' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductImageItemStatus' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductImageItemStatusIntegrationTest.testFindProductImageItemStatusEntries() {
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to initialize correctly", dod.getRandomProductImageItemStatus());
        long count = ProductImageItemStatus.countProductImageItemStatuses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductImageItemStatus> result = ProductImageItemStatus.findProductImageItemStatusEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductImageItemStatus' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductImageItemStatus' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductImageItemStatusIntegrationTest.testFlush() {
        ProductImageItemStatus obj = dod.getRandomProductImageItemStatus();
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to provide an identifier", id);
        obj = ProductImageItemStatus.findProductImageItemStatus(id);
        Assert.assertNotNull("Find method for 'ProductImageItemStatus' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProductImageItemStatus(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProductImageItemStatus' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductImageItemStatusIntegrationTest.testMergeUpdate() {
        ProductImageItemStatus obj = dod.getRandomProductImageItemStatus();
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to provide an identifier", id);
        obj = ProductImageItemStatus.findProductImageItemStatus(id);
        boolean modified =  dod.modifyProductImageItemStatus(obj);
        Integer currentVersion = obj.getVersion();
        ProductImageItemStatus merged = (ProductImageItemStatus)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProductImageItemStatus' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductImageItemStatusIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to initialize correctly", dod.getRandomProductImageItemStatus());
        ProductImageItemStatus obj = dod.getNewTransientProductImageItemStatus(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductImageItemStatus' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProductImageItemStatus' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductImageItemStatus' identifier to no longer be null", obj.getId());
    }
    
}
