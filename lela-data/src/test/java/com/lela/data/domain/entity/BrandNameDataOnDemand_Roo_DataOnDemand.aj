// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.BrandName;
import com.lela.data.domain.entity.BrandNameDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect BrandNameDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BrandNameDataOnDemand: @Component;
    
    private Random BrandNameDataOnDemand.rnd = new SecureRandom();
    
    private List<BrandName> BrandNameDataOnDemand.data;
    
    public BrandName BrandNameDataOnDemand.getNewTransientBrandName(int index) {
        BrandName obj = new BrandName();
        setBrand(obj, index);
        setBrandName(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setLocale(obj, index);
        setMainName(obj, index);
        return obj;
    }
    
    public void BrandNameDataOnDemand.setBrandName(BrandName obj, int index) {
        String brandName = "brandName_" + index;
        obj.setBrandName(brandName);
    }
    
    public void BrandNameDataOnDemand.setDateCreated(BrandName obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void BrandNameDataOnDemand.setDateModified(BrandName obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void BrandNameDataOnDemand.setMainName(BrandName obj, int index) {
        Integer mainName = new Integer(index);
        obj.setMainName(mainName);
    }
    
    public BrandName BrandNameDataOnDemand.getSpecificBrandName(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        BrandName obj = data.get(index);
        Long id = obj.getId();
        return BrandName.findBrandName(id);
    }
    
    public BrandName BrandNameDataOnDemand.getRandomBrandName() {
        init();
        BrandName obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return BrandName.findBrandName(id);
    }
    
    public boolean BrandNameDataOnDemand.modifyBrandName(BrandName obj) {
        return false;
    }
    
    public void BrandNameDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = BrandName.findBrandNameEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'BrandName' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<BrandName>();
        for (int i = 0; i < 10; i++) {
            BrandName obj = getNewTransientBrandName(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
