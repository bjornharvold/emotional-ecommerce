// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ColorPrimaryColor;
import com.lela.data.domain.entity.ColorPrimaryColorDataOnDemand;
import com.lela.data.domain.entity.ColorPrimaryColorIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ColorPrimaryColorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ColorPrimaryColorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ColorPrimaryColorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ColorPrimaryColorIntegrationTest: @Transactional;
    
    @Autowired
    ColorPrimaryColorDataOnDemand ColorPrimaryColorIntegrationTest.dod;
    
    @Test
    public void ColorPrimaryColorIntegrationTest.testCountColorPrimaryColors() {
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to initialize correctly", dod.getRandomColorPrimaryColor());
        long count = ColorPrimaryColor.countColorPrimaryColors();
        Assert.assertTrue("Counter for 'ColorPrimaryColor' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ColorPrimaryColorIntegrationTest.testFindColorPrimaryColor() {
        ColorPrimaryColor obj = dod.getRandomColorPrimaryColor();
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to provide an identifier", id);
        obj = ColorPrimaryColor.findColorPrimaryColor(id);
        Assert.assertNotNull("Find method for 'ColorPrimaryColor' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ColorPrimaryColor' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ColorPrimaryColorIntegrationTest.testFindAllColorPrimaryColors() {
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to initialize correctly", dod.getRandomColorPrimaryColor());
        long count = ColorPrimaryColor.countColorPrimaryColors();
        Assert.assertTrue("Too expensive to perform a find all test for 'ColorPrimaryColor', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ColorPrimaryColor> result = ColorPrimaryColor.findAllColorPrimaryColors();
        Assert.assertNotNull("Find all method for 'ColorPrimaryColor' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ColorPrimaryColor' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ColorPrimaryColorIntegrationTest.testFindColorPrimaryColorEntries() {
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to initialize correctly", dod.getRandomColorPrimaryColor());
        long count = ColorPrimaryColor.countColorPrimaryColors();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ColorPrimaryColor> result = ColorPrimaryColor.findColorPrimaryColorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ColorPrimaryColor' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ColorPrimaryColor' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ColorPrimaryColorIntegrationTest.testFlush() {
        ColorPrimaryColor obj = dod.getRandomColorPrimaryColor();
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to provide an identifier", id);
        obj = ColorPrimaryColor.findColorPrimaryColor(id);
        Assert.assertNotNull("Find method for 'ColorPrimaryColor' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyColorPrimaryColor(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ColorPrimaryColor' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ColorPrimaryColorIntegrationTest.testMergeUpdate() {
        ColorPrimaryColor obj = dod.getRandomColorPrimaryColor();
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to provide an identifier", id);
        obj = ColorPrimaryColor.findColorPrimaryColor(id);
        boolean modified =  dod.modifyColorPrimaryColor(obj);
        Integer currentVersion = obj.getVersion();
        ColorPrimaryColor merged = (ColorPrimaryColor)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ColorPrimaryColor' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ColorPrimaryColorIntegrationTest.testRemove() {
        ColorPrimaryColor obj = dod.getRandomColorPrimaryColor();
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ColorPrimaryColor' failed to provide an identifier", id);
        obj = ColorPrimaryColor.findColorPrimaryColor(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ColorPrimaryColor' with identifier '" + id + "'", ColorPrimaryColor.findColorPrimaryColor(id));
    }
    
}
