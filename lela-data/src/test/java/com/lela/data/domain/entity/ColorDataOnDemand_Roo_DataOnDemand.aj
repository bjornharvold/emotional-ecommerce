// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Color;
import com.lela.data.domain.entity.ColorDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ColorDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ColorDataOnDemand: @Component;
    
    private Random ColorDataOnDemand.rnd = new SecureRandom();
    
    private List<Color> ColorDataOnDemand.data;
    
    public Color ColorDataOnDemand.getNewTransientColor(int index) {
        Color obj = new Color();
        setColorName(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        return obj;
    }
    
    public void ColorDataOnDemand.setColorName(Color obj, int index) {
        String colorName = "colorName_" + index;
        if (colorName.length() > 255) {
            colorName = colorName.substring(0, 255);
        }
        obj.setColorName(colorName);
    }
    
    public void ColorDataOnDemand.setDateCreated(Color obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ColorDataOnDemand.setDateModified(Color obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public Color ColorDataOnDemand.getSpecificColor(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Color obj = data.get(index);
        Long id = obj.getId();
        return Color.findColor(id);
    }
    
    public Color ColorDataOnDemand.getRandomColor() {
        init();
        Color obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Color.findColor(id);
    }
    
    public boolean ColorDataOnDemand.modifyColor(Color obj) {
        return false;
    }
    
    public void ColorDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Color.findColorEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Color' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Color>();
        for (int i = 0; i < 10; i++) {
            Color obj = getNewTransientColor(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
