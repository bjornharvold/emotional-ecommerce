// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Brand;
import com.lela.data.domain.entity.BrandDataOnDemand;
import com.lela.data.domain.entity.Manufacturer;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect BrandDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BrandDataOnDemand: @Component;
    
    private Random BrandDataOnDemand.rnd = new SecureRandom();
    
    private List<Brand> BrandDataOnDemand.data;
    
    public Brand BrandDataOnDemand.getNewTransientBrand(int index) {
        Brand obj = new Brand();
        setBrandName(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setFacebookAccess(obj, index);
        setFacebookLikes(obj, index);
        setFacebookUrl(obj, index);
        setIsDirty(obj, index);
        setLelaUrl(obj, index);
        setManufacturer(obj, index);
        setManufacturerName(obj, index);
        setObjectId(obj, index);
        setPopshopsBrandId(obj, index);
        setPopshopsSuccess(obj, index);
        setTwitterFollowers(obj, index);
        setTwitterName(obj, index);
        setTwitterUrl(obj, index);
        setUrlname(obj, index);
        return obj;
    }
    
    public void BrandDataOnDemand.setBrandName(Brand obj, int index) {
        String brandName = "brandName_" + index;
        obj.setBrandName(brandName);
    }
    
    public void BrandDataOnDemand.setDateCreated(Brand obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void BrandDataOnDemand.setDateModified(Brand obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void BrandDataOnDemand.setFacebookAccess(Brand obj, int index) {
        Integer facebookAccess = new Integer(index);
        obj.setFacebookAccess(facebookAccess);
    }
    
    public void BrandDataOnDemand.setFacebookLikes(Brand obj, int index) {
        Integer facebookLikes = new Integer(index);
        obj.setFacebookLikes(facebookLikes);
    }
    
    public void BrandDataOnDemand.setFacebookUrl(Brand obj, int index) {
        String facebookUrl = "facebookUrl_" + index;
        obj.setFacebookUrl(facebookUrl);
    }
    
    public void BrandDataOnDemand.setIsDirty(Brand obj, int index) {
        Boolean isDirty = Boolean.TRUE;
        obj.setIsDirty(isDirty);
    }
    
    public void BrandDataOnDemand.setLelaUrl(Brand obj, int index) {
        String lelaUrl = "lelaUrl_" + index;
        obj.setLelaUrl(lelaUrl);
    }
    
    public void BrandDataOnDemand.setManufacturer(Brand obj, int index) {
        Manufacturer manufacturer = null;
        obj.setManufacturer(manufacturer);
    }
    
    public void BrandDataOnDemand.setManufacturerName(Brand obj, int index) {
        String manufacturerName = "manufacturerName_" + index;
        obj.setManufacturerName(manufacturerName);
    }
    
    public void BrandDataOnDemand.setObjectId(Brand obj, int index) {
        String objectId = "objectId_" + index;
        obj.setObjectId(objectId);
    }
    
    public void BrandDataOnDemand.setPopshopsBrandId(Brand obj, int index) {
        Integer popshopsBrandId = new Integer(index);
        obj.setPopshopsBrandId(popshopsBrandId);
    }
    
    public void BrandDataOnDemand.setPopshopsSuccess(Brand obj, int index) {
        Boolean popshopsSuccess = Boolean.TRUE;
        obj.setPopshopsSuccess(popshopsSuccess);
    }
    
    public void BrandDataOnDemand.setTwitterFollowers(Brand obj, int index) {
        Integer twitterFollowers = new Integer(index);
        obj.setTwitterFollowers(twitterFollowers);
    }
    
    public void BrandDataOnDemand.setTwitterName(Brand obj, int index) {
        String twitterName = "twitterName_" + index;
        obj.setTwitterName(twitterName);
    }
    
    public void BrandDataOnDemand.setTwitterUrl(Brand obj, int index) {
        String twitterUrl = "twitterUrl_" + index;
        obj.setTwitterUrl(twitterUrl);
    }
    
    public void BrandDataOnDemand.setUrlname(Brand obj, int index) {
        String urlname = "urlname_" + index;
        obj.setUrlname(urlname);
    }
    
    public Brand BrandDataOnDemand.getSpecificBrand(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Brand obj = data.get(index);
        Long id = obj.getId();
        return Brand.findBrand(id);
    }
    
    public Brand BrandDataOnDemand.getRandomBrand() {
        init();
        Brand obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Brand.findBrand(id);
    }
    
    public boolean BrandDataOnDemand.modifyBrand(Brand obj) {
        return false;
    }
    
    public void BrandDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Brand.findBrandEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Brand' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Brand>();
        for (int i = 0; i < 10; i++) {
            Brand obj = getNewTransientBrand(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
