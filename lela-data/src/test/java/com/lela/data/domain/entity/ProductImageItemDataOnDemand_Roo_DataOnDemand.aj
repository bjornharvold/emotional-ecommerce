// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductImageItem;
import com.lela.data.domain.entity.ProductImageItemDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ProductImageItemDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProductImageItemDataOnDemand: @Component;
    
    private Random ProductImageItemDataOnDemand.rnd = new SecureRandom();
    
    private List<ProductImageItem> ProductImageItemDataOnDemand.data;
    
    public ProductImageItem ProductImageItemDataOnDemand.getNewTransientProductImageItem(int index) {
        ProductImageItem obj = new ProductImageItem();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setDoNotUse(obj, index);
        setDuplicate(obj, index);
        setItem(obj, index);
        setProductImage(obj, index);
        setProductImageItemStatus(obj, index);
        return obj;
    }
    
    public void ProductImageItemDataOnDemand.setDateCreated(ProductImageItem obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ProductImageItemDataOnDemand.setDateModified(ProductImageItem obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ProductImageItemDataOnDemand.setDoNotUse(ProductImageItem obj, int index) {
        Boolean doNotUse = Boolean.TRUE;
        obj.setDoNotUse(doNotUse);
    }
    
    public void ProductImageItemDataOnDemand.setDuplicate(ProductImageItem obj, int index) {
        Boolean duplicate = Boolean.TRUE;
        obj.setDuplicate(duplicate);
    }
    
    public ProductImageItem ProductImageItemDataOnDemand.getSpecificProductImageItem(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ProductImageItem obj = data.get(index);
        Long id = obj.getId();
        return ProductImageItem.findProductImageItem(id);
    }
    
    public ProductImageItem ProductImageItemDataOnDemand.getRandomProductImageItem() {
        init();
        ProductImageItem obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ProductImageItem.findProductImageItem(id);
    }
    
    public boolean ProductImageItemDataOnDemand.modifyProductImageItem(ProductImageItem obj) {
        return false;
    }
    
    public void ProductImageItemDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ProductImageItem.findProductImageItemEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ProductImageItem' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ProductImageItem>();
        for (int i = 0; i < 10; i++) {
            ProductImageItem obj = getNewTransientProductImageItem(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
