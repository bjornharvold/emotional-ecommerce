// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductDetailSection;
import com.lela.data.domain.entity.ProductDetailSectionDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ProductDetailSectionDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProductDetailSectionDataOnDemand: @Component;
    
    private Random ProductDetailSectionDataOnDemand.rnd = new SecureRandom();
    
    private List<ProductDetailSection> ProductDetailSectionDataOnDemand.data;
    
    public ProductDetailSection ProductDetailSectionDataOnDemand.getNewTransientProductDetailSection(int index) {
        ProductDetailSection obj = new ProductDetailSection();
        setCategory(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setDirty(obj, index);
        setObjectId(obj, index);
        setProductDetailSectionLabel(obj, index);
        setSectionName(obj, index);
        setSectionOrder(obj, index);
        return obj;
    }
    
    public void ProductDetailSectionDataOnDemand.setDateCreated(ProductDetailSection obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ProductDetailSectionDataOnDemand.setDateModified(ProductDetailSection obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ProductDetailSectionDataOnDemand.setDirty(ProductDetailSection obj, int index) {
        Boolean dirty = Boolean.TRUE;
        obj.setDirty(dirty);
    }
    
    public void ProductDetailSectionDataOnDemand.setObjectId(ProductDetailSection obj, int index) {
        String objectId = "objectId_" + index;
        obj.setObjectId(objectId);
    }
    
    public void ProductDetailSectionDataOnDemand.setProductDetailSectionLabel(ProductDetailSection obj, int index) {
        String productDetailSectionLabel = "productDetailSectionLabel_" + index;
        obj.setProductDetailSectionLabel(productDetailSectionLabel);
    }
    
    public void ProductDetailSectionDataOnDemand.setSectionName(ProductDetailSection obj, int index) {
        String sectionName = "sectionName_" + index;
        obj.setSectionName(sectionName);
    }
    
    public void ProductDetailSectionDataOnDemand.setSectionOrder(ProductDetailSection obj, int index) {
        Integer sectionOrder = new Integer(index);
        obj.setSectionOrder(sectionOrder);
    }
    
    public ProductDetailSection ProductDetailSectionDataOnDemand.getSpecificProductDetailSection(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ProductDetailSection obj = data.get(index);
        Long id = obj.getId();
        return ProductDetailSection.findProductDetailSection(id);
    }
    
    public ProductDetailSection ProductDetailSectionDataOnDemand.getRandomProductDetailSection() {
        init();
        ProductDetailSection obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ProductDetailSection.findProductDetailSection(id);
    }
    
    public boolean ProductDetailSectionDataOnDemand.modifyProductDetailSection(ProductDetailSection obj) {
        return false;
    }
    
    public void ProductDetailSectionDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ProductDetailSection.findProductDetailSectionEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ProductDetailSection' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ProductDetailSection>();
        for (int i = 0; i < 10; i++) {
            ProductDetailSection obj = getNewTransientProductDetailSection(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
