// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.BrandCountry;
import com.lela.data.domain.entity.BrandCountryDataOnDemand;
import com.lela.data.domain.entity.BrandCountryIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect BrandCountryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: BrandCountryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: BrandCountryIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: BrandCountryIntegrationTest: @Transactional;
    
    @Autowired
    BrandCountryDataOnDemand BrandCountryIntegrationTest.dod;
    
    @Test
    public void BrandCountryIntegrationTest.testCountBrandCountrys() {
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to initialize correctly", dod.getRandomBrandCountry());
        long count = BrandCountry.countBrandCountrys();
        Assert.assertTrue("Counter for 'BrandCountry' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void BrandCountryIntegrationTest.testFindBrandCountry() {
        BrandCountry obj = dod.getRandomBrandCountry();
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to provide an identifier", id);
        obj = BrandCountry.findBrandCountry(id);
        Assert.assertNotNull("Find method for 'BrandCountry' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'BrandCountry' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void BrandCountryIntegrationTest.testFindAllBrandCountrys() {
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to initialize correctly", dod.getRandomBrandCountry());
        long count = BrandCountry.countBrandCountrys();
        Assert.assertTrue("Too expensive to perform a find all test for 'BrandCountry', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<BrandCountry> result = BrandCountry.findAllBrandCountrys();
        Assert.assertNotNull("Find all method for 'BrandCountry' illegally returned null", result);
        Assert.assertTrue("Find all method for 'BrandCountry' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void BrandCountryIntegrationTest.testFindBrandCountryEntries() {
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to initialize correctly", dod.getRandomBrandCountry());
        long count = BrandCountry.countBrandCountrys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<BrandCountry> result = BrandCountry.findBrandCountryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'BrandCountry' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'BrandCountry' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void BrandCountryIntegrationTest.testFlush() {
        BrandCountry obj = dod.getRandomBrandCountry();
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to provide an identifier", id);
        obj = BrandCountry.findBrandCountry(id);
        Assert.assertNotNull("Find method for 'BrandCountry' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyBrandCountry(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'BrandCountry' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void BrandCountryIntegrationTest.testMergeUpdate() {
        BrandCountry obj = dod.getRandomBrandCountry();
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to provide an identifier", id);
        obj = BrandCountry.findBrandCountry(id);
        boolean modified =  dod.modifyBrandCountry(obj);
        Integer currentVersion = obj.getVersion();
        BrandCountry merged = (BrandCountry)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'BrandCountry' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void BrandCountryIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to initialize correctly", dod.getRandomBrandCountry());
        BrandCountry obj = dod.getNewTransientBrandCountry(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'BrandCountry' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'BrandCountry' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void BrandCountryIntegrationTest.testRemove() {
        BrandCountry obj = dod.getRandomBrandCountry();
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandCountry' failed to provide an identifier", id);
        obj = BrandCountry.findBrandCountry(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'BrandCountry' with identifier '" + id + "'", BrandCountry.findBrandCountry(id));
    }
    
}
