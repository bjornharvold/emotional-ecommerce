// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemIdentifier;
import com.lela.data.domain.entity.ItemIdentifierDataOnDemand;
import com.lela.data.domain.entity.ItemIdentifierIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ItemIdentifierIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ItemIdentifierIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ItemIdentifierIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ItemIdentifierIntegrationTest: @Transactional;
    
    @Autowired
    ItemIdentifierDataOnDemand ItemIdentifierIntegrationTest.dod;
    
    @Test
    public void ItemIdentifierIntegrationTest.testCountItemIdentifiers() {
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to initialize correctly", dod.getRandomItemIdentifier());
        long count = ItemIdentifier.countItemIdentifiers();
        Assert.assertTrue("Counter for 'ItemIdentifier' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ItemIdentifierIntegrationTest.testFindItemIdentifier() {
        ItemIdentifier obj = dod.getRandomItemIdentifier();
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to provide an identifier", id);
        obj = ItemIdentifier.findItemIdentifier(id);
        Assert.assertNotNull("Find method for 'ItemIdentifier' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ItemIdentifier' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ItemIdentifierIntegrationTest.testFindAllItemIdentifiers() {
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to initialize correctly", dod.getRandomItemIdentifier());
        long count = ItemIdentifier.countItemIdentifiers();
        Assert.assertTrue("Too expensive to perform a find all test for 'ItemIdentifier', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ItemIdentifier> result = ItemIdentifier.findAllItemIdentifiers();
        Assert.assertNotNull("Find all method for 'ItemIdentifier' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ItemIdentifier' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ItemIdentifierIntegrationTest.testFindItemIdentifierEntries() {
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to initialize correctly", dod.getRandomItemIdentifier());
        long count = ItemIdentifier.countItemIdentifiers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ItemIdentifier> result = ItemIdentifier.findItemIdentifierEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ItemIdentifier' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ItemIdentifier' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ItemIdentifierIntegrationTest.testFlush() {
        ItemIdentifier obj = dod.getRandomItemIdentifier();
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to provide an identifier", id);
        obj = ItemIdentifier.findItemIdentifier(id);
        Assert.assertNotNull("Find method for 'ItemIdentifier' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyItemIdentifier(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ItemIdentifier' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemIdentifierIntegrationTest.testMergeUpdate() {
        ItemIdentifier obj = dod.getRandomItemIdentifier();
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to provide an identifier", id);
        obj = ItemIdentifier.findItemIdentifier(id);
        boolean modified =  dod.modifyItemIdentifier(obj);
        Integer currentVersion = obj.getVersion();
        ItemIdentifier merged = (ItemIdentifier)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ItemIdentifier' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemIdentifierIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to initialize correctly", dod.getRandomItemIdentifier());
        ItemIdentifier obj = dod.getNewTransientItemIdentifier(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ItemIdentifier' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ItemIdentifier' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ItemIdentifierIntegrationTest.testRemove() {
        ItemIdentifier obj = dod.getRandomItemIdentifier();
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemIdentifier' failed to provide an identifier", id);
        obj = ItemIdentifier.findItemIdentifier(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ItemIdentifier' with identifier '" + id + "'", ItemIdentifier.findItemIdentifier(id));
    }
    
}
