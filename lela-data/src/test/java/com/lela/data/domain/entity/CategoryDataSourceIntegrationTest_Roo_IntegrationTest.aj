// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryDataSource;
import com.lela.data.domain.entity.CategoryDataSourceDataOnDemand;
import com.lela.data.domain.entity.CategoryDataSourceIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoryDataSourceIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoryDataSourceIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoryDataSourceIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoryDataSourceIntegrationTest: @Transactional;
    
    @Autowired
    CategoryDataSourceDataOnDemand CategoryDataSourceIntegrationTest.dod;
    
    @Test
    public void CategoryDataSourceIntegrationTest.testCountCategoryDataSources() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to initialize correctly", dod.getRandomCategoryDataSource());
        long count = CategoryDataSource.countCategoryDataSources();
        Assert.assertTrue("Counter for 'CategoryDataSource' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoryDataSourceIntegrationTest.testFindCategoryDataSource() {
        CategoryDataSource obj = dod.getRandomCategoryDataSource();
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to provide an identifier", id);
        obj = CategoryDataSource.findCategoryDataSource(id);
        Assert.assertNotNull("Find method for 'CategoryDataSource' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategoryDataSource' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoryDataSourceIntegrationTest.testFindAllCategoryDataSources() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to initialize correctly", dod.getRandomCategoryDataSource());
        long count = CategoryDataSource.countCategoryDataSources();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategoryDataSource', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategoryDataSource> result = CategoryDataSource.findAllCategoryDataSources();
        Assert.assertNotNull("Find all method for 'CategoryDataSource' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategoryDataSource' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoryDataSourceIntegrationTest.testFindCategoryDataSourceEntries() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to initialize correctly", dod.getRandomCategoryDataSource());
        long count = CategoryDataSource.countCategoryDataSources();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategoryDataSource> result = CategoryDataSource.findCategoryDataSourceEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategoryDataSource' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategoryDataSource' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoryDataSourceIntegrationTest.testFlush() {
        CategoryDataSource obj = dod.getRandomCategoryDataSource();
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to provide an identifier", id);
        obj = CategoryDataSource.findCategoryDataSource(id);
        Assert.assertNotNull("Find method for 'CategoryDataSource' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategoryDataSource(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategoryDataSource' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryDataSourceIntegrationTest.testMergeUpdate() {
        CategoryDataSource obj = dod.getRandomCategoryDataSource();
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to provide an identifier", id);
        obj = CategoryDataSource.findCategoryDataSource(id);
        boolean modified =  dod.modifyCategoryDataSource(obj);
        Integer currentVersion = obj.getVersion();
        CategoryDataSource merged = (CategoryDataSource)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategoryDataSource' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryDataSourceIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to initialize correctly", dod.getRandomCategoryDataSource());
        CategoryDataSource obj = dod.getNewTransientCategoryDataSource(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CategoryDataSource' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'CategoryDataSource' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CategoryDataSourceIntegrationTest.testRemove() {
        CategoryDataSource obj = dod.getRandomCategoryDataSource();
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryDataSource' failed to provide an identifier", id);
        obj = CategoryDataSource.findCategoryDataSource(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategoryDataSource' with identifier '" + id + "'", CategoryDataSource.findCategoryDataSource(id));
    }
    
}
