// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.FunctionalFilter;
import com.lela.data.domain.entity.FunctionalFilterDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect FunctionalFilterDataOnDemand_Roo_DataOnDemand {
    
    declare @type: FunctionalFilterDataOnDemand: @Component;
    
    private Random FunctionalFilterDataOnDemand.rnd = new SecureRandom();
    
    private List<FunctionalFilter> FunctionalFilterDataOnDemand.data;
    
    public FunctionalFilter FunctionalFilterDataOnDemand.getNewTransientFunctionalFilter(int index) {
        FunctionalFilter obj = new FunctionalFilter();
        setDataKey(obj, index);
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setDirty(obj, index);
        setFunctionalFilterLabel(obj, index);
        setFunctionalFilterName(obj, index);
        setFunctionalFilterOrder(obj, index);
        setFunctionalFilterType(obj, index);
        setLocale(obj, index);
        setObjectId(obj, index);
        return obj;
    }
    
    public void FunctionalFilterDataOnDemand.setDataKey(FunctionalFilter obj, int index) {
        String dataKey = "dataKey_" + index;
        obj.setDataKey(dataKey);
    }
    
    public void FunctionalFilterDataOnDemand.setDateCreated(FunctionalFilter obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void FunctionalFilterDataOnDemand.setDateModified(FunctionalFilter obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void FunctionalFilterDataOnDemand.setDirty(FunctionalFilter obj, int index) {
        Boolean dirty = Boolean.TRUE;
        obj.setDirty(dirty);
    }
    
    public void FunctionalFilterDataOnDemand.setFunctionalFilterLabel(FunctionalFilter obj, int index) {
        String functionalFilterLabel = "functionalFilterLabel_" + index;
        obj.setFunctionalFilterLabel(functionalFilterLabel);
    }
    
    public void FunctionalFilterDataOnDemand.setFunctionalFilterName(FunctionalFilter obj, int index) {
        String functionalFilterName = "functionalFilterName_" + index;
        obj.setFunctionalFilterName(functionalFilterName);
    }
    
    public void FunctionalFilterDataOnDemand.setFunctionalFilterOrder(FunctionalFilter obj, int index) {
        Integer functionalFilterOrder = new Integer(index);
        obj.setFunctionalFilterOrder(functionalFilterOrder);
    }
    
    public void FunctionalFilterDataOnDemand.setObjectId(FunctionalFilter obj, int index) {
        String objectId = "objectId_" + index;
        obj.setObjectId(objectId);
    }
    
    public FunctionalFilter FunctionalFilterDataOnDemand.getSpecificFunctionalFilter(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        FunctionalFilter obj = data.get(index);
        Long id = obj.getId();
        return FunctionalFilter.findFunctionalFilter(id);
    }
    
    public FunctionalFilter FunctionalFilterDataOnDemand.getRandomFunctionalFilter() {
        init();
        FunctionalFilter obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return FunctionalFilter.findFunctionalFilter(id);
    }
    
    public boolean FunctionalFilterDataOnDemand.modifyFunctionalFilter(FunctionalFilter obj) {
        return false;
    }
    
    public void FunctionalFilterDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = FunctionalFilter.findFunctionalFilterEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'FunctionalFilter' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<FunctionalFilter>();
        for (int i = 0; i < 10; i++) {
            FunctionalFilter obj = getNewTransientFunctionalFilter(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
