// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Brand;
import com.lela.data.domain.entity.BrandDataOnDemand;
import com.lela.data.domain.entity.BrandIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect BrandIntegrationTest_Roo_IntegrationTest {
    
    declare @type: BrandIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: BrandIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: BrandIntegrationTest: @Transactional;
    
    @Autowired
    BrandDataOnDemand BrandIntegrationTest.dod;
    
    @Test
    public void BrandIntegrationTest.testCountBrands() {
        Assert.assertNotNull("Data on demand for 'Brand' failed to initialize correctly", dod.getRandomBrand());
        long count = Brand.countBrands();
        Assert.assertTrue("Counter for 'Brand' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void BrandIntegrationTest.testFindBrand() {
        Brand obj = dod.getRandomBrand();
        Assert.assertNotNull("Data on demand for 'Brand' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Brand' failed to provide an identifier", id);
        obj = Brand.findBrand(id);
        Assert.assertNotNull("Find method for 'Brand' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Brand' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void BrandIntegrationTest.testFindBrandEntries() {
        Assert.assertNotNull("Data on demand for 'Brand' failed to initialize correctly", dod.getRandomBrand());
        long count = Brand.countBrands();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Brand> result = Brand.findBrandEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Brand' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Brand' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void BrandIntegrationTest.testFlush() {
        Brand obj = dod.getRandomBrand();
        Assert.assertNotNull("Data on demand for 'Brand' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Brand' failed to provide an identifier", id);
        obj = Brand.findBrand(id);
        Assert.assertNotNull("Find method for 'Brand' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyBrand(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Brand' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void BrandIntegrationTest.testMergeUpdate() {
        Brand obj = dod.getRandomBrand();
        Assert.assertNotNull("Data on demand for 'Brand' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Brand' failed to provide an identifier", id);
        obj = Brand.findBrand(id);
        boolean modified =  dod.modifyBrand(obj);
        Integer currentVersion = obj.getVersion();
        Brand merged = (Brand)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Brand' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void BrandIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Brand' failed to initialize correctly", dod.getRandomBrand());
        Brand obj = dod.getNewTransientBrand(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Brand' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Brand' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Brand' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void BrandIntegrationTest.testRemove() {
        Brand obj = dod.getRandomBrand();
        Assert.assertNotNull("Data on demand for 'Brand' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Brand' failed to provide an identifier", id);
        obj = Brand.findBrand(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Brand' with identifier '" + id + "'", Brand.findBrand(id));
    }
    
}
