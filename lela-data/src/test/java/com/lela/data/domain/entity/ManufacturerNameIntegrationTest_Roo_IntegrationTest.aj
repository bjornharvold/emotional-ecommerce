// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ManufacturerName;
import com.lela.data.domain.entity.ManufacturerNameDataOnDemand;
import com.lela.data.domain.entity.ManufacturerNameIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ManufacturerNameIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ManufacturerNameIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ManufacturerNameIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ManufacturerNameIntegrationTest: @Transactional;
    
    @Autowired
    ManufacturerNameDataOnDemand ManufacturerNameIntegrationTest.dod;
    
    @Test
    public void ManufacturerNameIntegrationTest.testCountManufacturerNames() {
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to initialize correctly", dod.getRandomManufacturerName());
        long count = ManufacturerName.countManufacturerNames();
        Assert.assertTrue("Counter for 'ManufacturerName' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ManufacturerNameIntegrationTest.testFindManufacturerName() {
        ManufacturerName obj = dod.getRandomManufacturerName();
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to provide an identifier", id);
        obj = ManufacturerName.findManufacturerName(id);
        Assert.assertNotNull("Find method for 'ManufacturerName' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ManufacturerName' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ManufacturerNameIntegrationTest.testFindAllManufacturerNames() {
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to initialize correctly", dod.getRandomManufacturerName());
        long count = ManufacturerName.countManufacturerNames();
        Assert.assertTrue("Too expensive to perform a find all test for 'ManufacturerName', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ManufacturerName> result = ManufacturerName.findAllManufacturerNames();
        Assert.assertNotNull("Find all method for 'ManufacturerName' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ManufacturerName' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ManufacturerNameIntegrationTest.testFindManufacturerNameEntries() {
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to initialize correctly", dod.getRandomManufacturerName());
        long count = ManufacturerName.countManufacturerNames();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ManufacturerName> result = ManufacturerName.findManufacturerNameEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ManufacturerName' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ManufacturerName' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ManufacturerNameIntegrationTest.testFlush() {
        ManufacturerName obj = dod.getRandomManufacturerName();
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to provide an identifier", id);
        obj = ManufacturerName.findManufacturerName(id);
        Assert.assertNotNull("Find method for 'ManufacturerName' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyManufacturerName(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ManufacturerName' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ManufacturerNameIntegrationTest.testMergeUpdate() {
        ManufacturerName obj = dod.getRandomManufacturerName();
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to provide an identifier", id);
        obj = ManufacturerName.findManufacturerName(id);
        boolean modified =  dod.modifyManufacturerName(obj);
        Integer currentVersion = obj.getVersion();
        ManufacturerName merged = (ManufacturerName)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ManufacturerName' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ManufacturerNameIntegrationTest.testRemove() {
        ManufacturerName obj = dod.getRandomManufacturerName();
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ManufacturerName' failed to provide an identifier", id);
        obj = ManufacturerName.findManufacturerName(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ManufacturerName' with identifier '" + id + "'", ManufacturerName.findManufacturerName(id));
    }
    
}
