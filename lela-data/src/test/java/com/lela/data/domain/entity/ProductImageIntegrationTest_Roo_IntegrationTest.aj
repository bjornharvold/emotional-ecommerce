// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductImage;
import com.lela.data.domain.entity.ProductImageDataOnDemand;
import com.lela.data.domain.entity.ProductImageIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductImageIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductImageIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductImageIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductImageIntegrationTest: @Transactional;
    
    @Autowired
    ProductImageDataOnDemand ProductImageIntegrationTest.dod;
    
    @Test
    public void ProductImageIntegrationTest.testCountProductImages() {
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to initialize correctly", dod.getRandomProductImage());
        long count = ProductImage.countProductImages();
        Assert.assertTrue("Counter for 'ProductImage' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductImageIntegrationTest.testFindProductImage() {
        ProductImage obj = dod.getRandomProductImage();
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to provide an identifier", id);
        obj = ProductImage.findProductImage(id);
        Assert.assertNotNull("Find method for 'ProductImage' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductImage' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductImageIntegrationTest.testFindAllProductImages() {
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to initialize correctly", dod.getRandomProductImage());
        long count = ProductImage.countProductImages();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductImage', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductImage> result = ProductImage.findAllProductImages();
        Assert.assertNotNull("Find all method for 'ProductImage' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductImage' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductImageIntegrationTest.testFindProductImageEntries() {
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to initialize correctly", dod.getRandomProductImage());
        long count = ProductImage.countProductImages();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductImage> result = ProductImage.findProductImageEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductImage' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductImage' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductImageIntegrationTest.testFlush() {
        ProductImage obj = dod.getRandomProductImage();
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to provide an identifier", id);
        obj = ProductImage.findProductImage(id);
        Assert.assertNotNull("Find method for 'ProductImage' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProductImage(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProductImage' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductImageIntegrationTest.testMergeUpdate() {
        ProductImage obj = dod.getRandomProductImage();
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to provide an identifier", id);
        obj = ProductImage.findProductImage(id);
        boolean modified =  dod.modifyProductImage(obj);
        Integer currentVersion = obj.getVersion();
        ProductImage merged = (ProductImage)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProductImage' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductImageIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to initialize correctly", dod.getRandomProductImage());
        ProductImage obj = dod.getNewTransientProductImage(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProductImage' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductImage' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductImageIntegrationTest.testRemove() {
        ProductImage obj = dod.getRandomProductImage();
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductImage' failed to provide an identifier", id);
        obj = ProductImage.findProductImage(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductImage' with identifier '" + id + "'", ProductImage.findProductImage(id));
    }
    
}
