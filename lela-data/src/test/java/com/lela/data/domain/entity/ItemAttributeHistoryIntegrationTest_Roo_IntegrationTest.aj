// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemAttributeHistory;
import com.lela.data.domain.entity.ItemAttributeHistoryDataOnDemand;
import com.lela.data.domain.entity.ItemAttributeHistoryIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ItemAttributeHistoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ItemAttributeHistoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ItemAttributeHistoryIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ItemAttributeHistoryIntegrationTest: @Transactional;
    
    @Autowired
    ItemAttributeHistoryDataOnDemand ItemAttributeHistoryIntegrationTest.dod;
    
    @Test
    public void ItemAttributeHistoryIntegrationTest.testCountItemAttributeHistorys() {
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to initialize correctly", dod.getRandomItemAttributeHistory());
        long count = ItemAttributeHistory.countItemAttributeHistorys();
        Assert.assertTrue("Counter for 'ItemAttributeHistory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ItemAttributeHistoryIntegrationTest.testFindItemAttributeHistory() {
        ItemAttributeHistory obj = dod.getRandomItemAttributeHistory();
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to provide an identifier", id);
        obj = ItemAttributeHistory.findItemAttributeHistory(id);
        Assert.assertNotNull("Find method for 'ItemAttributeHistory' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ItemAttributeHistory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ItemAttributeHistoryIntegrationTest.testFindAllItemAttributeHistorys() {
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to initialize correctly", dod.getRandomItemAttributeHistory());
        long count = ItemAttributeHistory.countItemAttributeHistorys();
        Assert.assertTrue("Too expensive to perform a find all test for 'ItemAttributeHistory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ItemAttributeHistory> result = ItemAttributeHistory.findAllItemAttributeHistorys();
        Assert.assertNotNull("Find all method for 'ItemAttributeHistory' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ItemAttributeHistory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ItemAttributeHistoryIntegrationTest.testFindItemAttributeHistoryEntries() {
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to initialize correctly", dod.getRandomItemAttributeHistory());
        long count = ItemAttributeHistory.countItemAttributeHistorys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ItemAttributeHistory> result = ItemAttributeHistory.findItemAttributeHistoryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ItemAttributeHistory' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ItemAttributeHistory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ItemAttributeHistoryIntegrationTest.testFlush() {
        ItemAttributeHistory obj = dod.getRandomItemAttributeHistory();
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to provide an identifier", id);
        obj = ItemAttributeHistory.findItemAttributeHistory(id);
        Assert.assertNotNull("Find method for 'ItemAttributeHistory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyItemAttributeHistory(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ItemAttributeHistory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemAttributeHistoryIntegrationTest.testMergeUpdate() {
        ItemAttributeHistory obj = dod.getRandomItemAttributeHistory();
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to provide an identifier", id);
        obj = ItemAttributeHistory.findItemAttributeHistory(id);
        boolean modified =  dod.modifyItemAttributeHistory(obj);
        Integer currentVersion = obj.getVersion();
        ItemAttributeHistory merged = (ItemAttributeHistory)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ItemAttributeHistory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemAttributeHistoryIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to initialize correctly", dod.getRandomItemAttributeHistory());
        ItemAttributeHistory obj = dod.getNewTransientItemAttributeHistory(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ItemAttributeHistory' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ItemAttributeHistory' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ItemAttributeHistoryIntegrationTest.testRemove() {
        ItemAttributeHistory obj = dod.getRandomItemAttributeHistory();
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemAttributeHistory' failed to provide an identifier", id);
        obj = ItemAttributeHistory.findItemAttributeHistory(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ItemAttributeHistory' with identifier '" + id + "'", ItemAttributeHistory.findItemAttributeHistory(id));
    }
    
}
