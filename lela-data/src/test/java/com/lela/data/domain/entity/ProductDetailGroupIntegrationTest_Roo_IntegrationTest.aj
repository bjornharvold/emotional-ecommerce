// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductDetailGroup;
import com.lela.data.domain.entity.ProductDetailGroupDataOnDemand;
import com.lela.data.domain.entity.ProductDetailGroupIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductDetailGroupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductDetailGroupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductDetailGroupIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductDetailGroupIntegrationTest: @Transactional;
    
    @Autowired
    ProductDetailGroupDataOnDemand ProductDetailGroupIntegrationTest.dod;
    
    @Test
    public void ProductDetailGroupIntegrationTest.testCountProductDetailGroups() {
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to initialize correctly", dod.getRandomProductDetailGroup());
        long count = ProductDetailGroup.countProductDetailGroups();
        Assert.assertTrue("Counter for 'ProductDetailGroup' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductDetailGroupIntegrationTest.testFindProductDetailGroup() {
        ProductDetailGroup obj = dod.getRandomProductDetailGroup();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to provide an identifier", id);
        obj = ProductDetailGroup.findProductDetailGroup(id);
        Assert.assertNotNull("Find method for 'ProductDetailGroup' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductDetailGroup' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductDetailGroupIntegrationTest.testFindAllProductDetailGroups() {
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to initialize correctly", dod.getRandomProductDetailGroup());
        long count = ProductDetailGroup.countProductDetailGroups();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductDetailGroup', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductDetailGroup> result = ProductDetailGroup.findAllProductDetailGroups();
        Assert.assertNotNull("Find all method for 'ProductDetailGroup' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductDetailGroup' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductDetailGroupIntegrationTest.testFindProductDetailGroupEntries() {
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to initialize correctly", dod.getRandomProductDetailGroup());
        long count = ProductDetailGroup.countProductDetailGroups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductDetailGroup> result = ProductDetailGroup.findProductDetailGroupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductDetailGroup' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductDetailGroup' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductDetailGroupIntegrationTest.testFlush() {
        ProductDetailGroup obj = dod.getRandomProductDetailGroup();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to provide an identifier", id);
        obj = ProductDetailGroup.findProductDetailGroup(id);
        Assert.assertNotNull("Find method for 'ProductDetailGroup' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProductDetailGroup(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProductDetailGroup' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductDetailGroupIntegrationTest.testMergeUpdate() {
        ProductDetailGroup obj = dod.getRandomProductDetailGroup();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to provide an identifier", id);
        obj = ProductDetailGroup.findProductDetailGroup(id);
        boolean modified =  dod.modifyProductDetailGroup(obj);
        Integer currentVersion = obj.getVersion();
        ProductDetailGroup merged = (ProductDetailGroup)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProductDetailGroup' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductDetailGroupIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to initialize correctly", dod.getRandomProductDetailGroup());
        ProductDetailGroup obj = dod.getNewTransientProductDetailGroup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProductDetailGroup' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProductDetailGroup' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductDetailGroupIntegrationTest.testRemove() {
        ProductDetailGroup obj = dod.getRandomProductDetailGroup();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductDetailGroup' failed to provide an identifier", id);
        obj = ProductDetailGroup.findProductDetailGroup(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductDetailGroup' with identifier '" + id + "'", ProductDetailGroup.findProductDetailGroup(id));
    }
    
}
