// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ProductMotivator;
import com.lela.data.domain.entity.ProductMotivatorDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ProductMotivatorDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProductMotivatorDataOnDemand: @Component;
    
    private Random ProductMotivatorDataOnDemand.rnd = new SecureRandom();
    
    private List<ProductMotivator> ProductMotivatorDataOnDemand.data;
    
    public ProductMotivator ProductMotivatorDataOnDemand.getNewTransientProductMotivator(int index) {
        ProductMotivator obj = new ProductMotivator();
        setDateCreated(obj, index);
        setDateModified(obj, index);
        setDirty(obj, index);
        setItem(obj, index);
        setMotivator(obj, index);
        setMotivatorScore(obj, index);
        return obj;
    }
    
    public void ProductMotivatorDataOnDemand.setDateCreated(ProductMotivator obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void ProductMotivatorDataOnDemand.setDateModified(ProductMotivator obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void ProductMotivatorDataOnDemand.setDirty(ProductMotivator obj, int index) {
        Boolean dirty = Boolean.TRUE;
        obj.setDirty(dirty);
    }
    
    public void ProductMotivatorDataOnDemand.setMotivatorScore(ProductMotivator obj, int index) {
        Integer motivatorScore = new Integer(index);
        obj.setMotivatorScore(motivatorScore);
    }
    
    public ProductMotivator ProductMotivatorDataOnDemand.getSpecificProductMotivator(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ProductMotivator obj = data.get(index);
        Long id = obj.getId();
        return ProductMotivator.findProductMotivator(id);
    }
    
    public ProductMotivator ProductMotivatorDataOnDemand.getRandomProductMotivator() {
        init();
        ProductMotivator obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ProductMotivator.findProductMotivator(id);
    }
    
    public boolean ProductMotivatorDataOnDemand.modifyProductMotivator(ProductMotivator obj) {
        return false;
    }
    
    public void ProductMotivatorDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ProductMotivator.findProductMotivatorEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ProductMotivator' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ProductMotivator>();
        for (int i = 0; i < 10; i++) {
            ProductMotivator obj = getNewTransientProductMotivator(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
