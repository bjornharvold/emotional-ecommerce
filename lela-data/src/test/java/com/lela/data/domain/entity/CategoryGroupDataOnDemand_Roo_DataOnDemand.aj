// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryGroup;
import com.lela.data.domain.entity.CategoryGroupDataOnDemand;
import com.lela.data.domain.entity.NavbarDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect CategoryGroupDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CategoryGroupDataOnDemand: @Component;
    
    private Random CategoryGroupDataOnDemand.rnd = new SecureRandom();
    
    private List<CategoryGroup> CategoryGroupDataOnDemand.data;
    
    @Autowired
    NavbarDataOnDemand CategoryGroupDataOnDemand.navbarDataOnDemand;
    
    public void CategoryGroupDataOnDemand.setCategoryGroupName(CategoryGroup obj, int index) {
        String categoryGroupName = "categoryGroupName_" + index;
        if (categoryGroupName.length() > 255) {
            categoryGroupName = categoryGroupName.substring(0, 255);
        }
        obj.setCategoryGroupName(categoryGroupName);
    }
    
    public void CategoryGroupDataOnDemand.setCategoryGroupOrder(CategoryGroup obj, int index) {
        Integer categoryGroupOrder = new Integer(index);
        obj.setCategoryGroupOrder(categoryGroupOrder);
    }
    
    public void CategoryGroupDataOnDemand.setCategoryGroupStatus(CategoryGroup obj, int index) {
        String categoryGroupStatus = "categoryGroupStatus_" + index;
        if (categoryGroupStatus.length() > 255) {
            categoryGroupStatus = categoryGroupStatus.substring(0, 255);
        }
        obj.setCategoryGroupStatus(categoryGroupStatus);
    }
    
    public void CategoryGroupDataOnDemand.setCategoryGroupUrlName(CategoryGroup obj, int index) {
        String categoryGroupUrlName = "categoryGroupUrlName_" + index;
        if (categoryGroupUrlName.length() > 255) {
            categoryGroupUrlName = categoryGroupUrlName.substring(0, 255);
        }
        obj.setCategoryGroupUrlName(categoryGroupUrlName);
    }
    
    public void CategoryGroupDataOnDemand.setDateCreated(CategoryGroup obj, int index) {
        Date dateCreated = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateCreated(dateCreated);
    }
    
    public void CategoryGroupDataOnDemand.setDateModified(CategoryGroup obj, int index) {
        Date dateModified = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDateModified(dateModified);
    }
    
    public void CategoryGroupDataOnDemand.setDirty(CategoryGroup obj, int index) {
        Boolean dirty = Boolean.TRUE;
        obj.setDirty(dirty);
    }
    
    public CategoryGroup CategoryGroupDataOnDemand.getSpecificCategoryGroup(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CategoryGroup obj = data.get(index);
        Long id = obj.getId();
        return CategoryGroup.findCategoryGroup(id);
    }
    
    public CategoryGroup CategoryGroupDataOnDemand.getRandomCategoryGroup() {
        init();
        CategoryGroup obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CategoryGroup.findCategoryGroup(id);
    }
    
    public boolean CategoryGroupDataOnDemand.modifyCategoryGroup(CategoryGroup obj) {
        return false;
    }
    
    public void CategoryGroupDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CategoryGroup.findCategoryGroupEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CategoryGroup' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CategoryGroup>();
        for (int i = 0; i < 10; i++) {
            CategoryGroup obj = getNewTransientCategoryGroup(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
