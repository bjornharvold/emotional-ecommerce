// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.Color;
import com.lela.data.domain.entity.ColorDataOnDemand;
import com.lela.data.domain.entity.ColorIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ColorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ColorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ColorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ColorIntegrationTest: @Transactional;
    
    @Autowired
    ColorDataOnDemand ColorIntegrationTest.dod;
    
    @Test
    public void ColorIntegrationTest.testCountColors() {
        Assert.assertNotNull("Data on demand for 'Color' failed to initialize correctly", dod.getRandomColor());
        long count = Color.countColors();
        Assert.assertTrue("Counter for 'Color' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ColorIntegrationTest.testFindColor() {
        Color obj = dod.getRandomColor();
        Assert.assertNotNull("Data on demand for 'Color' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Color' failed to provide an identifier", id);
        obj = Color.findColor(id);
        Assert.assertNotNull("Find method for 'Color' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Color' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ColorIntegrationTest.testFindAllColors() {
        Assert.assertNotNull("Data on demand for 'Color' failed to initialize correctly", dod.getRandomColor());
        long count = Color.countColors();
        Assert.assertTrue("Too expensive to perform a find all test for 'Color', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Color> result = Color.findAllColors();
        Assert.assertNotNull("Find all method for 'Color' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Color' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ColorIntegrationTest.testFindColorEntries() {
        Assert.assertNotNull("Data on demand for 'Color' failed to initialize correctly", dod.getRandomColor());
        long count = Color.countColors();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Color> result = Color.findColorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Color' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Color' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ColorIntegrationTest.testFlush() {
        Color obj = dod.getRandomColor();
        Assert.assertNotNull("Data on demand for 'Color' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Color' failed to provide an identifier", id);
        obj = Color.findColor(id);
        Assert.assertNotNull("Find method for 'Color' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyColor(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Color' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ColorIntegrationTest.testMergeUpdate() {
        Color obj = dod.getRandomColor();
        Assert.assertNotNull("Data on demand for 'Color' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Color' failed to provide an identifier", id);
        obj = Color.findColor(id);
        boolean modified =  dod.modifyColor(obj);
        Integer currentVersion = obj.getVersion();
        Color merged = (Color)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Color' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ColorIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Color' failed to initialize correctly", dod.getRandomColor());
        Color obj = dod.getNewTransientColor(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Color' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Color' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Color' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ColorIntegrationTest.testRemove() {
        Color obj = dod.getRandomColor();
        Assert.assertNotNull("Data on demand for 'Color' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Color' failed to provide an identifier", id);
        obj = Color.findColor(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Color' with identifier '" + id + "'", Color.findColor(id));
    }
    
}
