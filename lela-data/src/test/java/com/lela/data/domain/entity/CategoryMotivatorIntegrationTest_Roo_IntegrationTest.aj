// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.CategoryMotivator;
import com.lela.data.domain.entity.CategoryMotivatorDataOnDemand;
import com.lela.data.domain.entity.CategoryMotivatorIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CategoryMotivatorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CategoryMotivatorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CategoryMotivatorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CategoryMotivatorIntegrationTest: @Transactional;
    
    @Autowired
    CategoryMotivatorDataOnDemand CategoryMotivatorIntegrationTest.dod;
    
    @Test
    public void CategoryMotivatorIntegrationTest.testCountCategoryMotivators() {
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to initialize correctly", dod.getRandomCategoryMotivator());
        long count = CategoryMotivator.countCategoryMotivators();
        Assert.assertTrue("Counter for 'CategoryMotivator' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CategoryMotivatorIntegrationTest.testFindCategoryMotivator() {
        CategoryMotivator obj = dod.getRandomCategoryMotivator();
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to provide an identifier", id);
        obj = CategoryMotivator.findCategoryMotivator(id);
        Assert.assertNotNull("Find method for 'CategoryMotivator' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CategoryMotivator' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CategoryMotivatorIntegrationTest.testFindAllCategoryMotivators() {
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to initialize correctly", dod.getRandomCategoryMotivator());
        long count = CategoryMotivator.countCategoryMotivators();
        Assert.assertTrue("Too expensive to perform a find all test for 'CategoryMotivator', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CategoryMotivator> result = CategoryMotivator.findAllCategoryMotivators();
        Assert.assertNotNull("Find all method for 'CategoryMotivator' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CategoryMotivator' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CategoryMotivatorIntegrationTest.testFindCategoryMotivatorEntries() {
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to initialize correctly", dod.getRandomCategoryMotivator());
        long count = CategoryMotivator.countCategoryMotivators();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CategoryMotivator> result = CategoryMotivator.findCategoryMotivatorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CategoryMotivator' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CategoryMotivator' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CategoryMotivatorIntegrationTest.testFlush() {
        CategoryMotivator obj = dod.getRandomCategoryMotivator();
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to provide an identifier", id);
        obj = CategoryMotivator.findCategoryMotivator(id);
        Assert.assertNotNull("Find method for 'CategoryMotivator' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCategoryMotivator(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CategoryMotivator' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryMotivatorIntegrationTest.testMergeUpdate() {
        CategoryMotivator obj = dod.getRandomCategoryMotivator();
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to provide an identifier", id);
        obj = CategoryMotivator.findCategoryMotivator(id);
        boolean modified =  dod.modifyCategoryMotivator(obj);
        Integer currentVersion = obj.getVersion();
        CategoryMotivator merged = (CategoryMotivator)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CategoryMotivator' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CategoryMotivatorIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to initialize correctly", dod.getRandomCategoryMotivator());
        CategoryMotivator obj = dod.getNewTransientCategoryMotivator(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CategoryMotivator' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'CategoryMotivator' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CategoryMotivatorIntegrationTest.testRemove() {
        CategoryMotivator obj = dod.getRandomCategoryMotivator();
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CategoryMotivator' failed to provide an identifier", id);
        obj = CategoryMotivator.findCategoryMotivator(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CategoryMotivator' with identifier '" + id + "'", CategoryMotivator.findCategoryMotivator(id));
    }
    
}
