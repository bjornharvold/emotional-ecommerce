// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ReviewSource;
import com.lela.data.domain.entity.ReviewSourceDataOnDemand;
import com.lela.data.domain.entity.ReviewSourceIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ReviewSourceIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ReviewSourceIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ReviewSourceIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ReviewSourceIntegrationTest: @Transactional;
    
    @Autowired
    ReviewSourceDataOnDemand ReviewSourceIntegrationTest.dod;
    
    @Test
    public void ReviewSourceIntegrationTest.testCountReviewSources() {
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to initialize correctly", dod.getRandomReviewSource());
        long count = ReviewSource.countReviewSources();
        Assert.assertTrue("Counter for 'ReviewSource' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ReviewSourceIntegrationTest.testFindReviewSource() {
        ReviewSource obj = dod.getRandomReviewSource();
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to provide an identifier", id);
        obj = ReviewSource.findReviewSource(id);
        Assert.assertNotNull("Find method for 'ReviewSource' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ReviewSource' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ReviewSourceIntegrationTest.testFindAllReviewSources() {
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to initialize correctly", dod.getRandomReviewSource());
        long count = ReviewSource.countReviewSources();
        Assert.assertTrue("Too expensive to perform a find all test for 'ReviewSource', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ReviewSource> result = ReviewSource.findAllReviewSources();
        Assert.assertNotNull("Find all method for 'ReviewSource' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ReviewSource' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ReviewSourceIntegrationTest.testFindReviewSourceEntries() {
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to initialize correctly", dod.getRandomReviewSource());
        long count = ReviewSource.countReviewSources();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ReviewSource> result = ReviewSource.findReviewSourceEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ReviewSource' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ReviewSource' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ReviewSourceIntegrationTest.testFlush() {
        ReviewSource obj = dod.getRandomReviewSource();
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to provide an identifier", id);
        obj = ReviewSource.findReviewSource(id);
        Assert.assertNotNull("Find method for 'ReviewSource' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyReviewSource(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ReviewSource' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ReviewSourceIntegrationTest.testMergeUpdate() {
        ReviewSource obj = dod.getRandomReviewSource();
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to provide an identifier", id);
        obj = ReviewSource.findReviewSource(id);
        boolean modified =  dod.modifyReviewSource(obj);
        Integer currentVersion = obj.getVersion();
        ReviewSource merged = (ReviewSource)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ReviewSource' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ReviewSourceIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to initialize correctly", dod.getRandomReviewSource());
        ReviewSource obj = dod.getNewTransientReviewSource(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ReviewSource' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ReviewSource' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ReviewSourceIntegrationTest.testRemove() {
        ReviewSource obj = dod.getRandomReviewSource();
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ReviewSource' failed to provide an identifier", id);
        obj = ReviewSource.findReviewSource(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ReviewSource' with identifier '" + id + "'", ReviewSource.findReviewSource(id));
    }
    
}
