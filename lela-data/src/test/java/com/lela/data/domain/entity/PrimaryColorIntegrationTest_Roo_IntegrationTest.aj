// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.PrimaryColor;
import com.lela.data.domain.entity.PrimaryColorDataOnDemand;
import com.lela.data.domain.entity.PrimaryColorIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PrimaryColorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PrimaryColorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PrimaryColorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PrimaryColorIntegrationTest: @Transactional;
    
    @Autowired
    PrimaryColorDataOnDemand PrimaryColorIntegrationTest.dod;
    
    @Test
    public void PrimaryColorIntegrationTest.testCountPrimaryColors() {
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to initialize correctly", dod.getRandomPrimaryColor());
        long count = PrimaryColor.countPrimaryColors();
        Assert.assertTrue("Counter for 'PrimaryColor' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PrimaryColorIntegrationTest.testFindPrimaryColor() {
        PrimaryColor obj = dod.getRandomPrimaryColor();
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to provide an identifier", id);
        obj = PrimaryColor.findPrimaryColor(id);
        Assert.assertNotNull("Find method for 'PrimaryColor' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PrimaryColor' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PrimaryColorIntegrationTest.testFindAllPrimaryColors() {
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to initialize correctly", dod.getRandomPrimaryColor());
        long count = PrimaryColor.countPrimaryColors();
        Assert.assertTrue("Too expensive to perform a find all test for 'PrimaryColor', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PrimaryColor> result = PrimaryColor.findAllPrimaryColors();
        Assert.assertNotNull("Find all method for 'PrimaryColor' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PrimaryColor' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PrimaryColorIntegrationTest.testFindPrimaryColorEntries() {
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to initialize correctly", dod.getRandomPrimaryColor());
        long count = PrimaryColor.countPrimaryColors();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PrimaryColor> result = PrimaryColor.findPrimaryColorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PrimaryColor' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PrimaryColor' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PrimaryColorIntegrationTest.testFlush() {
        PrimaryColor obj = dod.getRandomPrimaryColor();
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to provide an identifier", id);
        obj = PrimaryColor.findPrimaryColor(id);
        Assert.assertNotNull("Find method for 'PrimaryColor' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyPrimaryColor(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'PrimaryColor' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PrimaryColorIntegrationTest.testMergeUpdate() {
        PrimaryColor obj = dod.getRandomPrimaryColor();
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to provide an identifier", id);
        obj = PrimaryColor.findPrimaryColor(id);
        boolean modified =  dod.modifyPrimaryColor(obj);
        Integer currentVersion = obj.getVersion();
        PrimaryColor merged = (PrimaryColor)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'PrimaryColor' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PrimaryColorIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to initialize correctly", dod.getRandomPrimaryColor());
        PrimaryColor obj = dod.getNewTransientPrimaryColor(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PrimaryColor' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'PrimaryColor' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PrimaryColorIntegrationTest.testRemove() {
        PrimaryColor obj = dod.getRandomPrimaryColor();
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PrimaryColor' failed to provide an identifier", id);
        obj = PrimaryColor.findPrimaryColor(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'PrimaryColor' with identifier '" + id + "'", PrimaryColor.findPrimaryColor(id));
    }
    
}
