// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.ItemVideo;
import com.lela.data.domain.entity.ItemVideoDataOnDemand;
import com.lela.data.domain.entity.ItemVideoIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ItemVideoIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ItemVideoIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ItemVideoIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ItemVideoIntegrationTest: @Transactional;
    
    @Autowired
    ItemVideoDataOnDemand ItemVideoIntegrationTest.dod;
    
    @Test
    public void ItemVideoIntegrationTest.testCountItemVideos() {
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to initialize correctly", dod.getRandomItemVideo());
        long count = ItemVideo.countItemVideos();
        Assert.assertTrue("Counter for 'ItemVideo' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ItemVideoIntegrationTest.testFindItemVideo() {
        ItemVideo obj = dod.getRandomItemVideo();
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to provide an identifier", id);
        obj = ItemVideo.findItemVideo(id);
        Assert.assertNotNull("Find method for 'ItemVideo' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ItemVideo' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ItemVideoIntegrationTest.testFindAllItemVideos() {
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to initialize correctly", dod.getRandomItemVideo());
        long count = ItemVideo.countItemVideos();
        Assert.assertTrue("Too expensive to perform a find all test for 'ItemVideo', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ItemVideo> result = ItemVideo.findAllItemVideos();
        Assert.assertNotNull("Find all method for 'ItemVideo' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ItemVideo' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ItemVideoIntegrationTest.testFindItemVideoEntries() {
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to initialize correctly", dod.getRandomItemVideo());
        long count = ItemVideo.countItemVideos();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ItemVideo> result = ItemVideo.findItemVideoEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ItemVideo' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ItemVideo' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ItemVideoIntegrationTest.testFlush() {
        ItemVideo obj = dod.getRandomItemVideo();
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to provide an identifier", id);
        obj = ItemVideo.findItemVideo(id);
        Assert.assertNotNull("Find method for 'ItemVideo' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyItemVideo(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ItemVideo' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemVideoIntegrationTest.testMergeUpdate() {
        ItemVideo obj = dod.getRandomItemVideo();
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to provide an identifier", id);
        obj = ItemVideo.findItemVideo(id);
        boolean modified =  dod.modifyItemVideo(obj);
        Integer currentVersion = obj.getVersion();
        ItemVideo merged = (ItemVideo)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ItemVideo' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ItemVideoIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to initialize correctly", dod.getRandomItemVideo());
        ItemVideo obj = dod.getNewTransientItemVideo(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ItemVideo' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ItemVideo' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ItemVideoIntegrationTest.testRemove() {
        ItemVideo obj = dod.getRandomItemVideo();
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ItemVideo' failed to provide an identifier", id);
        obj = ItemVideo.findItemVideo(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ItemVideo' with identifier '" + id + "'", ItemVideo.findItemVideo(id));
    }
    
}
