// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lela.data.domain.entity;

import com.lela.data.domain.entity.BrandName;
import com.lela.data.domain.entity.BrandNameDataOnDemand;
import com.lela.data.domain.entity.BrandNameIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect BrandNameIntegrationTest_Roo_IntegrationTest {
    
    declare @type: BrandNameIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: BrandNameIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: BrandNameIntegrationTest: @Transactional;
    
    @Autowired
    BrandNameDataOnDemand BrandNameIntegrationTest.dod;
    
    @Test
    public void BrandNameIntegrationTest.testCountBrandNames() {
        Assert.assertNotNull("Data on demand for 'BrandName' failed to initialize correctly", dod.getRandomBrandName());
        long count = BrandName.countBrandNames();
        Assert.assertTrue("Counter for 'BrandName' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void BrandNameIntegrationTest.testFindBrandName() {
        BrandName obj = dod.getRandomBrandName();
        Assert.assertNotNull("Data on demand for 'BrandName' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandName' failed to provide an identifier", id);
        obj = BrandName.findBrandName(id);
        Assert.assertNotNull("Find method for 'BrandName' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'BrandName' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void BrandNameIntegrationTest.testFindAllBrandNames() {
        Assert.assertNotNull("Data on demand for 'BrandName' failed to initialize correctly", dod.getRandomBrandName());
        long count = BrandName.countBrandNames();
        Assert.assertTrue("Too expensive to perform a find all test for 'BrandName', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<BrandName> result = BrandName.findAllBrandNames();
        Assert.assertNotNull("Find all method for 'BrandName' illegally returned null", result);
        Assert.assertTrue("Find all method for 'BrandName' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void BrandNameIntegrationTest.testFindBrandNameEntries() {
        Assert.assertNotNull("Data on demand for 'BrandName' failed to initialize correctly", dod.getRandomBrandName());
        long count = BrandName.countBrandNames();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<BrandName> result = BrandName.findBrandNameEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'BrandName' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'BrandName' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void BrandNameIntegrationTest.testFlush() {
        BrandName obj = dod.getRandomBrandName();
        Assert.assertNotNull("Data on demand for 'BrandName' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandName' failed to provide an identifier", id);
        obj = BrandName.findBrandName(id);
        Assert.assertNotNull("Find method for 'BrandName' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyBrandName(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'BrandName' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void BrandNameIntegrationTest.testMergeUpdate() {
        BrandName obj = dod.getRandomBrandName();
        Assert.assertNotNull("Data on demand for 'BrandName' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandName' failed to provide an identifier", id);
        obj = BrandName.findBrandName(id);
        boolean modified =  dod.modifyBrandName(obj);
        Integer currentVersion = obj.getVersion();
        BrandName merged = (BrandName)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'BrandName' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void BrandNameIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'BrandName' failed to initialize correctly", dod.getRandomBrandName());
        BrandName obj = dod.getNewTransientBrandName(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'BrandName' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'BrandName' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'BrandName' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void BrandNameIntegrationTest.testRemove() {
        BrandName obj = dod.getRandomBrandName();
        Assert.assertNotNull("Data on demand for 'BrandName' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'BrandName' failed to provide an identifier", id);
        obj = BrandName.findBrandName(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'BrandName' with identifier '" + id + "'", BrandName.findBrandName(id));
    }
    
}
